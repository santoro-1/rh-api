# SeedVR2 视频清晰化流程开发文档

> 文档状态：已按本文实现、通过完整 mock 回归，并完成一次经用户授权的真实 48G 付费冒烟
> 编写日期：2026-08-10
> 涉及项目：`runninghub_mvp`、`jyd_plain_json_probe`
> RunningHub AI App：SeedVR2 放大（`2064116518987845634`）

## 1. 目标与范围

数字人工作流输出的视频在下载并再次导入剪映后会出现可见清晰度损失。本次改造在每个数字人分段生成完成后，强制增加一次 SeedVR2 放大：

```text
数字人分段生成
  -> 下载数字人原始片段
  -> 上传原始片段到 SeedVR2
  -> SeedVR2 生成并下载清晰片段
  -> 页面/工作台输出清晰片段
  -> 多片段合并或工作台后期
```

本文中的“新老都要更新”定义为：所有当前仍在使用的数字人生成入口都走清晰化，不是对历史已经完成的视频自动补跑。

纳入范围：

- 旧网页单条数字人生成。
- 旧网页快速批量生成，包括上传音频和 MiniMax 文本语音。
- 旧网页长音频切分后的所有数字人子任务。
- 新版剪映工作台 4A 创建的所有数字人分段任务。
- 功能上线时尚未完成、之后才取得数字人成功结果的在途任务。

不纳入范围：

- LTX 2.3 工作流及其结果。
- 已经处于 `SUCCESS` 的历史数字人任务；如以后需要补跑，另做人工、可计费的批量工具。
- 在本地剪映工作台再次调用 SeedVR2。清晰化只由云端数字人网站执行一次。
- 未经用户当次明确授权的生产部署、生产账号修改或真实 RunningHub 付费调用。

## 2. 已确认的固定参数

SeedVR2 请求以工作区根目录的 `SeedVr2放大api文档.md` 为接口依据，固定参数如下：

| 项目 | 固定值 | 说明 |
| --- | --- | --- |
| API | `POST /openapi/v2/run/ai-app/2064116518987845634` | RunningHub AI App 调用；App ID 位于 URL 路径中 |
| AI App ID | `2064116518987845634` | SeedVR2 放大流 |
| `instanceType` | `plus` | **固定 48G，不允许降级为 24G/default** |
| `usePersonalQueue` | `false` | 实际 JSON 布尔值，不使用字符串 |
| 节点 `46.video` | 上传后返回的文件名 | 每个数字人分段各上传一次 |
| 节点 `108.select` | `"1"` | 固定模型选项 |
| 节点 `112.value` | `"1920"` | 固定输出尺寸参数 |
| 查询 | `POST /api/openapi/v2/query` | 复用标准 v2 查询 |

`instanceType` 必须在适配器内作为常量写死，不能由浏览器、本地工作台或普通用户请求覆盖。配置和日志中均应明确显示 `plus/48G`。

接口文档的通用成功示例展示的是 PNG，不能据此认定 SeedVR2 的真实输出类型。适配器必须严格选择视频结果，并用 mock 测试覆盖多结果、非视频结果和空结果。

2026-08-10 经用户明确授权，使用 5 秒真人语音执行了一次完整真实付费冒烟，结果如下：

| 阶段 | RunningHub 任务 ID | 输出节点/类型 | 媒体结果 | 消耗 |
| --- | --- | --- | --- | --- |
| 数字人 `plus/48G` | `2086735069879431170` | `348` / `mp4` | H.264 + AAC，576×1024，4.84 秒 | 130 RH 币，工作流 325 秒 |
| SeedVR2 `plus/48G` | `2086736444621291521` | `97` / `mp4` | H.264 + AAC，1080×1920，4.84 秒 | 97 RH 币，工作流 242 秒 |

两阶段总墙钟耗时 574.016 秒。源片段和清晰片段抽帧检查通过，未发现黑帧、比例拉伸或构图异常；音轨保留且时长无漂移。因此当前适配器应把 SeedVR2 的真实视频结果识别为节点 `97`、`outputType=mp4`，但仍不得只写死节点编号，必须继续按唯一视频输出选择，以兼容工作流后续调整。

## 3. 核心产品决策

### 3.1 每个分段一对一放大

SeedVR2 的输入输出粒度与数字人子任务一致。10 个数字人分段就创建 10 个放大任务，不先合并再放大。这样可以：

- 保留原来的分段重试、取消和工作台下载语义。
- 某一段失败时只重试该段，不重复消费其他成功段。
- 避免超长合并视频增加单次任务失败面和等待时间。

### 3.2 原始片段与清晰片段都保留

- 数字人工作流结果称为“源片段”。
- SeedVR2 结果称为“清晰片段”或“最终片段”。
- `generation_tasks.result_path` 最终只指向清晰片段，保持现有页面、合并器和工作台接口默认获取最终结果。
- 源片段保存到新的清晰化记录中，不覆盖、不删除，并提供独立的受权限控制下载地址。
- 多片段合并必须使用清晰片段；源片段只用于追溯和 SeedVR2 阶段重试。

### 3.3 清晰化是数字人任务的必经阶段

启用本功能后，数字人远端任务成功不再等于本站子任务成功。只有 SeedVR2 结果下载完成后，父 `GenerationTask` 才进入 `SUCCESS`。

数字人源片段已成功但 SeedVR2 尚未成功时：

- 父任务保持现有内部 `RUNNING`，避免破坏旧枚举和旧消费者。
- 对页面和工作台输出派生阶段 `VIDEO_ENHANCING`。
- 不触发多片段合并，不允许工作台将该段当成可下载的最终结果。

### 3.4 兼容新旧入口，不改入口请求契约

所有入口继续创建原来的 `GenerationTask`。调用方不需要提供 SeedVR2 参数，也不能选择跳过清晰化。Worker 根据任务的工作流类型自动接续放大阶段，因此旧网页与新版工作台共享同一条实现，不复制业务逻辑。

## 4. 数据模型

新增迁移建议命名为：

```text
0029_seedvr2_video_enhancement
```

迁移只新增表、索引和必要的可空字段，不重建 SQLite 父表，不改变现有任务 ID、状态值和历史结果路径。

### 4.1 `generation_task_enhancements`

每个数字人 `generation_task` 最多一条当前清晰化记录：

| 字段 | 含义 |
| --- | --- |
| `id` | 清晰化记录主键 |
| `generation_task_id` | 唯一外键，一对一关联数字人子任务 |
| `provider` | 固定 `runninghub` |
| `workflow_kind` | 固定 `seedvr2_upscale` |
| `status` | 清晰化阶段状态 |
| `source_result_path` | 数字人源片段的服务器路径 |
| `source_filename` / `source_size` / `source_sha256` | 上传幂等与审计信息 |
| `remote_task_id` | 当前 SeedVR2 RunningHub 任务 ID |
| `execution_account_id` | 实际提交 SeedVR2 的 RunningHub 执行账号 |
| `result_path` | 清晰片段路径；成功后同步到父任务 `result_path` |
| `result_filename` / `result_size` / `result_sha256` | 最终结果审计信息 |
| `output_metadata_json` | RunningHub 输出类型、节点、URL 等原始元数据 |
| `error_message` / `failed_reason` | 用户可读错误和程序分类 |
| `auto_retry_count` / `auto_retry_after` | 仅清晰化阶段的自动重试控制 |
| `submitted_at` / `started_at` / `finished_at` | 看门狗和耗时统计 |
| `created_at` / `updated_at` | 审计时间 |

清晰化状态使用独立枚举：

```text
PENDING -> UPLOADING -> SUBMITTED -> RUNNING -> SUCCESS
                                      |          |
                                      v          v
                                    FAILED   DOWNLOAD_FAILED

任意未完成状态 -> CANCELLED
```

`DOWNLOAD_FAILED` 表示远端已经成功，只允许重下同一远端结果，不得重新提交付费任务。

### 4.2 `generation_task_enhancement_attempts`

建议同时新增不可变的尝试记录，保存每一次实际提交或提交前失败：

- 关联清晰化记录和尝试序号。
- 保存执行账号、远端任务 ID、提交载荷摘要、起止时间、终态和失败原因。
- 每个已取得 `remote_task_id` 的尝试只能轮询、取消或下载该 ID，不能换账号后覆盖。
- 自动重试或人工重试创建新尝试，历史尝试不删除。

该表用于费用审计、资源池故障分析和防止“网络结果不明时重复付费提交”。

## 5. Worker 流程

### 5.1 主流程

Worker 处理数字人任务时按持久化阶段恢复，而不是每次都从数字人提交开始：

1. 正常上传数字人素材并提交原数字人工作流。
2. 轮询数字人任务；成功后严格选择视频输出。
3. 将视频下载到新的源片段路径，计算大小和 SHA-256。
4. 在事务中创建或恢复 `generation_task_enhancements`；父任务仍不成功。
5. 上传源片段到 RunningHub，取得 SeedVR2 所需文件名。
6. 使用固定 48G 参数提交 SeedVR2，并立即持久化远端任务 ID。
7. 轮询 SeedVR2；成功后严格选择视频输出。
8. 下载到临时文件，完成视频有效性、大小和哈希检查后原子移动到最终路径。
9. 在同一事务中把清晰化记录置为 `SUCCESS`，把父任务 `result_path` 指向清晰片段并置为 `SUCCESS`。
10. 仅当批次全部子任务都完成第 9 步后，才进入预览合并或通知工作台下载。

源片段和清晰片段必须使用不同目录或稳定后缀，不能原地覆盖。例如：

```text
outputs/tasks/<task_id>/source/digital-human.mp4
outputs/tasks/<task_id>/enhanced/seedvr2.mp4
```

具体目录需复用项目现有安全路径工具，并防止路径穿越。

### 5.2 重启幂等

每一步先读取数据库阶段，再决定下一动作：

- 已有源文件且哈希匹配：不重新下载数字人结果。
- 已有 SeedVR2 `remote_task_id`：只查询该任务，不重新提交。
- SeedVR2 已远端成功但本地下载失败：只重新下载。
- 清晰片段已校验并落盘但事务中断：根据文件和记录完成数据库收口，不重新调用 RunningHub。
- 清晰化已成功：不得因重复队列消息或 Worker 重启再次提交。

任何无法确认提交是否被 RunningHub 接收的网络异常，沿用现有安全策略：标记为结果不明确并等待人工处理，不自动重复付费。

### 5.3 重试策略

重试必须按失败阶段隔离：

| 失败位置 | 重试行为 |
| --- | --- |
| 数字人提交或生成失败 | 沿用现有数字人重试，从数字人阶段重试 |
| 数字人结果下载失败 | 重下同一数字人远端结果 |
| SeedVR2 上传前失败 | 复用已保存源片段，再次上传 |
| SeedVR2 明确返回 `FAILED` | 复用源片段，只创建新的 SeedVR2 尝试 |
| SeedVR2 查询暂时失败 | 保留远端 ID，继续查询，不重提 |
| SeedVR2 结果下载失败 | 重下同一 SeedVR2 结果，不重提 |

自动重试次数和 60/120/240 秒退避可复用现有默认策略，但数字人与 SeedVR2 分别计数。人工重试成功段时直接返回幂等成功，不重复消费。

### 5.4 取消与看门狗

- 取消父任务时，先判断当前活动阶段；只取消该阶段已持久化的远端任务。
- SeedVR2 已提交时取消 SeedVR2，不回头取消已经成功的数字人任务。
- 看门狗分别使用数字人和清晰化的 `submitted_at`，不能因切换阶段错误累计为一个超时周期。
- 远端成功、本地下载中的任务不能被当成远端超时再次提交。
- 批次取消需覆盖仍未开始清晰化和正在清晰化的所有子任务。

## 6. RunningHub 账号与并发

### 6.1 账号绑定

- 旧入口沿用该用户现有 RunningHub 配置。
- 新版工作台任务不得突破创建任务时冻结的资源池账号快照。
- SeedVR2 首次尝试优先复用该数字人子任务实际使用的执行账号。
- 第一版保持使用该数字人子任务已经绑定的执行账号；提交前容量不足时等待，安全失败时按同账号重试，不跨账号改约，因此不会突破冻结的资源池账号快照。一旦取得远端任务 ID，该尝试更不得切换账号。
- 每个尝试记录实际账号，便于查询、取消、费用和故障追踪。

### 6.2 本地并发语义

一个数字人分段从数字人生成到 SeedVR2 完成视为一个逻辑任务。第一版保持现有本地槽位语义，不额外开放无上限的放大并发。以后如需把数字人和放大拆为两类并发池，必须基于真实耗时和 RunningHub 配额另行设计。

## 7. 云端接口与页面

### 7.1 现有接口的兼容行为

- 原有任务详情、结果下载、批次结果和工作台分段视频接口保持 URL 和主要响应结构兼容。
- 原来返回 `task.result_path` 的接口，在清晰化成功后自然返回 SeedVR2 清晰片段。
- 清晰化未成功时不得回退返回源片段伪装为最终结果。
- 合并接口只读取成功任务的清晰片段。

### 7.2 新增阶段字段

内部父状态不扩枚举，外部任务/工作台响应新增或补全派生字段：

```json
{
  "processing_stage": "VIDEO_ENHANCING",
  "enhancement_status": "RUNNING",
  "quality_variant": "seedvr2_upscaled"
}
```

建议阶段值至少包括：

```text
DIGITAL_HUMAN_RUNNING
VIDEO_ENHANCING
VIDEO_MERGING
BASE_VIDEO_READY
COMPOSITION_FAILED
```

错误响应需要区分 `DIGITAL_HUMAN_FAILED` 与 `VIDEO_ENHANCEMENT_FAILED`，页面提示“数字人生成失败”或“视频清晰化失败”，不能都显示为模糊的生成失败。

### 7.3 源片段下载

新增受原任务权限校验保护的源片段下载接口，建议：

```text
GET /api/tasks/{task_id}/source-video
GET /api/workbench/tasks/{item_id}/videos/{video_index}/source
```

主结果接口仍返回清晰片段。工作台清单中的单段视频可增加：

```json
{
  "download_url": ".../videos/0",
  "source_download_url": ".../videos/0/source",
  "quality_variant": "seedvr2_upscaled",
  "processing_stage": "BASE_VIDEO_READY"
}
```

下载权限、路径白名单、任务归属和媒体类型校验必须与现有结果接口一致。

### 7.4 页面显示

旧网页和新版任务详情均增加可辨识状态：

- “数字人生成中”
- “视频清晰化中（SeedVR2 48G）”
- “清晰化失败，可重试清晰化”
- “清晰视频已完成”

任务详情同时提供“下载清晰片段”和次要入口“下载数字人源片段”。默认按钮、预览播放器和批量下载均使用清晰片段。

## 8. 本地剪映工作台改造

本地项目 `D:\工作内容\轻盈健\公寓\jyd_plain_json_probe` 只做契约与状态兼容，不调用 SeedVR2：

1. 将 `VIDEO_ENHANCING` 加入远端仍在处理的活动阶段集合。
2. 轮询期间显示“云端视频清晰化中”，不提前判定失败或尝试下载。
3. `BASE_VIDEO_READY` 后继续使用现有分段下载接口，取得的已经是清晰片段。
4. 下载资产继续保持现有有序分段角色；在资产元数据中记录：

   ```json
   {
     "quality_variant": "seedvr2_upscaled",
     "enhanced_by": "runninghub_seedvr2",
     "source_is_available_on_cloud": true
   }
   ```

5. 本地基础视频合并、字幕、BGM、B-roll、后期变体和剪映草稿都自动消费清晰片段，不改时间轴语义。
6. 工作台不默认下载云端源片段，避免本地磁盘翻倍；需要诊断时可通过新增源片段 URL 人工获取。

这项改造不得影响已经在本地落盘的旧项目，也不对旧项目素材执行替换或重下载。

## 9. 代码改造清单

### 9.1 `runninghub_mvp`

实现时预计涉及以下模块，最终以当前代码结构为准：

- 新增 `app/workflows/seedvr2_upscale.py`：固定节点参数、48G 提交和严格视频结果解析。
- 数据库模型与 `0029` 迁移：清晰化记录和尝试记录。
- `task_worker.py`：在数字人成功后接续清晰化阶段，并支持重启恢复。
- RunningHub 客户端：复用上传、查询、取消和下载；不得复制密钥处理。
- 重试、取消、看门狗和清理服务：改成识别当前阶段。
- `video_merge.py`：明确只合并最终清晰片段。
- 旧网页任务/批次接口与模板：显示清晰化状态并提供源片段下载。
- 工作台 API 与响应模型：输出 `VIDEO_ENHANCING` 和质量元数据。
- 清理与删除：同时安全删除源片段、清晰片段及关联数据库记录。
- README、开发者指南、集成说明和 CHANGELOG：实现完成后同步更新实际行为。

### 9.2 `jyd_plain_json_probe`

- `ProjectCompositionCoordinator`：把 `VIDEO_ENHANCING` 视为远端活动状态。
- 云端响应解析和 UI 状态文案：兼容新增字段，未知字段保持向前兼容。
- 分段素材元数据：记录 `quality_variant` 与来源。
- `docs/WEB_API.md`、开发者指南、数字人集成文档和项目状态：实现完成后同步更新。

## 10. 测试计划

自动化测试不得调用真实 RunningHub，必须 mock 上传、提交、查询、取消和下载。

### 10.1 适配器测试

- 请求固定使用路径 `/openapi/v2/run/ai-app/2064116518987845634`。
- 请求固定使用 `instanceType=plus`，不存在 `default` 回退分支。
- 节点 `46/108/112` 的名称和值完全正确。
- `usePersonalQueue` 是 JSON 布尔 `false`。
- 多输出时只选视频；只有图片、空结果或多个歧义视频时安全失败并留下诊断信息。

### 10.2 流程测试

- 旧网页单片段：数字人成功后进入清晰化，最终下载清晰片段。
- 旧网页批量和长音频：每段一对一清晰化，全部完成后才合并。
- 新工作台 4A：看到 `VIDEO_ENHANCING`，就绪后下载清晰片段。
- 任一清晰化段失败：不合并，不把源片段冒充最终结果。
- 成功段不因其他段重试而重复提交。
- LTX 任务完全不进入清晰化表。
- 迁移前已完成任务保持原结果和下载行为，不自动付费补跑。
- 迁移时尚未完成的数字人任务在远端成功后正常进入清晰化。

### 10.3 容错与费用保护测试

- Worker 在上传后、提交后、远端成功后和文件落盘后分别崩溃，重启均能从正确阶段恢复。
- 已有 SeedVR2 远端 ID 时绝不重复提交。
- 下载失败只重下；明确生成失败才允许创建新尝试。
- 查询超时与提交结果不明不会触发付费重提。
- 取消只作用于当前远端阶段。
- 看门狗使用清晰化自己的开始时间。
- 资源池改约不突破工作台任务冻结账号快照。

### 10.4 文件与接口测试

- 源片段和清晰片段均保留，哈希和路径正确。
- 主下载、预览、批量下载和合并全部使用清晰片段。
- 源片段接口有登录、归属和路径越界保护。
- 清理任务不会遗漏文件，也不会删除任务目录外内容。
- 本地工作台对新增状态持续轮询，对旧响应仍兼容。

### 10.5 验证顺序

1. 两个项目各自的目标单元测试。
2. 两个项目完整回归测试。
3. 本地 mock 端到端：旧单片段、旧多片段、新工作台各一条。
4. 已在用户明确同意后，使用一个极短视频完成一次真实 48G 付费冒烟。
5. 已根据真实输出回写节点和媒体元数据；生产发布仍必须使用安全发布脚本现场预检与验收。

## 11. 实施顺序

1. 新增数据库迁移、模型、清晰化状态和仓储方法。
2. 新增 SeedVR2 适配器及纯 mock 单元测试，锁死 48G 参数。
3. 改造 Worker 为数字人/清晰化两阶段可恢复状态机。
4. 接入分阶段重试、取消、看门狗、清理和费用保护。
5. 更新旧网页所有入口、页面状态、最终结果与源片段接口。
6. 更新新版工作台 API 的阶段和质量元数据。
7. 修改本地剪映工作台的活动状态与资产元数据兼容。
8. 完成两个项目的目标测试和完整回归。
9. 经用户授权执行一次真实 48G 短片冒烟；未经授权停在 mock 验收。
10. 用户另行明确要求后才部署生产环境。

数据库和适配器先落地，但在完整状态机与接口尚未完成前，不应在生产配置中启用强制清晰化，避免出现半流程任务。

## 12. 验收标准

以下条件全部满足才算开发完成：

- 所有现行数字人新旧入口都按“数字人 -> SeedVR2 -> 页面/工作台”执行。
- 每一个数字人分段只对应自己的 SeedVR2 放大任务。
- 所有 SeedVR2 提交均可由测试和日志证明使用 `plus/48G`。
- 页面、主下载、合并和工作台默认得到 SeedVR2 清晰片段。
- 数字人源片段仍保留并可在有权限时单独下载。
- 清晰化失败不会重复数字人生成；下载失败不会重复 SeedVR2 生成。
- Worker 重启、网络抖动、取消、超时和多段部分失败不会造成重复付费提交。
- 新工作台能正确等待 `VIDEO_ENHANCING`；旧本地项目不被重写。
- LTX 与历史已完成数字人任务不受影响。
- 两个项目完整自动化测试通过；任何真实付费调用均有用户当次明确授权。

## 13. 已确认实施边界

本方案已经用户确认并按以下决策完成本地实现：

- “新老都更新”按第 1 节的现行入口范围执行。
- 历史已完成任务不自动补跑。
- 原始片段保留，默认输出切换为清晰片段。
- SeedVR2 固定 48G、1920 参数，每段一对一处理。
- 不对 LTX 增加放大。
- 不部署生产、不做真实付费联调，除非用户另行明确授权。
