# 工作台请求风暴与页面卡顿修复开发文档

> 文档状态：本地代码实施完成，待灰度与部署  
> 建立日期：2026-09-03  
> 适用项目：`runninghub_mvp`、`jyd_plain_json_probe`  
> 本期范围：登录校验请求合并、连接失败快速保护、历史任务分页、缩略图按需加载、状态局部轮询  
> 明确不做：确认请求来自哪台物理电脑、客户端安装编号、客户端实例编号、按设备追踪

## 1. 背景

2026-09-03 对一台工作台电脑的页面卡顿问题进行本地日志与生产服务器访问日志联合排查。
生产服务器自身没有宕机或重启：Web、Worker、媒体服务和 Nginx 均保持运行，服务器健康检查正常，
事发时间段也没有应用异常堆栈或 Nginx 严重错误。

问题时间段内，同一个公网出口地址在约四分钟内出现 334 次请求。根据 User-Agent 和请求路径可拆成
三组不同来源：

| 请求来源 | 数量 | 主要用途 |
| --- | ---: | --- |
| `Python-urllib/3.11` | 190 | 本地 JYD 工作台访问账号中心和工作台接口 |
| Edge 151 | 105 | 生产网站 `/tasks` 历史任务页及其缩略图 |
| Edge 152 | 39 | 生产网站批次页面和状态轮询 |

主要路径统计包括：

- `/api/auth/center/verify`：166 次；
- `/api/tasks/{id}/image`：84 次，其中 83 次返回 404、1 次返回 200；
- `/api/batches/{id}`：45 次；
- `/api/workbench/h3-batches/{id}`：13 次。

14:36:47，Edge 151 从 `/batches` 进入 `/tasks`。历史任务页随即在同一秒请求了几乎所有数字人
任务的参考图。当前页面没有分页，模板又为每条数字人任务直接生成图片地址，因此浏览器会一次性
并发请求全部缩略图。大部分旧任务的本地图片已经清理，最终形成大量 404。

本地工作台的另一股请求来自账号中心校验。当前受保护的本地接口在进入中间件时会远程校验登录，
部分项目接口内部还会再次取得用户。页面同时加载项目状态、音频状态和多条预览时，会在很短时间内
创建多次远程登录校验。日志中曾出现同一秒 16 次校验请求；发生网络连接问题时，每个调用又可能独立
等待约 15～21 秒，使页面表现为卡住。

现有熔断保护针对 H3 后台下载及其主机失败，不覆盖账号中心登录校验，也不控制生产网站浏览器的
缩略图请求。因此“已经有熔断”与本次请求增长并不矛盾。

## 2. 问题定义

本次需要解决四个相互关联但边界不同的问题：

1. 同一登录令牌的多个本地请求会重复访问账号中心。
2. 账号中心连接异常时，多个请求分别长时间等待并继续堆积。
3. `/tasks` 一次读取并渲染全部历史任务，导致浏览器同时加载全部图片。
4. 存在运行中任务时，历史任务页使用整页刷新，可能反复触发 HTML 和图片加载。

批次详情的正常状态轮询需要保留。不能把所有短时间内的多个请求都当成异常，也不能简单使用公网 IP
限流，因为同一公网出口后可能存在多台正常工作的电脑。

## 3. 目标与验收摘要

实施完成后应满足：

- 同一进程、同一 token 在 5 秒有效期内最多进行一次远程登录校验；
- 同一 token 的并发校验共享一个正在执行的远程调用；
- 一个本地请求不会在中间件和接口依赖中重复校验同一 token；
- 账号中心连续发生连接失败后进入短时保护，新请求快速返回，不继续创建长等待连接；
- `/tasks` 默认每页只显示 20 条，最大每页 50 条；
- 图片不存在时页面直接显示占位内容，不再请求一个必然返回 404 的地址；
- 存在的图片使用懒加载和浏览器缓存；
- 运行中任务只更新状态区域，不再每 5 秒执行 `location.reload()`；
- 不改变 H3 下载熔断、ASR、声音生成、内容分析、视频生成和视频下载的业务合同。

## 4. 范围与非目标

### 4.1 本期实施范围

本地 JYD 工作台：

- 登录校验短时成功缓存；
- 同 token 并发请求合并；
- 请求内用户结果复用；
- 登录中心连接失败的短时保护和半开探测；
- 登录、退出、换号时的缓存失效；
- 对应日志、单元测试和接口回归。

生产数字人网站：

- 历史任务服务端分页；
- 当前页缩略图存在性判断；
- 图片懒加载、尺寸固定和缓存；
- 运行中任务的轻量状态接口；
- 前端局部轮询、可见性暂停和单请求约束；
- 对应路由、模板和权限回归。

### 4.2 本期明确不做

- 不生成或保存客户端安装编号；
- 不增加客户端实例编号或设备追踪请求头；
- 不根据日志自动判断请求来自哪台物理电脑；
- 不修改 Nginx 日志格式来记录设备身份；
- 不按公网 IP 对账号中心实施严格限流；
- 不取消图片、视频、预览等接口的权限校验；
- 不把账号中心保护与现有 H3 下载熔断合并；
- 不修改云端账号数据库，不新增数据库迁移；
- 本文档阶段不部署生产环境，也不构建更新包。

服务器日志仍可通过公网 IP、时间、路径和 User-Agent 区分请求类型，但同一 NAT 后具体是哪台电脑不在
本期解决范围内。

## 5. 总体设计

```text
本地浏览器的多个受保护请求
              │
              ▼
       本地认证中间件
              │
       ┌──────┴──────┐
       │请求内已有用户│──是──→ 直接复用
       └──────┬──────┘
              │否
              ▼
       token 成功缓存（5 秒）
              │未命中
              ▼
       同 token 并发合并
              │仅首个请求外发
              ▼
       登录中心失败保护
              │允许探测
              ▼
     /api/auth/center/verify
```

```text
浏览器打开 /tasks
        │
        ▼
服务端权限过滤 + 日期过滤 + 分页
        │
        ▼
只检查当前页缩略图是否存在
        │
        ▼
渲染最多 20 条任务
        │
        ├── 图片存在：懒加载并使用缓存
        └── 图片缺失：直接显示占位块

运行中任务 ──→ 每 5 秒请求小型状态接口 ──→ 只更新对应卡片
```

## 6. 本地工作台认证改造

涉及代码：

- `D:\工作内容\轻盈健\公寓\jyd_plain_json_probe\src\jyd_probe\auth_center.py`
- `D:\工作内容\轻盈健\公寓\jyd_plain_json_probe\src\jyd_probe\web_api.py`
- 工作台现有认证和新前端测试文件

### 6.1 成功缓存

在账号中心客户端的共享实例内维护进程级缓存。建议缓存结构至少包含：

- token；
- 已验证用户的只读副本；
- 使用 `time.monotonic()` 计算的到期时间。

规则：

- 成功结果默认有效 5 秒；
- 缓存只存在内存，不落盘；
- token 不得写入日志；
- 返回给调用方时不能允许其修改缓存中的原对象；
- 每次访问顺便清理已过期条目，避免长期运行导致无界增长；
- 可以设置小型硬上限，超过上限优先清理过期和最旧条目。

安全边界：缓存只缩短相同 token 的重复远程验证，不延长 token 本身的有效期。账号被停用后最多存在
5 秒传播延迟，超过缓存有效期必须重新访问账号中心。

### 6.2 同 token 并发合并

FastAPI 的同步依赖可能在线程池中同时执行，不能只使用普通的未加锁字典。实现需要使用线程安全的
锁与等待对象，并遵循：

1. 第一个缓存未命中的调用登记为该 token 的执行者；
2. 后续相同 token 调用加入同一个进行中状态；
3. 只有执行者访问远程账号中心；
4. 成功、401 或网络异常都必须唤醒所有等待者；
5. 等待者取得相同结果或相同错误；
6. 无论发生什么异常，都必须清理进行中状态，不能留下永久等待；
7. 不同 token 允许并行校验，不能被全局串行锁相互阻塞。

不建议把 token 的摘要写入普通 INFO 日志。测试需要确认并发合并行为，但生产日志只记录汇总计数。

### 6.3 请求内结果复用

认证中间件校验成功后，将当前用户保存到 `request.state`。项目接口取得用户时按以下顺序处理：

1. 检查 `request.state` 是否已有认证用户；
2. 已存在则直接复用；
3. 不存在时才从 cookie 读取 token 并调用账号中心客户端；
4. 取得结果后同样写回 `request.state`。

必须确保管理员本地认证和数字人网站账号认证仍使用各自原有规则，不能把两类用户混在同一个字段中。

### 6.4 缓存失效

以下事件必须清除相关 token 的成功缓存和进行中状态引用：

- 用户主动退出；
- 新账号登录成功；
- 登录交接写入新 token；
- token 被账号中心明确判定为无效；
- 本地认证配置发生切换；
- 服务正常关闭。

发生 401/403 时必须立即移除成功缓存。网络超时不能伪装成登录成功，也不能无限使用过期成功结果。

### 6.5 登录中心失败保护

这一保护独立于 H3 下载熔断，建议初始参数：

| 参数 | 建议值 |
| --- | ---: |
| 连续网络失败阈值 | 3 次 |
| 统计窗口 | 10 秒 |
| 打开时长 | 15 秒 |
| 半开探测并发 | 1 个 |
| 成功缓存时间 | 5 秒 |

只把连接超时、连接拒绝、DNS 失败、TLS 建连失败等传输异常计入失败。以下情况不计入：

- 401、403；
- 正常返回的业务校验失败；
- 本地输入为空；
- 调用方取消请求。

打开期间：

- 尚在 5 秒成功缓存内的请求仍可复用成功结果；
- 没有有效成功缓存的请求立即返回统一 503；
- 不再创建新的远程连接；
- 到期后只允许一个请求进入半开探测；
- 探测成功立即关闭保护并恢复正常；
- 探测失败重新打开 15 秒。

错误提示应描述为“暂时无法连接数字人账号中心，请稍后重试”，不再把所有网络异常都描述为“网站没有
启动”。

### 6.6 超时边界

账号校验是短请求，不应复用内容分析或文件下载的长超时。建议将账号校验总等待控制在 5～8 秒内。
内容分析、声音生成查询、视频下载和 H3 下载继续保留各自独立超时。

如果当前 HTTP 客户端无法区分连接超时和读取超时，本期可先使用单一 8 秒上限；后续更换客户端时再
细分，不应为了细分超时扩大本次改造范围。

### 6.7 本地日志

增加以下结构化事件：

- `auth_verify.remote_started`；
- `auth_verify.remote_succeeded`；
- `auth_verify.remote_failed`；
- `auth_verify.breaker_opened`；
- `auth_verify.breaker_recovered`。

缓存命中和并发合并频率较高，不逐条写 INFO。每 60 秒输出一次汇总：

- 受保护请求数量；
- 缓存命中数量；
- 加入进行中请求的数量；
- 实际远程校验数量；
- 远程成功、401 和网络失败数量；
- 平均、P95 和最大远程耗时；
- 当前保护状态。

任何日志都不得包含 token、cookie、密码或完整请求体。

## 7. 历史任务页改造

涉及代码：

- `app/routes/tasks.py`
- `app/templates/tasks.html`
- 必要的页面样式和任务路由测试

### 7.1 服务端分页

`GET /tasks` 增加：

- `page`：从 1 开始，默认 1；
- `page_size`：默认 20，只允许 20 或 50；
- 原有 `start_date`、`end_date` 参数保持不变。

查询顺序必须是：

1. 排除批次子分段；
2. 应用当前用户权限；
3. 应用日期条件；
4. 查询过滤后的总数；
5. 按创建时间倒序；
6. 执行 `offset` 和 `limit`；
7. 只加载当前页记录。

需要处理：

- page 小于 1 时按 400 或统一归一为 1，测试固定一种合同；
- page 超过总页数时显示空页或跳转末页，测试固定一种合同；
- 翻页链接必须保留日期和每页数量；
- 管理员和普通用户的总数必须基于各自权限过滤后计算。

### 7.2 缩略图可用性

不能在 Jinja 模板里直接操作磁盘路径。路由层只对当前页任务执行安全路径解析和文件存在检查，并向
模板提供 `thumbnail_available`。

规则：

- 只有数字人任务需要参考图；
- 路径非法、为空或文件不存在均视为不可用；
- 不可用时渲染固定占位块，不输出 `/api/tasks/{id}/image`；
- 文件检查失败不能导致整个历史任务页 500；
- 每页最多检查 50 条，不扫描任务目录。

图片接口仍保留 404 防御，因为用户可能直接访问旧地址，但正常页面不应再主动制造这些 404。

### 7.3 图片加载和缓存

存在的缩略图标签增加：

- `loading="lazy"`；
- `decoding="async"`；
- 固定宽高或固定宽高比；
- 明确的替代文本。

`/api/tasks/{task_id}/image` 对成功响应增加私有缓存。第一阶段至少增加合理的
`Cache-Control: private, max-age=3600`。如果项目已有统一文件响应 ETag 方案，则复用该方案；不要为
本次功能单独实现一套复杂缓存框架。

### 7.4 状态局部轮询

删除任务页中运行任务存在时的 `location.reload()` 定时逻辑，新增只读取当前页活动任务状态的接口。

建议接口：

```http
GET /api/tasks/statuses?ids=id1,id2,id3
```

响应只包含页面更新所需字段：

```json
{
  "tasks": [
    {
      "id": "task-id",
      "status": "RUNNING",
      "status_label": "生成中",
      "error_message": null,
      "download_available": false,
      "updated_at": "2026-09-03T14:36:40+08:00"
    }
  ]
}
```

接口要求：

- 最多接收当前页允许的 50 个 ID；
- 每个任务继续执行现有权限校验；
- 无权任务不能通过响应泄漏其是否存在；
- 不返回图片、视频或大字段；
- 响应禁止缓存；
- 查询应一次完成，避免逐条数据库查询。

前端轮询规则：

- 初始间隔 5 秒；
- 同一时刻最多一个请求；
- 上一轮未完成时不创建下一轮；
- 页面不可见时停止；
- 页面重新可见后立即执行一次；
- 当前页没有活动任务后停止；
- 任务成功后更新详情、下载按钮；
- 任务失败或取消后更新错误和操作按钮；
- 连续网络失败采用 5、10、20、30 秒退避，成功后恢复 5 秒；
- 页面卸载时取消未完成请求。

如果局部状态变化过大、无法可靠更新整张卡片，可以在某个任务首次进入终态时只刷新一次页面；不能
继续保持固定每 5 秒整页刷新。

## 8. 批次页面轮询检查

Edge 152 的批次轮询数量不构成本次主要异常，但实施时应检查现有批次页面是否满足：

- 一个页面只创建一个轮询器；
- 请求进行中不重复发起；
- 页面不可见时暂停；
- 批次进入成功、失败或取消状态后停止；
- 网络错误使用退避；
- 页面卸载时取消请求。

如果现有实现已经满足，只补测试，不为了形式重写。批次轮询的正常业务频率不能被账号中心保护错误
判定为 H3 下载失败。

## 9. 熔断边界

本次系统中需要明确三类保护：

| 保护对象 | 处理方式 | 是否复用现有 H3 熔断 |
| --- | --- | --- |
| H3 后台视频下载 | 现有自适应并发和主机失败熔断 | 是，保持原逻辑 |
| 登录中心校验 | 成功缓存、同 token 并发合并、短时失败保护 | 否，独立状态 |
| 浏览器历史任务图片 | 分页、存在性判断、懒加载和缓存 | 否，不使用熔断 |

不增加全站统一熔断。内容分析可能正常执行 60～120 秒，视频下载也可能持续更久；如果把这些请求按同一
标准统计，会把正常长任务误判为故障。

## 10. 测试计划

### 10.1 本地认证单元测试

至少覆盖：

1. 50 个并发调用使用同一 token，远程校验只执行一次；
2. 5 秒内后续调用命中成功缓存；
3. 缓存到期后只重新执行一次；
4. 不同 token 分别校验且结果不串用；
5. 中间件与项目用户依赖在同一请求中只校验一次；
6. 登录、退出、换号后缓存立即失效；
7. 401 清除已有成功缓存；
8. 网络异常会唤醒全部并发等待者；
9. 执行者异常退出不会留下永久进行中状态；
10. 三次连接失败后进入保护；
11. 保护期间无有效缓存的请求快速返回；
12. 半开状态只执行一个探测；
13. 探测成功后恢复；
14. 401 和业务错误不计入网络失败；
15. 日志中不存在 token 和 cookie。

### 10.2 工作台接口回归

- 新项目列表、项目详情、声音状态和预览接口权限不变；
- 管理员本地会话不受数字人网站缓存污染；
- 普通用户不能借助缓存访问其他用户项目；
- 内容分析仍使用原有长请求合同；
- 音频、视频和 H3 预览仍能正常读取；
- 账号中心不可用时返回统一、可理解的 503；
- H3 下载现有熔断专项全部继续通过。

### 10.3 历史任务页面测试

1. 200 条记录时默认只渲染 20 条；
2. `page_size=50` 最多渲染 50 条；
3. 非法分页参数按确定合同处理；
4. 普通用户总数和列表均只包含自己的任务；
5. 管理员和日期筛选保持正确；
6. 图片存在时输出懒加载标签；
7. 图片不存在时输出占位块且不包含图片 API 地址；
8. 非法图片路径不会导致页面 500；
9. 状态接口只返回有权查看的任务；
10. 状态接口拒绝超过 50 个 ID；
11. 页面没有活动任务时不启动轮询；
12. 活动任务进入终态后停止轮询；
13. 页面轮询不调用 `location.reload()`；
14. 翻页链接保留日期筛选。

### 10.4 并发联调

使用模拟账号中心和测试数据库完成：

- 一个工作台页面同时请求 30 个受保护接口；
- 一个历史任务页准备 200 条任务，其中大部分图片缺失；
- 账号中心延迟 10 秒或主动拒绝连接；
- 同一个 token 与不同 token 混合请求；
- 页面保持前台、切换后台、重新回到前台。

期望：

- 同 token 的实际远程登录校验数符合缓存窗口；
- 不出现线程永久等待；
- 初次历史页只处理当前页记录；
- 缺失图片不产生 404 请求；
- 状态轮询响应保持小体积；
- 页面输入和滚动不被长网络请求阻塞。

## 11. 实施顺序

### 阶段 A：建立回归基线

1. 固化当前认证、任务列表、任务图片和批次轮询测试；
2. 增加能够复现并发校验和全量缩略图问题的失败测试；
3. 记录改动前远程校验次数、页面请求数和超时耗时。

### 阶段 B：本地认证去重

1. 实现成功缓存；
2. 实现同 token 并发合并；
3. 实现请求内用户复用；
4. 完成失效和线程安全测试；
5. 验证远程校验由并发多次降为一次。

### 阶段 C：本地连接失败保护

1. 分类传输异常和业务错误；
2. 实现关闭、打开、半开状态；
3. 调整登录校验等待上限；
4. 增加汇总日志；
5. 验证网络异常时不会形成新的请求风暴。

### 阶段 D：服务器历史任务页

1. 实现权限过滤后的分页和总数；
2. 实现当前页缩略图可用性；
3. 增加懒加载和缓存；
4. 增加批量状态接口；
5. 用局部轮询替换整页刷新；
6. 完成权限、日期和页面测试。

### 阶段 E：联合回归

1. 运行 RunningHub 任务、认证和页面测试；
2. 运行 JYD 认证、新前端、项目接口、音频和 H3 相关测试；
3. 模拟账号中心慢响应；
4. 模拟大量历史任务和缺失图片；
5. 核对没有数据库迁移和配置破坏。

### 阶段 F：灰度与发布

1. 先部署向后兼容的服务器端页面改动；
2. 验证 `/tasks` 初始图片请求下降且没有批量 404；
3. 再给一台问题电脑安装本地代码更新包；
4. 观察登录校验汇总、超时和页面响应；
5. 验证后再分发到其他电脑。

## 12. 发布物

本次实现最终产生两个独立发布物：

### 12.1 服务器代码部署

包含：

- `/tasks` 分页；
- 缩略图可用性和懒加载；
- 图片缓存；
- 任务状态局部轮询。

按 `runninghub_mvp/deploy/README.md` 执行服务器预检、部署、健康检查和回滚准备。不得在生产服务器直接
编辑代码。

### 12.2 本地代码更新包

包含：

- 账号校验成功缓存；
- 并发合并；
- 请求内用户复用；
- 登录中心失败保护和日志。

继续遵守当前统一更新包规则：代码更新包通过覆盖 `digital-human` 目录安装。本次不修改 ASR，不需要
重复携带或替换已经安装的 ASR 运行时。

两个发布物分别提交、分别验证，不能因为其中一端通过就宣称整个问题已经修复。

## 13. 风险与回滚

### 13.1 认证缓存风险

风险：账号停用最多延迟 5 秒生效。  
控制：TTL 不超过 5 秒；401 立即清除；退出和换号主动失效。  
回滚：关闭缓存和并发合并，恢复原远程校验路径。

### 13.2 并发等待风险

风险：执行远程调用的线程异常后，其他线程永久等待。  
控制：所有出口使用 `finally` 唤醒并清理，等待本身设置上限。  
回滚：关闭进行中请求共享，仅保留成功缓存。

### 13.3 分页兼容风险

风险：管理员误以为任务消失，日期筛选翻页丢失。  
控制：显示总数、页码和清晰翻页；所有链接保留筛选参数。  
回滚：恢复原列表查询和模板，不涉及数据回滚。

### 13.4 局部轮询风险

风险：页面状态更新不完整。  
控制：响应合同覆盖状态、错误、下载可用性；进入终态可允许一次受控刷新。  
回滚：临时恢复人工刷新按钮，不能恢复固定 5 秒整页刷新。

## 14. 完成定义

只有同时满足以下条件才能标记完成：

- 本地认证并发、缓存、失效、失败保护测试全部通过；
- 历史任务分页、权限、图片和状态轮询测试全部通过；
- H3、ASR、声音、内容分析、普通数字人和视频下载相关回归通过；
- 测试证明同 token 并发请求只产生一次远程校验；
- 测试证明缺失缩略图不会产生图片 API 请求；
- 测试证明任务页不再周期性整页刷新；
- 一台问题电脑灰度运行后，不再出现同类登录校验突发和页面长时间卡住；
- 服务器页面灰度后不再出现打开一次历史任务页产生几十个图片 404；
- 文档、变更日志、部署说明和更新包清单与最终实现一致。

本期不以“能够确认具体是哪台电脑”为完成条件，也不实施相应功能。

## 15. 实施记录（2026-09-03）

### 15.1 已完成：本地账号校验请求风暴 P0

已在 `jyd_plain_json_probe` 实现第 6 节的本地认证治理：

- 相同 token 的成功校验结果使用 5 秒、最多 128 项的进程内缓存，并以深拷贝返回；
- 相同 token 的并发缓存未命中只允许一个执行者访问账号中心，其他请求共享结果或错误；
- 不同 token 不使用全局串行锁，可以独立并发；
- 中间件在受控线程池执行阻塞式远程校验，把已验证用户写入请求状态；首次慢校验不会占住页面
  事件循环，项目接口在同一请求内直接复用；
- 登录、退出、云端/本地登录接力、401 和进程关闭会清理相关缓存及进行中引用；
- 账号校验使用独立 8 秒上限，不改变内容分析、声音、视频和 H3 的长请求超时；
- 10 秒内连续 3 次传输失败后打开 15 秒独立保护，半开时只放行一个探测；
- 新增 `auth_verify.remote_started`、`remote_succeeded`、`remote_failed`、
  `breaker_opened`、`breaker_recovered` 和一分钟脱敏汇总；
- 用户提示统一为“暂时无法连接数字人账号中心，请稍后重试”，503 可携带 `Retry-After`；
- 没有生成客户端编号、设备标识或物理电脑识别信息。

专项自动化覆盖 50 路同 token 并发、缓存过期、不同 token 隔离、异常唤醒、意外异常清理、
显式失效、401、熔断、单半开探测、8 秒超时、请求内只校验一次，以及慢校验期间健康接口仍可
响应。实现只修改本地工作台代码，没有数据库迁移、生产部署或更新包构建。

### 15.2 已完成：生产网站历史任务页请求治理

已在 `runninghub_mvp` 完成第 7 节的服务器端代码：

- `/tasks` 在权限、分段排除和日期过滤后计数、排序及分页，默认每页 20 条，只允许 20 或 50 条；
- 页面只检查当前页数字人缩略图，路径为空、越界、不可访问或文件缺失时直接显示占位内容，不再
  输出必然返回 404 的图片地址；
- 可用缩略图增加懒加载、异步解码和固定尺寸，图片成功响应增加一小时私有缓存；
- 新增最多接收 50 个 ID 的批量状态接口，普通用户在查询层按所有者过滤，无权 ID 不出现在响应中，
  并使用 `Cache-Control: no-store`；
- 历史页由固定 5 秒整页刷新改为单请求局部更新，页面隐藏时暂停，回到前台立即查询，网络失败按
  5、10、20、30 秒退避，当前页没有活动任务后停止；
- 状态变化会同步更新标题、错误提示、下载/取消/重试/删除按钮和批量删除资格，不需要刷新整页；
- 没有修改任务状态机、费用规则、数据库结构或其他业务接口。

服务器专项及相关回归分别通过：历史页与认证页面 19 项、任务管理与批次导航 14 项、部署资源
27 项。工作台账号校验专项 13 项及此前扩展认证、项目、H3、音频、内容与下载回归均通过。

### 15.3 尚未完成

尚未进行问题电脑和生产服务器灰度，也没有构建本地更新包、提交代码或部署生产。第 8 节批次页面
在原有单轮询器与终态停止基础上补齐了页面隐藏暂停、未完成请求取消、单请求保护和
5/10/20/30 秒失败退避。只有完成第 14 节列出的实机与服务器灰度后，才能把整项标记为生产完成。
