# 工作台设备授权与免重复激活开发文档

版本：实施设计 v1.20（受控模式切换与 A/B 双机验收工具）
日期：2026-09-01
状态：开发中；正式生产签名根已在受控服务器初始化，只把公钥清单导回并编入 JYD，私钥没有进入源码或客户端。正式根下 RH/JYD/Pu-LTX 三套全量回归分别为 882、1206、61 项通过。首次完整包在全新解压启动时暴露 Windows PowerShell UTF-8 BOM 兼容问题，现已修复生成端和 JYD 读取端；修正版 `...02-full.zip` 已从最终 ZIP 全新解压，双工作台健康、登录首跳、正式公钥诊断和本地 ASR 导入均通过。生产 `ENFORCE` 仍未启用，设备授权数据库未修改，业务代码未部署。真实 TPM/软件保护、GUI、不同 Windows 用户、LTX 付费入口、两机复制拒绝和连续覆盖更新免激活仍待实机验收，因此不能宣称整个产品已经完成防转拷。用户已接管代码更新包，不再把其压缩交付计入本阶段完成项。

## 阅读说明：最终使用体验

本方案做的是“设备授权与防普通转拷”，不是给压缩包加解压密码。下面描述的是完成开发并验收后的体验，不代表现有安装包已经具备这些限制。

- 新电脑：解压并启动 Pu → 登录现有网站账号 → 点击申请设备授权 → 管理员在网站后台批准 → 正常使用。用户不需要手工生成、保管或输入密钥。
- 已批准的电脑：后台自动校验授权。正常覆盖更新、换程序目录、重启、重新登录，不需要管理员再次批准；短期凭据刷新与首次激活是两回事。
- 拷到别人电脑：即使一起拷走配置和授权缓存，也不能继承原电脑的批准关系；新电脑要单独申请。
- 换机或重装系统：如果原设备密钥已经丢失，由管理员核实并办理恢复/换机；不能仅复制一个“激活文件”恢复。
- 使用 250 等处理机：绑定的是实际运行工作台服务的处理机，不是远程桌面或浏览器所在的电脑。多个网站账号使用它时，授权关系分别管理。

“正常升级免激活”是验收硬要求，不是可选优化。更新包不得包含、覆盖或删除设备密钥；设备身份也不得由程序版本或安装路径计算。

负责人可先看第 1、8、11～15 节；开发人员按第 4～10 节理解设计，按第 12.1 节领取后续工作，并用第 13 节逐项验收。第 16～25 节记录不同阶段的实施进度；当前交接状态以第 25 节为准，不能把历史测试记录当作当前工作树或整包发布验收。

本文是设备授权功能的主开发文档，不再另建一份互相冲突的“加密方案”。v1.10 只核对并整理现有工作树的开发交接，没有新增程序实现、生成真实密钥、打包或部署；此前代码进展和测试证据分别记录，未验证的部分不标记为完成。

v1.11 已继续接入软件初始化许可、独立辅助程序验签、本机受限管道、稳定密钥定位及显式页面申请，并补充模拟与跨端验证；没有执行真实设备初始化或启用生产保护。第 19 节保留为 v1.10 的历史交接快照。

v1.12 为三处实际命令行入口补上合法账号登录和本机持钥会话，不保存密码/令牌，不重新登记设备。本阶段只新增客户端代码及服务端合同测试，没有新增云端业务接口、迁移或生产配置。独立 Agent 的共用密码不能作为设备证明，其后续接力协议尚未实现。

v1.13 是本次文档整理：核对工作树中已有的 Agent 许可/协议/队列组件，补充接口、剩余接线任务和验收边界；本次没有新增或修复程序代码，也没有重新运行测试。第 22 节中的源码和报告为已存在的开发成果，不能理解为本次文档编辑完成了 Agent 功能。

v1.14 继续实施第 22 节的 Agent 接线任务：实际中央接口验证、执行机登录与本地核心授权、启动回执和幂等结果回报已有源码及模拟端到端测试。第 22 节保留为接线前快照；不能按其中“尚未接通”的历史描述重复实现，也不能由本阶段通过测试推断整个防转拷项目已完成。

v1.18 记录正式签名根受控初始化、公钥编入、正式根全量回归和完整环境包校验。它没有开启生产强制模式、修改设备授权数据或部署业务代码；更新包压缩按用户最新要求跳过。第 25.4 节是当前交接状态。

v1.19 增加从最终 ZIP 全新解压并实际启动双工作台的包级验收。扫描通过但启动失败的 `...01.zip` 已隔离；修复 BOM 后生成 `...02.zip`。

v1.20 补齐服务器受控模式切换工具和权威 A/B 双机验收记录工具；当前开发电脑固定为 A，250 处理机固定为 B。工具源码和隔离测试已完成，但生产业务代码、迁移、模式和真实设备数据仍未改变，当前交接以第 25.6 节为准。

## 1. 目标与已确认要求

用户的核心需求不是给 ZIP 设置解压密码，而是：别人把整套工作台复制到另一台电脑后，未经批准不能继续使用受保护的业务功能。

必须同时满足以下要求：

1. 首次使用的新设备需要管理员批准；不能仅凭一个有效账号自动获得设备授权。
2. 正常迭代、重新打包、覆盖升级不能要求重新激活。
3. 设备身份与程序版本、EXE 内容、安装目录、盘符、IP、电脑备注分离。
4. 继续复用数字人网站账号，不建立另一套普通用户账号或密码库。
5. 授权异常不能删除素材、损坏草稿或重复提交已经付费的任务。
6. 服务器、工作台实际业务入口共同执行限制，不能只在 Pu 启动器或网页按钮上检查。

对用户的正常操作应保持为：第一次申请并获批；以后关闭程序、覆盖更新、重新打开即可。

### 1.1 三个容易混淆的概念

| 概念 | 含义 | 是否需要管理员再次批准 |
| --- | --- | --- |
| 设备激活 | 批准某账号使用某设备 | 首次或换机时需要 |
| 账号登录 | 验证当前操作人 | 登录失效后重新登录，不等于重新激活 |
| 授权刷新 | 后台确认原设备授权仍有效，并取得短期凭据 | 正常情况下自动完成 |

商业授权到期、账号停用和设备停用是独立状态。不得把所有失败统一提示为“新电脑，请重新激活”。

### 1.2 更新与换机规则

| 场景 | 预期行为 |
| --- | --- |
| 更新后端、前端、依赖、ASR 模型或 FFmpeg | 原设备身份不变，不重新激活 |
| 重打 ZIP、修改 EXE、更新启动器 | 原设备身份不变，不重新激活 |
| 同一 Windows 系统内移动目录或改变安装盘符 | 不重新激活；业务数据仍需按原更新文档迁移 |
| 重启程序、重启电脑、改变网络或 IP | 不重新激活 |
| 退出账号后再次登录 | 重新验证账号，复用既有账号与设备授权 |
| 同机多个安装目录 | 复用同一产品设备身份；不是多个免费设备名额 |
| 清除浏览器缓存或重建本地授权缓存 | 通过原设备密钥向服务器恢复，无需重新批准 |
| 同机更换 Windows 登录用户 | 不自动创建另一设备；核对机器级密钥访问权限和网站账号授权 |
| 复制程序、配置、缓存甚至设备编号到另一电脑 | 无原设备持钥证明，不能继承授权 |
| 重装 Windows、清除 TPM/设备密钥、更换相关硬件 | 可能需要重新登记；按受控恢复/换机流程处理 |
| 后台明确停用设备或撤销该账号授权 | 阻止新业务操作，不因重新安装自动恢复 |

“免重复激活”以原系统设备密钥仍存在且原授权有效为前提，不承诺系统重装、密钥丢失后自动恢复。

## 2. 当前实现基线

本节记录开始实施前的业务基线；新增授权模块的实施进度见第 16～24 节，不能把设计要求直接当作已完成能力。

| 范围 | 当前事实 | 实施入口 |
| --- | --- | --- |
| 云端账号认证 | 工作台令牌为 `runninghub.workbench-auth.v1`，有效期 12 小时，绑定账号及密码修订，不绑定设备 | [workbench_auth.py](app/services/workbench_auth.py) |
| 云端接口 | 普通工作台、H3、LTX 路由分别存在令牌验证入口，不能只改一个路由就认为全覆盖 | [workbench.py](app/routes/workbench.py)、[workbench_h3.py](app/routes/workbench_h3.py)、[workbench_ltx.py](app/routes/workbench_ltx.py) |
| 本地登录与代理 | JYD 服务端调用数字人账号中心；浏览器不直接保存第三方 API Key | [auth_center.py](../../公寓/jyd_plain_json_probe/src/jyd_probe/auth_center.py)、[web_api.py](../../公寓/jyd_plain_json_probe/src/jyd_probe/web_api.py) |
| 更新包 | `-UpdateOnly` 排除全部 `data` 和 ASR 运行时，但尚无本文设备密钥保护与检测逻辑 | [build_processor.ps1](../../公寓/jyd_plain_json_probe/scripts/build/build_processor.ps1) |
| 统一完整包 | Pu、JYD、LTX 由统一脚本组装；可复用旧编译产物和基础包，必须防止带入旧认证组件或激活状态 | [build_production_bundle.ps1](../ltx_lip_sync_workbench/build_production_bundle.ps1) |

现有部署操作继续参考 [工作台更新说明](../../公寓/jyd_plain_json_probe/docs/PROCESSOR_UPDATE.md) 与 [快速打包](../../公寓/jyd_plain_json_probe/docs/FAST_BUILD.md)。本文不替换这些现有命令，也不声称当前包已经防转拷。

## 3. 保护范围与限制

### 3.1 一期业务范围

- 优先完成当前正式 H3 工作台的设备激活、生成/重试准入和本地建草稿/导出准入。
- 不修改 H3 Prompt、分段、声音清理、字幕、ASR 算法和 RunningHub 账号调度规则。
- 不改变任务所有者：`user_id` 仍是业务账号，`device_id` 只是授权与审计维度。
- 公用处理机绑定实际运行服务/执行任务的电脑，例如 250；仅通过浏览器或远程桌面访问它的电脑不因此注册为处理设备。
- 多账号使用同一处理机时，分别检查“账号—设备”的授权关系，不能把批准一个账号等同于对所有账号开放。

### 3.2 不得夸大的安全承诺

- 目标是阻止普通目录转拷、复制缓存、共享账号后随意扩散，以及直接绕过启动器的常见路径。
- 本地管理员能够修改程序、控制系统或借用已授权电脑提供代理服务；纯本地功能不能保证绝对防破解。
- TPM 持钥证明不是“程序未被修改”的证明，也不能单独阻止已授权电脑代签或远程转发。
- 已交付的明文图片、视频、模板、草稿不能靠设备授权变成不可复制。素材加密是另一项需求。
- FFmpeg、ASR 及其他第三方组件按各自许可使用；不把它们可以独立运行视为本项目授权失效。
- 统一包若仍包含未接入授权的 LTX、旧工作台、独立 Agent 或其他私有入口，必须明确标记未覆盖。整包防复用发布前，应逐一接入检查，或经用户确认从该交付包移除；不得擅自改动 LTX 业务。
- 旧网页若仍对合法账号开放同类云端能力，这是网站授权范围，不等同于桌面设备授权。必须逐项判断旧接口能否被旧工作台借用；不能仅靠 URL、版本号、User-Agent 或来源字段区分可信客户端。

服务端关键权限校验必须执行在服务端，而不是只信任客户端结果，遵循 [OWASP 授权原则](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html)。

## 4. 总体设计：设备身份、账号授权、短期凭据分离

| 对象 | 存放位置 | 是否随程序升级变化 |
| --- | --- | --- |
| 设备私钥 | Windows 机器级安全密钥存储，优先 TPM | 不变化，不进入安装目录/ZIP |
| 设备公钥与完整摘要 | 云端设备登记表；本地可保存非秘密副本 | 不变化 |
| 账号—设备批准关系 | 云端数据库 | 仅管理员或受控生命周期操作改变 |
| 短期授权凭据 | 本地独立缓存，云端签名 | 自动刷新，不触发重新激活 |
| 程序版本与构建摘要 | 程序/发布清单 | 每次升级可以变化，不能作为设备身份 |

设备编号来自稳定公钥标识，服务器另分配不可变 `device_id`。展示可使用缩短编号，但查重、比较和绑定必须使用完整值。

所谓“新机器”，实际是“无法证明持有已登记密钥的设备”。不能仅凭公钥字符串、磁盘序列号或客户端自报字段，证明物理机器身份。

## 5. Windows 设备身份模块

### 5.1 持久化规则

采用 Windows CNG 机器级持久化密钥，固定产品命名空间，例如 `PublicVideoWorkbench.DeviceIdentity`；名称不得包含版本号、路径、账号名或每次启动生成的随机值。

启动过程必须为：

1. 打开现有产品密钥。
2. 能打开则原样使用；检查公钥摘要与已登记设备一致。
3. 只有明确“密钥不存在”且用户开始首次登记时，才尝试创建。
4. 并发首次启动若返回“已存在”，重新打开，不能覆盖。
5. 权限不足、TPM 暂不可用、文件损坏等不是“首次运行”；报告错误并保留原状态。

禁止升级、启动、修复安装时使用覆盖已有密钥的选项。Windows 的持久化创建与机器级作用域见 [NCryptCreatePersistedKey](https://learn.microsoft.com/en-us/windows/win32/api/ncrypt/nf-ncrypt-ncryptcreatepersistedkey)，读取已有密钥见 [NCryptOpenKey](https://learn.microsoft.com/en-us/windows/win32/api/ncrypt/nf-ncrypt-ncryptopenkey)。

### 5.2 密钥保护和兼容性

- 首选 Platform Crypto Provider 创建不可导出的 TPM 密钥；硬件保护能力依据 [微软 TPM 说明](https://learn.microsoft.com/en-us/windows/security/hardware-security/tpm/how-windows-uses-the-tpm)。
- 密钥算法先在目标机验证。协议使用明确的非对称算法白名单；优先验证 P-256/ES256，不临时降级为共享对称密钥。
- 机器级密钥的创建权限与 ACL 必须在阶段 P0 验证。需要时首次初始化弹一次 UAC，日常运行和普通更新不应反复要求管理员操作。
- 不为整个 `Everyone` 开放宽泛密钥访问权限；支持的 Windows 用户必须通过受控初始化获得必要权限。
- 不支持 TPM 时先给出明确诊断。软件保护兼容模式只在管理员明确允许后启用，并显示较弱保护等级；不得静默改成明文私钥文件。
- 已建立的设备必须继续使用原密钥所在的存储提供程序。更新后 TPM 暂时故障不能触发创建软件密钥；原来使用软件保护的设备也不能因新版本支持 TPM 就自动换钥。v1.11 已接入固定机器级定位记录和冲突/损坏拒绝逻辑，实机恢复验收仍待完成，见第 20 节。
- “客户端报告使用 TPM”只作为诊断信息。若要让服务器确认硬件密钥来源，需另行实现和验证密钥证明；未实施前不能显示“服务器已验证 TPM”。
- 虚拟机、系统克隆、远程代签不在一期强保证范围；未验证的环境不得按物理 TPM 同等承诺。

### 5.3 独立状态目录

拟使用 `%ProgramData%\PublicVideoWorkbench\Licensing` 保存公钥摘要、设备 ID、授权缓存及最小诊断状态；具体 ACL 在 P0 定稿。

此目录不是秘密私钥仓库，也不是信任来源。复制/修改其中的内容不能代替持钥证明或伪造服务器签名。缓存缺失时使用现有密钥和正常账号登录向云端查回授权，不得直接申请第二个设备名额。

升级、卸载程序文件、清理构建缓存均不得删除产品密钥。彻底解除本机身份必须是独立、明确确认的管理操作；不能由普通“修复”按钮隐式执行。

## 6. 云端登记和请求验证

### 6.1 首次激活

1. 用户正常登录网站账号；该登录本身不授予设备业务权限。
2. 本地后端读取设备公钥并申请短时挑战。
3. 使用设备私钥完成持钥证明；服务器核对账号、挑战、目标用途及请求有效期。
4. 公钥已登记时返回原设备；新的账号—设备关系进入 `PENDING`。
5. 管理员核对申请账号、设备备注和编号后批准；批准与设备数量校验在同一事务中完成。
6. 客户端查询到批准结果后取得绑定该账号和设备的短期凭据；不要求再次重装或重复生成密钥。

重复申请必须幂等；`PENDING` 不占用已激活设备额度，但要有申请频率和数量限制。被拒绝/撤销的关系不能靠重复点击恢复。

### 6.2 日常请求与防复制

云端受保护请求同时校验：有效账号、启用的设备、有效授权关系、设备持钥证明和对应业务权限。不能只检查 `device_id` 或“已激活”布尔值。

拟采用 RFC 9449 的 DPoP 持钥证明和令牌绑定方式：绑定公钥摘要，验证请求方法/目标、签发时间、唯一请求号、访问令牌摘要及服务端 nonce，并拒绝证明重放。该机制不替代 HTTPS，不证明客户端代码可信，也不自动保护任意请求正文；业务参数校验和幂等摘要仍需保留。[RFC 9449](https://datatracker.ietf.org/doc/html/rfc9449)

实施要求：

- 当前自定义账号令牌不是完整 OAuth 实现。接入时需定义明确的 v2 设备绑定令牌/交换合同及兼容边界，不能仅增加一个自报请求头就宣称符合标准。
- 保留账号登录用于激活申请；强制模式下，无设备绑定的旧令牌不能启动受保护业务。
- 登录接力只能传递用户身份。目标处理机使用自己的密钥取得目标设备授权，不能继承来源电脑的设备权限。
- 持钥操作在本地后端完成，不把私钥、签名接口或任意载荷代签能力直接暴露给网页/局域网。
- 本地代理只向受信任的正式授权服务发送设备证明；不能把它转发到 RunningHub 下载地址或任意用户输入的 URL。
- 测试与生产使用独立信任配置；修改 `processor_config.json` 不能把伪造授权服务器变成可信签发方。
- 幂等重试生成新的网络持钥证明，但复用原业务幂等键。网络证明重放被拒绝不等于允许重新创建付费任务。
- 重放记录、挑战与授权决策需要跨进程/重启安全，不能只存一个 Python 内存集合；服务不可确认时拒绝新受保护操作。
- 复用成熟密码库和规范化编码，不自行实现密码算法；依赖、协议测试向量和反向代理 URL 规范化在 P0 定稿。

### 6.3 短期凭据与永久设备身份

本地授权凭据由服务器独立的非对称签名密钥签发；客户端只内置验证公钥。不能把现有 `APP_SECRET_KEY` 下发到工作台用于离线验签。

凭据至少绑定：签发方、产品、环境、账号 ID、设备 ID、公钥摘要、授权 ID、授权修订号、权限范围、签发/失效时间和签名 Key ID。不能把凭据的持有本身当作设备证明。

设备身份长期不变，短期凭据可以不断刷新。凭据到期、签名验证失败或服务器不可达时，不重新生成设备密钥。

## 7. 授权状态、离线和任务收尾

### 7.1 独立状态

| 状态 | UI 提示 | 行为 |
| --- | --- | --- |
| `UNREGISTERED` | 此设备尚未申请授权 | 可登录、诊断、申请 |
| `PENDING` | 等待管理员批准 | 可查询申请，不允许新业务 |
| `ACTIVE` | 设备已授权 | 仍按账号和业务权限执行 |
| `AUTH_REFRESH_REQUIRED` | 正在重新校验授权 | 原设备保留，不弹首次激活流程 |
| `OFFLINE_GRACE` | 授权暂时无法刷新 | 仅在有效缓存范围内使用获准本地能力 |
| `SUSPENDED` / `REVOKED` | 设备或账号设备关系已停用 | 停止新业务，不自动重新申请 |
| `KEY_UNAVAILABLE` | 无法读取本机设备密钥 | 提供诊断，禁止自动换钥匙 |
| `LOGIN_REQUIRED` | 请重新登录账号 | 不清除设备批准关系 |
| `CLIENT_UPGRADE_REQUIRED` | 当前版本需要更新 | 更新后沿用原设备身份 |

状态应由结构化错误码驱动，不能根据错误文本猜测。授权权限错误、密码错误、网络故障和升级要求必须分开。

### 7.2 建议默认值，发布前确认

- 首次登记必须联网。
- 活跃期间每 5 分钟尝试后台刷新；短期凭据建议最长有效 30 分钟。
- 缓存过期后不启动新本地任务；用户仍可在正常账号/数据访问权限内查看和取回已有成果。
- 本机重启或时钟异常导致离线有效期无法可靠确定时，要求联网刷新，不要求管理员重新批准。
- 不提供永久离线授权或每次断网重新开始的宽限期。

以上时间是建议值，不是用户已确认的商业规则。云端请求根据最新授权状态即时判断；离线本地程序无法收到即时撤销，其最长延迟受剩余凭据有效期限制。不能向管理员承诺断网电脑立即停用。

### 7.3 任务准入与安全收尾

“授权失效”与“任务失败”分开保存，不能为了阻止操作而伪造 RunningHub 终态。

| 任务所处阶段 | 设备授权失效后的处理 |
| --- | --- |
| 尚未提交的新生成、新付费重试、本地新导出 | 不准入；明确提示，不创建付费请求 |
| 已排队但尚未真正付费提交 | 调度前复核，不凭陈旧页面批准无限放行 |
| 已取得远程任务 ID | 保留原账号、原任务 ID，继续必要的查询/保存/取消处理，不换号重提 |
| 已明确失败，准备再次付费 | 视为新的付费准入，不能以“收尾”绕过授权 |
| 已开始的本地导出 | 允许当前安全单元收尾，不能强杀剪映或删除草稿；后续任务重新检查 |
| 已完成结果下载 | 继续做用户归属校验；设备停用不得变成匿名下载许可 |

准入记录应绑定用户、设备、授权修订、操作 ID、请求摘要和费用阶段。该记录不是永久通行证；后台 Worker 不保存用户密码或设备私钥。

账号本身被停用/删除时，原有账号安全规则仍优先；“可以收尾”不赋予被停用账号新的访问权限。付费结果由服务器安全保存，交由有权用户后续处理。

## 8. 后台与前端交互

### 8.1 管理员页面

在数字人服务器增加“工作台设备”页面，复用现有管理员身份与 CSRF 防护。

最小字段：账号、设备备注、短编号、完整公钥摘要查询、申请时间、最近在线时间、授权状态、保护等级、程序版本和操作审计。

最小操作：批准、拒绝、暂停、恢复、撤销、修改备注、设置账号设备额度、受控换机。设备额度建议默认每账号 1 台，但需用户确认；管理员账号也不能隐式无限激活。

设备全局停用与某账号的设备关系撤销分开。多账号共用 250 时，撤销其中一个账号不应误停其他获准账号。

换机采用明确的“撤销旧关系并批准新关系”事务，不提供无限次自动解绑。旧记录保留审计；相同物理电脑丢失密钥，也不能直接凭备注认领旧授权。

### 8.2 工作台页面

首次申请只要求登录、填写可选备注、点击申请；普通用户不输入密钥或复制授权字符串。已激活设备顶部仅显示简洁状态，正常刷新不打断操作。

Pu 启动器显示诊断和打开激活页面，不作为唯一执行点。直接打开内部 EXE、旧网页地址、本地渲染 API、导出接口等路径必须在受保护业务层再次检查。

本地技术管理员不能替代云端设备管理员批准设备。采集器、Agent、旧版 API 和内部交接的许可边界需要建立清单逐项验收，不能漏掉无需 Pu 的独立入口。

### 8.3 管理员如何知道这是一台新电脑

完成开发后的日常流程如下，不要求管理员懂密钥或手工查硬盘编号：

1. 对方在实际执行任务的电脑上打开工作台，登录自己的现有网站账号，填写“张某的处理机”等可选备注，点击申请。
2. 程序负责读取或首次初始化本机身份，服务器收到经过持钥验证的申请后，在“工作台设备”后台显示待审批记录。
3. 管理员核对账号、备注和页面显示的设备编号；需要时通过原有联系方式确认申请人。备注和电脑名称仅辅助核对，不能单独作为可信身份依据。
4. 管理员批准后，程序自动取得可用授权。后续覆盖更新只会刷新原授权，不应出现第二条待审批记录，也不占第二个设备名额。

如果对方只是用自己的浏览器或远程桌面连接 250，登记对象是实际运行工作台服务的 250，不是对方手里的电脑。250 的多个账号仍分别审批。

“无法读取原密钥”必须显示为故障，不能冒充“发现新电脑”。确实换机、重装系统或丢失密钥时，管理员使用受控换机流程核对并处理旧关系；普通更新不能进入该流程。

## 9. 拟定 API 和数据模型

以下为接口设计合同；登记与管理路由已加入本地代码，需完成迁移及独立签名配置才能使用。实际接入状态见第 16～23 节，不得削弱上述验证边界。

### 9.1 API 草案

| 方法与路径 | 用途与约束 |
| --- | --- |
| `POST /api/workbench/device-auth/challenge` | 登录用户取得有用途、期限和请求上下文绑定的挑战，有限流 |
| `POST /api/workbench/device-auth/register` | 验证持钥证明后幂等登记申请，不自动批准 |
| `GET /api/workbench/device-auth/status` | 返回当前账号、本设备的安全摘要，不列出他人设备 |
| `POST /api/workbench/device-auth/exchange` | 有效账号＋获准设备换取设备绑定令牌及本地短期凭据 |
| `POST /api/workbench/device-auth/refresh` | 原设备证明续取凭据，停用关系不得续期 |
| `POST /api/workbench/device-auth/software-initialization-permit` | 验证账号及管理员软件保护策略，签发短期专用初始化许可；不是设备批准或业务凭据，已接客户端显式初始化，见第 20 节 |
| `POST /api/workbench/device-auth/agent-permit` | 签发绑定单次中央队列请求的执行或上报许可；已接中央 HTTP 和 Agent 运行流程，见第 23 节 |
| `GET /admin/workbench-devices` | 管理设备申请和授权 |
| `POST /admin/workbench-devices/{grant_id}/approve` | 管理员批准，事务内检查额度和状态版本 |
| `POST /admin/workbench-devices/{grant_id}/revoke` | 撤销授权并提升修订号，保留审计 |
| `POST /admin/workbench-devices/replace` | 明确旧/新授权关系，原子换机 |

拒绝、暂停、恢复等使用同一管理服务实现。登录接力的现有接口需适配目标设备验证，不增加另一套普通登录系统。

### 9.2 数据实体草案

| 实体 | 最小内容 |
| --- | --- |
| `workbench_devices` | device_id、规范化公钥、唯一完整公钥摘要、全局状态、首次/最近时间、保护能力报告及可信等级 |
| `workbench_device_grants` | user_id、device_id、状态、权限、有效期（可空）、修订号、批准/撤销审计；账号与设备唯一 |
| `workbench_device_policies` | 账号设备额度、许可保护等级及修订号 |
| `workbench_device_control` | 服务端统一控制的 OFF/OBSERVE/ENFORCE 模式及修订号；H3 云端入口/队列已接入，其他保护范围仍待实施 |
| `workbench_device_challenges` | 有期限的挑战上下文、消费状态、绑定账号/公钥/用途 |
| `workbench_device_proof_replays` | 证明唯一号及到期时间；多进程可原子拒绝重复使用 |
| `workbench_device_audit_events` | 申请、批准、拒绝、换机、停用、恢复、保护模式变化等追加式审计 |
| `workbench_device_operations` | 服务端创建的操作准入快照：账号、设备、公钥摘要、授权/策略修订、操作类型、权限、请求摘要和准入模式；不保存令牌 |
| `workbench_device_work_bindings` | 分段/任务到最近一次操作准入的关联及授权等待原因；独立于 RunningHub 状态，不重新定义业务所有者 |

约束：并发批准不得突破额度；重复注册不占新名额；暂停名额如何计数需明确，建议暂停仍占额、撤销才释放。过期挑战/重放记录可定期清理，不删除长期设备与授权审计。

实施时核对的原迁移头为 `0048_legacy_task_runninghub_pool`；现新增 `0049_workbench_devices`，只增加独立授权表，不重建父表、不清空既有账号与任务。普通程序回滚保留授权表；显式迁移降级到 0049 之前会删除授权表，不得作为日常回滚方式。

H3 排队准入新增迁移 `0050_device_work_admission`，只增加操作与资源关联两张表，不改动 0049 的设备身份和批准关系。0050 是当前源码迁移头；升级测试必须验证已有批准关系、模式、账号和已付费任务原样保留。

## 10. 模块职责和代码落点

下面是模块职责分工；服务端基础、客户端持钥/凭据、登记接口、H3 云端准入、本地核心与内嵌队列、H3 历史批次补授权及后台刷新已加入源码。TPM 初始化源码接入见第 18 节，软件初始化及稳定原密钥定位见第 20 节，三处命令行见第 21 节。Agent 组件见第 22 节，实际运行接线与剩余恢复项见第 23 节；其他私有入口、真实首次初始化和发布打包项仍待完成。源码存在不等于实机可发布。

| 项目 | 拟新增/修改位置 | 职责 |
| --- | --- | --- |
| RunningHub 服务端 | `app/services/device_auth/` | 设备登记、授权状态、持钥验证、额度事务、短期签名与审计 |
| RunningHub 服务端 | `app/routes/workbench_device_auth.py`、管理路由与模板 | 参数解析和响应，不在路由中另写一套加密协议 |
| RunningHub 服务端 | 现有 `workbench_auth.py` 和各工作台鉴权入口 | 统一账号/设备上下文；明确旧令牌兼容与强制边界 |
| JYD | `src/jyd_probe/device_identity_windows.py` | CNG 访问、固定密钥读取、首次创建、明确错误与模拟 Provider |
| JYD | `device_identity_setup.py`、`device_identity_setup_windows.py`、`device_identity_acl.py` | 显式 TPM 初始化、必要的提权、核对原操作用户和修复原密钥访问权限；不负责管理员批准 |
| RunningHub 与 JYD | `device_auth/initialization.py`、`device_software_initialization.py` | 软件初始化许可签发、独立验签和内存交接；许可自身不替代设备审批 |
| JYD | `device_identity_store.py`、`device_initialization_channel.py` | 稳定机器级密钥定位、跨进程初始化锁，以及仅向指定辅助进程发送许可的本机管道 |
| JYD | `src/jyd_probe/device_authorization.py` | 状态、缓存、自动刷新、服务器签名验证、统一准入检查 |
| JYD | `src/jyd_probe/device_background_refresh.py` | Web 服务生命周期内刷新已有账号会话，不自动登记设备或启动等待任务 |
| JYD | 现有 `auth_center.py`、`web_api.py` 和任务准入层 | 云端请求证明、目标设备登录接力、本地建草稿/导出限制 |
| H3 云端与 JYD | `h3_queue_recovery.py`、`device_h3_recovery_routes.py`、`device-h3-recovery.js` | 原账号显式查看并补授权，保留原任务与费用计划；不是重建生成任务 |
| JYD 命令行 | `device_command_authorization.py` | 复用网站登录和本机原密钥建立单次命令会话，不保存密码/令牌，不自动申请设备 |
| 云端与 JYD Agent | `device_auth/agent_permits.py`、`device_agent_protocol.py`、`device_agent_client.py`、`device_agent_gate.py`、`device_agent_queue.py` | 独立用途许可、中央请求绑定、防重放及账号/原执行机归属组件 |
| JYD Agent 接线与恢复 | `device_agent_routes.py`、`device_agent_operations.py`、`device_agent_transport.py`、`device_agent_runtime.py`、`device_agent_journal.py` | 实际 HTTP、事务性领取/启动/回报、安全传输、执行机核心上下文及持久回执；不明确的执行中断仍需受控人工恢复 |
| Pu 及打包 | 启动器状态展示、Processor 和完整包构建流程 | 正确带入代码，排除激活数据；不是新的密码或授权中心 |

授权模块与图像生成、音频清理、ASR、字幕排版模块解耦。不能把机器码计算分散到多个入口，也不能在前端复刻服务器额度逻辑。

## 11. 升级、打包、密钥轮换和恢复

### 11.1 更新免激活的硬规则

1. 设备密钥名称和设备公钥标识稳定；EXE 哈希、版本号只能用于诊断/兼容检查。
2. 普通更新只替换程序，不能创建、覆盖、导出或删除设备密钥。
3. 协议 schema 可升级，设备身份不能跟着 schema 自动换钥匙；新旧协议应有明确重叠兼容期。
4. 授权缓存损坏可重建，但必须先找回原密钥和云端关系，不能自动注册新设备。
5. 服务器数据库迁移保留全部既有设备/批准关系；重启服务器不触发设备重新激活。
6. 密钥不可用与密钥不存在严格区分；修复失败不允许悄悄退回临时、明文或每用户随机身份。
7. 保留原密钥的存储提供程序和机器级作用域；不得在升级时自动切换 TPM/软件保护。权限修复只恢复原钥匙的使用权，不变更云端批准关系。

### 11.2 发布包检查

- `-UpdateOnly` 继续不包含 `data`、用户数据库、授权缓存、登录令牌和模型。
- 完整包也不能带开发机的设备密钥、已激活状态、私人账号配置或服务器签名私钥。
- 基础 ZIP、复用 dist、Pu 根目录、JYD/LTX 子目录、独立工具和嵌套归档均需纳入检查；不能只扫描最终 ZIP 的第一层。
- 复用旧编译产物时，核对所有私有入口是否支持本次授权协议，不能前端显示授权而后端仍是旧程序。
- 普通升级不删除 `%ProgramData%` 下授权状态，也不调用密钥删除 API。
- 发布签名和完整性检查用于防篡改及版本核对，不作为“设备重新激活”的触发器。

### 11.3 签名密钥轮换

设备私钥与服务器签名私钥是两类不同密钥。服务器轮换签名密钥必须使用 Key ID 和重叠验证期：先发布新验证公钥，再启用新签发密钥，保留旧凭据验证直至过期。

轮换服务器签名密钥、TLS 证书、应用登录密钥不应要求设备管理员逐台重新批准；用户可能需要重新登录或更新兼容客户端。设备密钥主动轮换则必须通过旧钥匙证明或受控管理员恢复，不能被普通更新隐式触发。

### 11.4 备份恢复

服务器授权数据库、审计与服务器签名密钥分别按受限权限备份。不得把这些备份放入客户端包。正常应用回滚优先保留新授权表与数据，不通过还原旧数据库撤销全部设备登记。

设备私钥不做可供跨电脑迁移的便携备份。原钥匙丢失后采取撤销旧授权、批准新登记的流程；“恢复授权”不能成为复制一份文件就能激活另一电脑的后门。

## 12. 实施阶段与完成条件

| 阶段 | 工作 | 完成条件 |
| --- | --- | --- |
| P0 兼容性与合同验证 | 检查目标机 Windows/TPM/CNG；验证机器级密钥、ACL、重启和移动目录；冻结协议及建议参数 | 不调用生成接口；产出持钥验证、并发创建和更新复用测试证据 |
| P1 云端登记与后台 | 增量迁移、申请/批准/停用/换机、额度事务和审计 | 全部 mock 测试通过；无自动批准/跨账号越权 |
| P2 本地设备与透明刷新 | 固定设备身份、登录接力、凭据缓存、失败状态 UI | 首次只批准一次；缓存重建和重新登录不增加设备 |
| P3 业务入口完整性 | 统一云端与本地准入；处理旧路由、绕过 Pu、队列和付费收尾 | 未批准设备不能进入保护范围；已有远程任务不会重复提交 |
| P4 打包与迭代验收 | 清洁包扫描、连续升级、目录迁移、服务器升级/签名轮换 | 至少连续三个不同构建版本复用同一设备与授权 |
| P5 用户授权后的上线 | 兼容登记、指定设备试点、批准现有处理机、开启强制模式 | 记录生产验收；未覆盖入口不能宣称已防复制 |

实施时同时更新数字人开发者指南、JYD 更新说明和相关接口文档。本次文档提交不能替代 P0～P5 的代码、测试或生产验收。

### 12.1 后续开发任务单

以下工作包按当前交接状态整理；D01 的 TPM 源码接入见第 18 节，软件初始化/原密钥定位及模拟回归见第 20 节，D02 已接入的命令行入口见第 21 节，Agent 组件见第 22 节，实际运行和待完成的人工核实恢复见第 23 节。D01 的真实硬件、无 TPM 与不同 Windows 用户验证未完成，不将 D01 整体标记完成。不要重复实现已存在的登记、H3 恢复或后台刷新。先核对当前代码与测试，再按依赖推进；需要真实电脑、真实密钥或生产操作的步骤另行取得用户允许。

| 顺序 | 工作包 | 必须交付的内容 | 完成判据 |
| --- | --- | --- | --- |
| D01 | 首次初始化和身份恢复 | Windows 机器级密钥的首次初始化、必要的 UAC/ACL、原密钥读取诊断及受控软件兼容流程 | 首次可用，后续普通用户启动/升级不换钥；权限故障不创建第二个身份；模拟与实机证据分开记录 |
| D02 | 执行入口覆盖 | 私有 EXE、CLI、独立 Agent、旧本地接口的入口清单及合法授权接力 | 每项注明已保护或尚未覆盖；已获准用户能够正常执行，不能把“一律拒绝”算成完成；身份来自实际执行机 |
| D03 | 其余云端付费路径 | 网站工作台代理、声音等付费入口和对应 Worker 的准入检查及旧接口兼容审计 | 新付费操作受控，原远程任务安全收尾且不重发；不改 H3/LTX 生成算法，不擅自移除包内功能 |
| D04 | 发布公钥与服务端配置 | 独立签名配置的受控初始化、公开验证密钥编入客户端、轮换和配置诊断流程 | 测试/生产隔离，客户端只有公钥；缺配置明确失败；普通轮换不改变设备批准关系 |
| D05 | 完整包和更新包保护 | 覆盖基础包、复用 dist、内部工具及嵌套归档的发布检查；设备状态保留规则 | 不分发私钥、账号令牌或激活缓存，不混入绕过授权的旧私有程序；更新不删除机器身份或业务数据 |
| D06 | 跨机与连续更新验收 | 两台电脑复制测试、至少三次构建升级、停用/断网/换机和原任务收尾记录 | A01～A20 有逐项证据；模拟通过、实机通过和待验收分别标注；最终发布仍需用户确认 |

D02、D03 若涉及用户尚未确认的保护范围，先提交入口清单和影响说明，不能自行扩大商业授权范围。历史任务缺少账号归属时也不得猜测认领，应另列受控恢复方案。

每个工作包交接至少包含：修改文件、接口/配置变化、验证结果、未覆盖项、对更新免激活及已付费任务的影响。D04/D05 完成前不得把当前源码直接打成“已防转拷”的交付包；D06 完成前不得宣称已验证跨机防复制。

## 13. 验收清单

所有付费供应商调用先使用 mock；真实硬件、跨电脑和剪映验证需用户明确允许。

| 编号 | 测试 | 通过标准 |
| --- | --- | --- |
| A01 | 首次申请、重复申请、管理员批准 | 一个设备一条幂等登记，未批准不能执行业务 |
| A02 | 已激活 V1 → V2 → V3 → V4 覆盖更新 | 完整公钥摘要、device_id、grant_id 不变，无第二次激活 |
| A03 | 同机改路径、改盘符、复制到第二目录 | 复用原设备身份；不自动迁移或丢失业务数据 |
| A04 | 重启、浏览器清缓存、授权缓存丢失 | 原密钥恢复授权；账号登录与设备激活提示分离 |
| A05 | EXE/依赖/ASR/启动器升级 | 不以构建哈希变化重新登记 |
| A06 | 整个目录、配置、设备编号和授权缓存拷到另一电脑 | 无原设备证明，不能使用原批准关系 |
| A07 | 修改编号、伪造公钥、重放证明、替换令牌/请求目标 | 全部拒绝，无付费请求 |
| A08 | 直接启动内部 EXE、旧路由、CLI、Agent/内部交接 | 各入口有明确准入或被列为未覆盖，不靠 Pu 单点保护 |
| A09 | 两个进程同时首次启动 | 只创建一个持久密钥，不相互覆盖 |
| A10 | 密钥权限错误、TPM 故障、Windows 用户变化 | 明确诊断，不偷偷创建新身份或降级保护 |
| A11 | 无 TPM 软件兼容申请 | 未经管理员批准不启用，保护等级显示真实 |
| A12 | 两个管理员并发批准、账号额度已满 | 事务保证不超额；暂停/撤销计数符合规则 |
| A13 | 多账号共用处理机 | 授权关系独立，备注或管理员身份不形成通用放行 |
| A14 | 停用、恢复、撤销、受控换机 | 修订和审计正确；旧缓存不能使云端继续放行 |
| A15 | 断网、服务器故障、系统时间回拨、重启 | 不无限延长宽限；不把异常变成免费授权或重新付费 |
| A16 | 付费提交后失联/撤销、结果下载失败 | 保留原任务与原账号；新付费尝试重新准入，无重复计费 |
| A17 | 服务器升级、兼容回滚、签名密钥轮换 | 原批准关系保留；需要刷新/升级但不重新激活 |
| A18 | 复制已激活开发环境后再构建完整包/更新包 | 构建检测到私钥/激活数据必须失败，不把问题包交付 |
| A19 | 修改授权服务器地址、使用本地伪造响应 | 不能凭可编辑配置替换正式授权信任根 |
| A20 | 日志/诊断/管理员审计检查 | 无私钥、访问令牌、第三方 Key、完整请求体或不必要的硬件序列号 |

软件模拟测试不能代替至少两台真实电脑的 A02/A06/A10 验收；没有跨机验证，不发布“复制后不能用”的结论。

## 14. 上线、旧版本与回滚边界

拟设服务端控制的三种模式：`OFF`（未启用）、`OBSERVE`（登记/观察）、`ENFORCE`（强制）。客户端不得自行把模式改回 `OFF`。

上线顺序：

1. 用户确认设备数、兼容模式、离线时间和保护范围。
2. 备份并部署支持新表和协议的服务器，先保持观察模式。
3. 分发新工作台，现有 250、188 等实际使用设备逐台申请并批准一次。
4. 在试点设备完成连续覆盖更新和另一台电脑复制验证。
5. 核查旧接口、独立私有程序和后台任务入口的覆盖情况。
6. 用户确认后开启强制模式；未升级客户端提示更新，而不是重新创建业务账号。

观察期旧客户端可能仍可使用，因此观察期不具备完整防转拷效果。开启强制模式后必须验证真实的设备绑定协议，不能只要求客户端声称自己是新版本。

回滚优先退回兼容授权协议的上一程序版本，并保留设备表、密钥和批准关系。紧急退回观察模式会暂时降低保护，必须由管理员明确操作并记录；不能把后台回滚变成静默永久放行。

本功能涉及服务端迁移/配置及客户端更新，不是重新压缩 ZIP 即完成。真正部署时按 [生产部署文档](deploy/README.md) 重新判断完整部署要求，不直接套用代码更新模式。

## 15. 发布前需要确认的建议项

已确认：防普通转拷、管理员批准设备、普通更新不重复激活、保留账号与任务边界。

以下为本文建议，尚需发布前确认，不应在实现中静默扩大权限：

- 每账号默认允许 1 台还是多台；公用处理机的授权对象及名额计数。
- 无 TPM 电脑是否允许较弱的软件保护模式。
- 5 分钟后台刷新、30 分钟短期凭据是否符合使用习惯。
- 是否设置商业到期日期；默认不新增收费/续费系统。
- 一期仅保护 H3 主工作台，还是同时覆盖包内全部私有程序；未覆盖部分如何交付。
- 系统重装、主板更换及丢失密钥后的管理员核实流程。

## 16. 实施进度与交接（2026-08-31）

### 16.1 已加入本地代码的部分

- `0049_workbench_devices`：设备、公钥摘要、账号设备关系、额度、控制模式、挑战、重放记录及追加审计；初始模式为 `OFF`。
- `app/services/device_auth/`：ES256/JWK、规范化 URI、账号与设备绑定凭据、独立服务器签名密钥、登记、审批、拒绝、暂停/恢复、撤销、原子换机及设备全局停用。
- `app/routes/workbench_device_auth.py`：挑战、登记、状态查询、交换与刷新；状态查询也需要持钥证明。重复登记不重新分配设备，版本仅是诊断字段。
- `app/routes/workbench_device_admin.py` 与管理模板：复用网站管理员和 CSRF；并发批准时在数据库事务中检查额度，软件保护需先明确允许。
- 新依赖 `PyJWT[crypto]>=2.10.1,<3`，签名运算使用现有 cryptography。P-256 公钥坐标固定 32 字节，保留前导零；不能使用省略前导零的设备标识。
- JYD 的 `device_identity_windows.py` 实现固定名称机器级密钥、先读取后显式创建、禁止覆盖、签名及保护属性检查。12 项模拟测试通过；尚未完成真实 TPM、ACL 和跨电脑验证。
- JYD 的 `device_auth_protocol.py` 校验固定 ES256 信任根、账号/设备/环境/权限/到期时间，并生成 DPoP 证明；`device_trust_roots.py` 只接受随程序编译的发布公钥，当前为空，不从用户配置或网络响应加载信任根。
- JYD 的 `device_authorization.py` 实现按账号隔离的会话、凭据刷新、短期离线状态、本机持钥复核和独立缓存。缓存只存服务器签名的本地凭据，不保存登录令牌/云端访问令牌；进程重启后先联网恢复，不能直接从缓存取得离线执行权，也不会重新登记或申请名额。
- JYD 的 `device_authorization_routes.py` 已接入 `web_api.py`：复用网站身份，不允许本地技术管理员代替普通网站账号；申请/刷新要求同源及专用请求头，浏览器不能指定密钥、设备 ID、软件批准标志或信任根。退出登录释放内存会话与密钥句柄，不删除持久设备密钥。
- JYD 的 `device_business_transport.py` 与 `AuthCenterClient` 已接通 H3 业务请求：仅向已接入的 H3 云端路径发送当前账号、本处理机的 DPoP；绑定请求移除旧正文令牌，不改业务幂等键。设备证明不跟随任何下载重定向，也不发送到 RunningHub 直达地址；业务被拒绝后不降级重发。
- 激活页面位于 `/app/new/device-authorization`，账号菜单可进入。读取状态不创建密钥；明确勾选确认后才申请。待审批、暂停/撤销、到期、密钥异常、需升级、账号登录和凭据刷新分别提示；设备凭据 HTTP 401 不冒充账号退出。页面可见时轮询，已有有效凭据断网时显示原短期状态，不延长有效期。
- `admission.py` 与 H3 路由已接入 v2 DPoP 验证、持久防重放及新操作检查；JSON 与请求头出现不同账号凭据时拒绝。H3 确认、付费重试和重生成在业务服务中再次检查，幂等回执读取不重复创建准入。
- `0050_device_work_admission` 与 `queued_work.py` 增加操作快照、分段关联和授权等待原因；关联覆盖尚未生成任务的 soft_chain 后续段。Worker 在领取前、上传前及付费提交前复核当前授权，授权暂停不写成供应商失败；提交结果未知不重提，已有远程 ID 继续按原账号查询。
- 共享 `task_management.py` 的 H3 付费重试/重生成也要求新的有效请求身份，防止旧网页或批量重试借用原任务的设备授权；只有服务器实际认定的下载失败且保留原远程 ID 时，允许只恢复下载。不改变 LTX/其他工作流的业务规则。

当前服务端签名配置读取以下变量，未设置时登记/签发相关接口明确报配置错误，不会自动生成密钥或使用 `APP_SECRET_KEY` 替代：

| 变量 | 内容 |
| --- | --- |
| `WORKBENCH_DEVICE_AUTH_ORIGIN` | 固定的授权服务公网源地址，生产必须 HTTPS，不带路径、查询和片段 |
| `WORKBENCH_DEVICE_AUTH_KEY_ID` | 当前签名 Key ID |
| `WORKBENCH_DEVICE_AUTH_PRIVATE_KEY_FILE` | 服务器专用 ES256 私钥文件，不能分发到工作台 |
| `WORKBENCH_DEVICE_AUTH_PUBLIC_KEYS_FILE` | 包含当前及轮换过渡期公钥的可信清单 |

公钥清单 schema 为 `publicvideo.device-trust.v1`，绑定 issuer、environment；每个 keys 项为 kid 与固定 P-256 公钥 jwk。真实部署密钥尚未生成。后续需提供受控初始化、发布公钥和轮换工具，不能手工把私钥粘进客户端配置。

### 16.2 已验证与仍待验证的边界

- 服务端基础阶段使用隔离 PyJWT 2.10.1：新增 24 项授权/迁移测试，相关回归 `54 passed`，当时网站全量 mock 回归 `602 passed`。客户端接入后对当前完整工作树重跑，全量为 `626 passed`、0 失败/错误/跳过（296.35 秒）。结果保存在本地忽略目录 `tests/.runtime/device-auth-foundation.xml`、`tests/.runtime/device-auth-full.xml`，不作为发布包内容。
- 已有自动化验证覆盖：首次待审、重复申请、V1→V4 服务端登记复用、换钥匙后复制令牌失败、挑战/请求重放、共享设备的账号隔离、软件兼容审批、并发额度、原子换机失败回滚、CSRF、停用/改密码、签名公钥轮换和增量迁移保留既有任务。
- 上述版本测试是服务端协议模拟，不是完整 EXE 连续覆盖更新，也不是两台真实 TPM 电脑的复制验收。
- 客户端新增测试和既有账号中心相关回归 `63 passed`，包含 12 项密钥模拟测试、22 项协议/会话测试、12 项本地接口测试及 17 项已有登录/下载/接力回归。真实 JYD 客户端代码与 RH 本地接口合同测试 5 项，加原服务端授权/迁移回归合计 `29 passed`；测试报告为 `tests/.runtime/device-client-integration.xml`、`tests/.runtime/device-client-contract.xml`。
- 跨端验证覆盖真实接口的首次待审/批准、无缓存重开会话与 V1→V4 重复登记复用、复制凭据后的双方拒绝、共享 nonce 配合不同 jti、防重放、管理员撤销及同机不同账号隔离。仍不等于实机 EXE 升级或跨 TPM 电脑验收。
- H3 云端准入新增 28 项场景测试及 1 项 0050 升级保留测试；当前网站完整工作树回归 `655 passed`、0 失败/错误/跳过（347.44 秒），报告 `tests/.runtime/device-admission-full.xml`。专项覆盖未绑定凭据/伪造重试范围、失败后重放、跨账号凭据冲突、准入与业务事务回滚、付费重试幂等、排队/上传中停用、原 ID 收尾、软件/权限状态复核、通用旧重试入口与迁移保留；不等于端到端工作台已经启用限制。
- H3 客户端持钥接线及激活 UI 阶段，工作台相关回归为 `126 passed`（85.53 秒），两端真实客户端合同与云端队列准入回归为 `35 passed`（31.35 秒），均无失败。报告为 `tests/.runtime/device-client-h3-ui-final.xml` 和 `tests/.runtime/device-business-contract-final.xml`。该阶段没有重新运行网站全量；后续本地执行阶段的完整回归如下。
- 本地核心执行与内嵌队列阶段：网站当前完整工作树回归 `663 passed`、0 失败/错误/跳过（274.73 秒），报告 `tests/.runtime/device-local-server-full.xml`。工作台授权/任务队列/H3/后处理/母版/字幕/批量相关广泛回归 `258 passed`（181.95 秒），报告 `device-local-regression.xml`；最后补齐等待页面、状态文件锁和旧 Agent 领取拦截后的专项回归 `61 passed`（48.59 秒），报告 `device-local-final.xml`。两端真实客户端合同与签名本地策略回归 `36 passed`（13.67 秒），报告 `device-local-server-contract.xml`。这些报告均在 RH 的 `tests/.runtime/`；集合重叠，不相加，也不称为工作台全量测试。
- 本地等待页面使用独立模拟服务及临时 Edge 验证：加载页面不恢复任务，首次恢复拒绝时保留记录，明确再次点击且服务端成功后更新列表；390px 窄屏无横向溢出，无脚本错误。测试没有执行真实渲染、申请真实设备或调用付费供应商。
- 授权页面使用独立本地模拟服务和新建的临时 Edge 会话检查：申请前必须确认、待批准、有效、撤销、密钥异常、账号登录、凭据失效、授权到期、需升级、断网提示及 390px 窄屏布局通过；无页面脚本错误。截图/测试临时文件位于忽略目录，不是发布资产，没有向真实服务器申请设备。
- 截至本节 2026-08-31 阶段仍待完成：真实 Windows CNG/ACL 与 UAC 初始化流程、无 TPM 兼容身份恢复、发布公钥初始化/编译流程、与页面关闭无关的后台刷新、其他本地私有/FFmpeg/CLI 入口及独立 Agent 的合法授权通路、云端其他工作台业务/声音等付费 Worker 准入、历史无归属/无设备关联任务的安全恢复、启动器及整包扫描。其中后台刷新与可归属 H3 批次的历史队列补授权已在第 17 节继续实现，其余边界仍保留。
- H3 云端入口和 Worker 已执行 `OFF/OBSERVE/ENFORCE`，默认仍为 `OFF`；管理页面暂不开放强制模式开关。H3 历史队列补授权的后续实现见第 17 节；其他入口与发布验收仍待完成，不能靠手工改控制表宣称整包保护生效。
- 未修改生产数据库、生成真实设备/服务器密钥、启动真实生成任务、打包、推送或部署。后续继续按 P0～P5 和 A01～A20 完成，不以本阶段测试替代全目标验收。

### 16.3 文档 v1.2 整理范围

补充面向使用者的快速说明，核对设备策略与全局模式的实际数据表职责，并区分已存在的客户端模块源码与尚未完成的接入/验证。本次仅整理文档，不新增程序实现、不生成设备密钥、不启用授权拦截，也不执行打包、推送或部署。

### 16.4 本地登记接口合同

| 方法与路径 | 行为 |
| --- | --- |
| `GET /api/new/device-authorization` | 当前网站账号在本处理机的授权状态；没有机器密钥时返回未登记，不创建密钥 |
| `POST /api/new/device-authorization/apply` | 显式申请：`confirm_initialize` 必须为布尔 `true`，可传 `label`、`client_version`；仍需云端管理员批准 |
| `POST /api/new/device-authorization/refresh` | 强制联网校验并刷新当前会话，只返回安全状态摘要，不向浏览器返回设备访问令牌或本地凭据 |

两个 POST 都要求 `Origin` 与当前工作台源完全一致，并带 `X-Device-Authorization-Action: 1`。申请参数禁止额外字段；程序不接受浏览器提供的持钥签名内容或软件兼容批准。接口只使用实际运行 JYD 的电脑密钥，不使用浏览器或登录接力来源电脑的身份。

正式信任根未配置时明确返回 `DEVICE_TRUST_NOT_CONFIGURED`；不能为了本地测试把任意服务器响应或普通配置文件作为信任根。生产公钥发布流程尚待实现。本轮没有配置真实公钥/私钥、启用强制限制或部署。

### 16.5 H3 云端业务准入合同

- H3 API 读取允许既有账号令牌；使用 v2 凭据时仍必须带合法 DPoP 证明。强制模式下，新预检/确认/重生成/付费重试需要获准设备及 `cloud:generate` 权限，旧令牌与自报设备 ID 都不能代替它。
- 请求证明先于业务写入验证并持久消费。因此业务参数出错后重用同一证明会被拒绝；重发业务应使用新证明、原业务幂等键。
- 初次确认记录 `h3.generate` 操作，付费重试记录 `h3.retry`，主动重生成记录 `h3.regenerate`；快照与业务变更在同一事务提交。操作 ID 仅供内部审计，不能作为持有即有效的许可。
- 已确认回执读取、所有者查询、取消及真正的 `download_only` 不重新付费，也不要求设备仍为 ACTIVE；正常账号有效性和资源归属检查仍保留。客户端传入 `retry_scope=download_only` 不能改变服务器根据任务状态及远程 ID 做出的分类。
- 后台任务使用服务器保存的分段关联复核当前账号、设备、授权关系、功能范围及软件保护策略，不保存访问令牌或私钥，也不把一次 HTTP 检查当成永久许可。排队可以超过短期 HTTP 凭据有效期，设备暂停/撤销仍能拦住新提交；恢复同一有效授权不需要重新付费确认。
- 最终付费提交前的检查与管理员授权变更使用相同控制锁。检查记录提交后才发出供应商请求；其后撤销不能追溯取消已开始的外部请求，不能承诺无条件撤回供应商已接受的操作。
- 未付费任务的授权失效记录在 `workbench_device_work_bindings`；保持/退回本地 PENDING，不伪造 RunningHub FAILED，不清除失败历史或增加自动付费重试次数。已预留但明确未提交的账号可以释放；存在远程 ID 或提交结果未知时不释放原绑定。
- OFF 时创建的无设备关联任务，切到 ENFORCE 后不能自动继承任意获准设备；会等待补授权。后续已加入第 17 节的原账号显式恢复入口，不是再次创建生成任务；其他私有入口仍未完成，因此仍不得开启生产整包强制模式。没有修改真实设备密钥或现有云端账号数据。

### 16.6 工作台 H3 请求与激活页面合同

#### 实际请求如何使用设备身份

1. `web_api.py` 将网站账号验证器与当前处理机的会话注册表接给 `DeviceBusinessProofs`。账号令牌不来自页面传入的任意用户 ID，也不借用其他账号的会话。
2. `AuthCenterClient` 只对审计过的 H3 POST/GET 路径申请证明。每次网络请求使用新的证明 ID，凭据/nonce 可以按协议短期复用；生成请求键及输入不被修改。
3. 已取得设备凭据时使用 `Authorization: DPoP …` 和独立 `DPoP` 证明头，移除正文中的旧账号令牌。正文和请求头不能混用不同身份。
4. 若发送业务请求前没有可用设备凭据，可以仅发一次原账号请求，用于读取既有结果或获得服务端的明确授权拒绝。ENFORCE 中新业务仍由服务器拒绝；这不是本地授权，也不是授权失败后再重试。配置了发布信任根而服务地址不匹配时，在发送前失败，不能泄露设备证明给可编辑地址。
5. 一旦业务请求已经发出，任何设备拒绝、超时或其他错误都不触发自动旧令牌重发。带证明的请求拒绝 301/302/303/307/308 重定向；RunningHub 直达下载不附带设备证明。已有账号身份仍承担所有成果归属校验。

源码开发环境尚未编入发布信任根时，不读取本机 CNG 密钥，H3 沿用一次普通账号请求；云端强制模式仍拒绝其新业务。此兼容行为不适用于本地受保护功能的放行，也不能作为发布版免授权开关。

#### 页面及错误合同

- 页面：`GET /app/new/device-authorization`；资源为 `apps/processor/frontend/new/device-authorization.html/.css/.js`。账号菜单新增“设备授权”。无需外部字体、样式或脚本服务，不在浏览器保存设备凭据。
- 申请仅在 `UNREGISTERED` 状态显示，并要求用户主动勾选确认。页面加载、轮询、重新登录和普通更新都不能触发 `register`。
- 页面可见时，待审批/需刷新/短期断网状态每 15 秒重查；有效状态每 5 分钟校验并刷新。取得待批准变为 ACTIVE 的状态后，再交换服务器签名凭据，不能把无签名状态响应当作业务授权。页面关闭后无持续 UI 轮询；后续已加入第 17 节的处理机后台刷新，业务请求仍按需校验。
- H3 代理返回设备错误时保留 `code`、`device_authorization_required:true` 和 `X-Workbench-Device-Error`。设备层 HTTP 401 对业务页面映射为 409，真实账号登录失败仍为 401；前端引导到授权页面，不清除网站账号。
- 登记/刷新 API 的结构化设备凭据 HTTP 401 与 `LOGIN_REQUIRED` 分开处理；`DEVICE_GRANT_EXPIRED` 显示授权到期，`CLIENT_UPGRADE_REQUIRED` 显示更新程序。网络错误仅在会话仍持有原有效签名凭据时返回 `OFFLINE_GRACE`；过期、撤销或时钟异常不能借此保留权限。
- 实际机器级首次初始化、UAC/ACL 与无 TPM 恢复尚未验收。页面完成不等于首次激活全流程已能在任意新电脑运行，不能发布“开箱已防拷贝”的结论。

### 16.7 本地执行与内嵌队列合同

#### 签名的本地运行模式

- 云端新增 `POST /api/workbench/device-auth/local-policy`。请求需要当前网站账号凭据和新随机 `nonce`；使用设备绑定凭据时仍需正常 DPoP 验证。响应是独立用途的 `workbench-local-policy+jwt`，不是设备租约，也不会登记设备或创建密钥。
- `app/services/device_auth/local_policy.py` 签发 `publicvideo.local-policy.v1`，绑定产品、环境、账号、账号凭据摘要、nonce、`OFF/OBSERVE/ENFORCE`、控制修订号和最长 300 秒有效期，使用原独立服务器签名密钥。
- JYD `device_local_policy.py` 只信任编译内的发布公钥，严格校验用途、签名、账号/nonce、时效及单进程内控制修订不倒退。策略只保存在账号会话内存，检查墙钟与单调时钟；不能在普通配置、任务 JSON 或缓存中填 `OFF` 放行。
- 有效签名 `OFF` 保持兼容，不要求初始化设备；`OBSERVE` 尝试验证但允许尚无授权的旧用法；`ENFORCE` 必须逐项校验设备租约和本机持钥。暂时断网且策略不能继续使用时只能按更严格的 ENFORCE 检查原有效设备租约，不延长离线期限，不把网络失败解释为 OFF。
- 模式签名最多缓存 5 分钟，因此模式切换不是所有客户端瞬时生效；设备撤销还受原短期离线租约边界约束。普通更新、退出重登、重新获取策略不重新申请设备名额。

#### 真正的执行入口

- `device_local_execution.py` 使用仅进程内部的授权上下文。正式冻结 EXE 始终启用检查；未配置发布信任根时拒绝执行，不接受环境变量或用户 JSON 关闭。仅空信任根的源码开发环境保留旧路径，不能作为正式防复制发布方式。
- `run_render_job`、建草稿的两个工厂函数、内容替换及实际剪映导出入口已有检查；请求网页以外直接调用这些函数也不能通过任务 JSON 伪造授权。未审计的其他私有入口仍需补齐，不宣称已经全面防旁路。
- `skip_export` 的权限计算与渲染器实际解析一致：只建草稿需要 `local:draft`，导出需要 `local:draft` 与 `local:render`；`existing-draft` 路径实际会导出且可能走草稿恢复，按两项权限检查。权限来自执行行为，不能由客户端自报 scopes 降低。
- 同一决策锁定会话，避免两个权限读取到不同修订的凭据。每个新执行单元重新检查；已获准启动的单元可完成其范围内的嵌套建草稿/导出步骤，不因中途到期强杀剪映、损坏草稿。下一单元必须重新验证。
- `device_local_web.py` 从当前网站账号和本处理机会话建立上下文；部分会创建协调任务的后处理入口在副作用前预检。未登录、设备异常和普通参数错误保持独立，设备层 401 不触发账号误退出。

#### 排队、暂停与恢复

- `RenderJobQueue` 入队前计算真实所需权限；内存保存内部授权器，状态保存 `publicvideo.local-operation.v1` 非秘密关联摘要（账号、设备/授权 ID、公钥摘要、模式/修订/范围）。不向任务 JSON 写入私钥、设备访问令牌或本地租约，状态摘要本身不是授权。
- `device_local_queue.py` 在每个内嵌队列任务真正执行前重新验证当前授权和原任务账号/设备。重启丢失内存会话、授权失效或历史 OFF 任务尚无设备关联时等待授权；不标成供应商失败，不增加自动重试次数，不换任务 ID，不删除结果或素材。
- 等待标记保存在现有 SQLite `status_json`，领取时排除等待任务，无新本地表结构迁移。数据库为权威来源；状态文件通过独立临时文件原子替换，文件锁失败保留数据库状态，不终止队列或卡死已恢复任务。取消请求仍优先保留。
- `GET /api/new/device-authorization/waiting-jobs` 仅返回当前网站账号最多 100 条待恢复本地任务的安全摘要，不返回任务载荷、令牌或路径。没有历史账号归属的旧任务尚未进入该恢复通路，不能猜测归属。
- `POST /api/jobs/{job_id}/resume-authorization` 要求同源及 `X-Device-Authorization-Action: 1`、原所属账号和新的有效本机授权。确认后更新原任务关联并恢复原 ID；不创建新的付费任务。非所属账号不暴露该任务，已不在等待状态时拒绝重复恢复。
- 授权页面展示“等待授权的本地任务”，只在用户点击“继续此任务”时恢复。进入页面、重新登录、刷新授权或管理员批准本身都不自动启动旧任务。重启后需要登录/恢复执行上下文，不等于重新激活机器。

#### 本阶段不能交付的边界

- 独立 Agent 尚无目标执行机持钥协议。当前受保护运行环境临时拒绝旧 Agent 仅凭接入密码领取新任务，返回 `DEVICE_AGENT_PROTOCOL_REQUIRED`；这在签名 OFF 下也会发生，是防旁路的过渡封口，不是完成了可用的 Agent 授权功能。已启动工作的正常报告/收尾不靠新领取恢复。
- 部分 CLI 下层核心入口已经拒绝无授权直接执行，但完整的合法 CLI/独立进程授权接力尚未实现。不能用“全部拒绝”代替完成产品，也不能要求用户以源码或旧 EXE 绕过。
- 本节的本地手动恢复与 H3 云端历史队列补授权是两条独立通路；后者的后续实现见第 17 节。其他本地私有入口、发布信任根、完整包敏感信息隔离和真实两机/连续 EXE 更新验收均仍未完成。
- 暂不要打包分发或打开生产强制模式。只有上述未完成项按原验收清单闭环后，才可称为可部署的防普通转拷实现。

## 17. H3 历史队列恢复与后台刷新（2026-09-01）

### 17.1 H3 云端补授权，不是重新生成

新增 `app/services/device_auth/h3_queue_recovery.py`。只处理原网站账号所属、原已确认 H3 批次中的未提交分段；`soft_chain` 尚未创建任务的后续等待段也沿用同一批次恢复。

| 云端 POST 接口 | 作用 |
| --- | --- |
| `/api/workbench/h3-authorization-waiting` | 当前账号等待列表，ENFORCE 时展示；按稳定批次 ID 分页，每页扫描最多 50 个批次并返回 `next_cursor`，不悄悄截断旧批次 |
| `/api/workbench/h3-batches/{batch_id}/authorization/prepare` | 读取当前可恢复分段、原行号/段号及 `review_token`，不补授权、不提交供应商 |
| `/api/workbench/h3-batches/{batch_id}/authorization/resume` | 验证 `resume_confirmed:true`、原查看版本、请求键及当前账号设备身份，更新原分段的授权关联 |

- 合同为 `runninghub.h3-authorization-recovery.v1`。`review_token` 是当前分段、输入、费用快照、原操作与执行状态的摘要，只用于防止“看完后状态变化仍盲目确认”，不是持有即有效的许可。
- 查看和列表沿用正常账号归属校验。首次恢复无论 OFF/OBSERVE/ENFORCE 都需要真实设备绑定凭据、有效 DPoP 和当前 `cloud:generate` 授权；不能用自报设备 ID、模式或旧 Bearer 令牌代替。
- 只准入当前 PENDING、明确尚未提交且没有在途执行占用的任务，以及合规的后续依赖段。已提交远程 ID、提交时间、上传/预留中、提交结果未知、成功、失败或取消的当前任务不被重置或重发。旧已结束尝试的审计可以保留，不因此阻塞已明确进入新一轮 PENDING 的合法重试。
- 恢复不调用供应商、不创建新的 `GenerationTask`、不重置原任务 ID/状态/结果/账号/失败历史/自动重试次数，也不改变原费用计划。后续原计划中尚未提交的生成仍会收费，因此页面要求用户再次明确“继续原片段及原计划费用”；不能把这项描述为所有后续生成免费。
- 事务使用既有控制锁，与管理员授权变更和并发恢复串行；重新读取当前状态后才绑定。状态变化返回 `H3_RECOVERY_REVIEW_CHANGED`，要求重新查看确认；异常整体回滚，不残留半个授权关联。
- 新操作类型 `h3.resume_authorization`，保存到既有操作表；配对追加审计 `device.h3_resume` 保存查看摘要和分段 ID 作为恢复回执，不存凭据、私钥或素材路径。请求键相同且内容相同只返回原回执；不同查看内容复用原键返回冲突。回执读取不等于新准入，撤销后仍可查原回执但 Worker 不会因此获得新权限。
- 无新增数据库迁移，仍依赖 `0049/0050` 的授权表。本次恢复只覆盖可归属到 H3 批次/分段的历史工作；历史孤立任务或缺少账号归属的数据不能猜测认领，仍需另做受控恢复。

### 17.2 工作台恢复页面

- `device_h3_recovery_routes.py` 接入实际 `web_api.py`，浏览器只用当前网站 Cookie；POST 要求同源及专用请求头，拒绝额外的访问令牌、设备身份和权限字段。`AuthCenterClient` 的三个恢复方法接入原 H3 DPoP 白名单，不向第三方转发证明，也不在拒绝后自动降级重发。
- 本地接口为 `GET /api/new/device-authorization/h3-waiting`、`POST /api/new/device-authorization/h3/{batch_id}/prepare` 和 `/resume`。设备类 401 仍转成独立设备错误，不误退出网站账号；响应不缓存。
- 账号菜单的设备授权页新增 H3 云端等待区，`device-h3-recovery.js` 支持分页、逐批查看原行/段、显式费用确认和继续。打开页面、刷新状态、管理员批准或登录都不会自动恢复原任务。
- 每次重新查看先清空上一轮勾选；确认区域尚未完成读取时不能继承旧确认。网络响应丢失保留同一请求键，用户重试可读取原回执，不新建批次；状态冲突则要求重新查看确认。
- 独立临时 Edge 检查已通过：首次不自动提交、必须勾选确认、分页、丢失响应重试同键、状态冲突退回重新确认、桌面与 390px 窄屏无溢出/脚本错误。模拟页面不代表真实 TPM 或生产验收。

### 17.3 关闭页面后的后台刷新

- 新增 JYD `device_background_refresh.py`，随处理机 Web 服务启动/关闭，不依赖浏览器页面或前端定时器。只扫描当前进程已有的账号会话，不从浏览器/磁盘缓存重建任意登录令牌，也不创建新账号会话。
- 调度每 5 秒检查一次，最多 4 个后台校验工作线程、128 个等待项，同一会话不重复在途；真正有效凭据仍遵守服务器签名协议规定的刷新时间，正常约每 5 分钟刷新，不是每 5 秒签发一次。
- `DeviceAuthorizationSession.background_refresh()` 与前台使用同一会话锁、签名校验、时钟锚点和原设备密钥；前台正忙时跳过本轮。没有密钥时只报告未登记，绝不调用首次创建/登记。待审批或暂时断网按 15 秒退避，其他拒绝按较长间隔；登录失效或需升级停止自动尝试，等待用户处理。
- 只有原仍有效的离线租约可以继续有限宽限；网络失败、时间回拨或重复重试不改变其失效时间。到期/撤销等权威拒绝清除失效凭据，不重新激活，不恢复业务队列。后台日志只记录异常类型，不输出可能含令牌的异常原文/堆栈。
- 退出账号移除并关闭该会话；服务关闭停止排队刷新并关闭句柄，不删除机器密钥。已在途的授权网络请求按既有传输超时收尾，不能为了立即退出而在签名中途删除原密钥。

### 17.4 验证与发布边界

- H3 云端恢复初轮 `40 passed`；增加分页与真实 JYD 请求合同后，恢复/原准入/费用预览/两端合同合计 `92 passed`，报告 `tests/.runtime/device-h3-recovery-contract.xml`。
- H3 恢复阶段网站完整工作树 `707 passed`、0 失败/错误/跳过（303.95 秒），报告 `device-h3-recovery-full.xml`。工作台恢复初轮 `85 passed`，H3/授权/前端相关广泛回归 `202 passed`（216.80 秒），报告 `device-h3-recovery-jyd-regression.xml`。这些报告都在 RH `tests/.runtime/`，集合重叠，不相加。
- 后台刷新专项及授权会话/接口/本地核心/H3 恢复接入回归 `109 passed`、0 失败/错误/跳过（44.73 秒），报告 `device-background-refresh.xml`；更新后的真实两端客户端合同及 H3 恢复回归 `52 passed`（27.80 秒），报告 `device-background-cross-contract.xml`。覆盖五分钟调度、不自动建钥/登记、待审批恢复、断网到期、撤销/登录失效、时钟回拨、前台锁占用跳过、多账号不重复在途、异常日志排敏、退出移除与服务启动/关闭，无真实 TPM 调用。
- 仍待：真实 Windows CNG/ACL/UAC 首次初始化、软件保护受控恢复、独立 Agent/CLI 和其他私有/云端付费入口、无归属历史任务恢复、发布公钥/完整包敏感信息隔离，以及真实两机复制和连续 EXE 更新验收。没有生成真实密钥、运行付费任务、打包、推送、部署或开启生产强制模式。

## 18. TPM 首次初始化与原密钥访问修复（2026-09-01）

本节是 D01 的源码与模拟验证进展，不代表真实 Windows 初始化或完整防复制验收完成。没有改动服务端授权规则、数据库迁移、生成算法或付费任务，也没有执行真实提权。

### 18.1 正式程序的首次初始化

- JYD 新增 `device_identity_setup.py` 与 `device_identity_setup_windows.py`。实际会话注册表改用交互初始化适配器；正常读取、刷新和已有密钥重复申请都只打开原机器级密钥，不启动提权。
- 显式申请且原密钥确实不存在时，以同一个正式处理机 EXE 的固定辅助入口完成 TPM 初始化。辅助入口先于处理机配置、日志、ASR、采集器、网站和任务启动，不把整个工作台提权运行。源码环境不自动提权创建机器密钥；生产信任根未配置仍不能从授权页面申请。
- 使用 `ShellExecuteExW` 的 `runas` 发起 Windows 确认，不调用命令 shell，不绕过 UAC。辅助进程隐藏额外控制台，Windows 安全确认仍由实际处理机上的使用者处理。[微软 ShellExecuteExW 文档](https://learn.microsoft.com/en-us/windows/win32/api/shellapi/nf-shellapi-shellexecuteexw)
- 参数只有固定动作和原进程的 PID/创建时间。辅助进程核对原进程仍存活、创建时间一致、属于同一 EXE 和同一非服务会话，再读取原进程 TokenUser；不信任网页传入的 SID、路径、设备号或命令。另一个管理员替普通用户输入 UAC 凭据时，仍为原工作台运行用户设置密钥使用权限。
- 固定密钥名、机器级作用域、禁止私钥导出和禁止覆盖的规则不变。创建后的初始 DACL 明确保留 SYSTEM/管理员控制及原操作用户的读取使用权限；权限属性持久保存。原始用户重新打开并实际签名成功后才继续网站登记，不把提权辅助程序退出成功直接当成设备获批。

### 18.2 访问修复不是重新激活

- 新增 `device_identity_acl.py`，仅在用户显式修复并完成 Windows 提权后，对固定名称的已有密钥合并原操作用户的读/签名权限；保留原 ACL 的其他条目，不替换整份访问列表，不取得文件所有权，不创建、删除或导出设备私钥。缺少原密钥或存在空 DACL 时明确失败。[微软 SetEntriesInAclW 文档](https://learn.microsoft.com/en-us/windows/win32/api/aclapi/nf-aclapi-setentriesinaclw)
- `POST /api/new/device-authorization/repair-key-access` 只接受 `{"confirm_repair":true}`，要求当前网站账号、同源及 `X-Device-Authorization-Action: 1`；SID、进程 ID、软件批准、密钥名称等额外字段一律拒绝。
- 修复流程先关闭旧句柄；权限修复成功后，用原用户重新读取原密钥，再联网交换签名凭据。不调用 register，不增加设备名额，不恢复任务。云端暂停/撤销的设备即使 Windows 权限修复成功也不能获得业务权限。
- 页面仅在 `KEY_ACCESS_DENIED` 时提供修复面板，必须勾选明确确认。一次修复完成或失败后清空勾选；取消 UAC 不冒充账号退出、未登记或已授权。

### 18.3 并发、取消与未完成辅助进程

- 同一处理机进程的所有账号共用一个初始化协调器；同一时间只保留一个辅助进程，不因页面轮询或其他账号申请而重复弹 UAC。
- 启动辅助进程后最多等待 45 秒取得完成结果；Windows 自身的权限确认可能在进程启动前等待使用者。超时返回 `KEY_SETUP_IN_PROGRESS` / `KEY_INITIALIZING`，保留真实进程句柄，后续读取只查询该句柄。观察失败不等于进程结束，不重新启动、不强杀、不删除密钥。
- 辅助进程后来成功但原申请没有完成网站登记时，页面重新显示申请入口，由用户明确继续申请；此时复用已经存在的密钥，不再次提权。不得用 GET、后台刷新或管理员批准自动补发 register。
- 取消、父进程失效、来源不符、访问拒绝、密钥缺失及策略异常使用独立错误码；不向浏览器输出原生异常堆栈、进程命令行或账号令牌。

### 18.4 验证与仍待完成项

- 初始化/原密钥/启动器模拟测试 `54 passed`，报告 `tests/.runtime/device-key-setup-unit.xml`。覆盖持有原钥时不提权、明确首次创建、并发、超时保留句柄、查询异常、取消、严格参数、PID 重用、不同 EXE/会话拒绝、原用户 SID、ACL 合并及失败清理。
- 工作台初始化/接口/授权会话/后台刷新/本地核心/启动器/前端相关回归 `200 passed`，报告 `device-key-setup-regression.xml`；真实两端代码的授权/H3 恢复/签名本地策略合同回归 `57 passed`，报告 `device-key-setup-cross-contract.xml`。集合重叠不相加；没有把源码合同测试写成 EXE 或真实 TPM 验收。
- 内置浏览器连接独立本地模拟页面：验证首次按钮禁用到勾选、修复取消、修复后保留原设备、等待状态不重复提交，页面无脚本错误。截图接口未能获取图像，本轮只记录页面结构/交互和请求证据，不宣称截图布局验收通过。
- 仍需完成受服务器批准的软件保护初始化/原软件密钥恢复、真实 TPM/ACL/UAC（包括不同 Windows 登录用户）、公用机/独立 Agent 与 CLI 合法执行通路、其余付费及私有入口、发布公钥/整包检查和跨机连续更新验收。现有发布信任根仍为空，暂不打包分发或开启生产强制模式。

## 19. v1.10 开发交接与软件初始化初稿（历史快照）

本节为 v1.10 的文档整理：依据当前文件核对已有实现，不新增或修复代码，不启动授权服务、设备初始化或付费业务。第 18 节的测试报告仍是该阶段的历史证据，不覆盖之后新增的软件许可初稿。

### 19.1 当前状态速查

| 范围 | 当前状态 | 下一项完成条件 |
| --- | --- | --- |
| 云端设备登记、审批、短期凭据 | 已有代码及历史模拟/两端合同测试 | 随后续改动重新回归，再配置真实发布信任链 |
| H3 请求、云端队列与历史批次恢复 | 已有代码及历史测试 | 保持原账号/原任务/原费用快照，补齐发布端到端验收 |
| 本地草稿/导出核心、内嵌队列、后台刷新 | 已有代码及历史测试，非所有入口覆盖 | 审计其余私有入口和独立进程的合法执行通路 |
| TPM 初始化和原密钥权限修复 | 已接入实际处理机入口；模拟测试通过 | 实测 Windows 用户、UAC、ACL、重启和同机覆盖升级 |
| 软件保护初始化 | 只有签名许可服务、客户端校验及显式会话方法初稿 | 先补测试，再接初始化器、稳定身份恢复和页面；不能算已支持无 TPM 电脑 |
| 发布公钥与完整/更新包保护 | 未完成；`TRUSTED_ISSUERS` 仍为空 | 受控配置、公钥编入、复用产物及嵌套归档检查全部完成 |
| 真正跨机防转拷与连续更新 | 未完成 | 两台电脑及 V1→V2→V3→V4 的实际 EXE 验收记录 |

本轮只复核了既有 `device-key-setup-unit.xml`（54 项）、`device-key-setup-regression.xml`（200 项）和 `device-key-setup-cross-contract.xml`（57 项）报告中的零失败/错误/跳过记录，未重新执行这些测试。不能把它们相加，也不能据此声称当前完整工作树已回归通过。

### 19.2 软件初始化许可合同：允许建钥不等于批准设备

目的：软件保护比 TPM 弱，必须先取得管理员在服务器上明确设置的许可；但新电脑尚未创建密钥，此步骤不能要求它先有获批设备凭据。允许已登录账号申请专用、短时、用途受限的初始化许可，随后仍走正常设备登记和管理员批准。

当前源码落点：

- 云端：[initialization.py](app/services/device_auth/initialization.py) 与 [workbench_device_auth.py](app/routes/workbench_device_auth.py)。
- 工作台：[device_software_initialization.py](../../公寓/jyd_plain_json_probe/src/jyd_probe/device_software_initialization.py) 与 [device_authorization.py](../../公寓/jyd_plain_json_probe/src/jyd_probe/device_authorization.py) 中的显式会话方法。

源码初稿约定如下，仍须专项测试验证：

1. `POST /api/workbench/device-auth/software-initialization-permit` 使用当前网站账号身份，请求正文只接受 `nonce` 和 `context_hash`。原始 Windows SID、进程编号、设备私钥及本地路径不上传；摘要仅用于关联本次请求，不是服务器验证过的硬件身份。
2. 服务端重新检查账号启用状态及该账号的 `allow_software`。`OFF/OBSERVE` 不隐含软件初始化同意；未获允许返回 `DEVICE_SOFTWARE_NOT_ALLOWED`。采用与管理员策略变更相同的控制锁。
3. 响应为专用签名许可：`typ=workbench-software-initialization+jwt`，`aud=PublicVideoWorkbench:software-initialization`，`schema=publicvideo.software-initialization.v1`，动作仅为 `initialize-software-key`，当前初稿有效期 120 秒。
4. 许可绑定账号及账号凭据摘要、请求随机数、初始化上下文摘要、产品/环境、策略修订和时间。客户端必须使用已编入的服务器公钥校验，不能从响应或可编辑配置取得新的信任根。
5. 许可只在本次会话内存短暂持有。当前对象提供一次性交付检查以及墙钟/单调时钟约束；这不等于已经实现跨进程或服务端全局一次性消费。初始化器仍需独立验证用途、签名、有效期和实际原进程上下文。
6. 请求许可不创建设备、批准关系、任务或已激活名额。初始化完成后仍需本机持钥登记、管理员批准和正常业务凭据；不能拿此许可直接生成视频、导出或续期设备授权。
7. 登记页面、状态查询、后台刷新、升级流程不得自动申请此许可或自动降级保护。当前尚无软件初始化的页面/辅助进程接线，现有 TPM 辅助入口不能据此被描述为已支持软件初始化。

初始化许可、本地运行模式、设备业务凭据必须采用互斥的用途验证规则，防止把一种签名 JWT 当作另一种使用；遵循 [RFC 8725 §3.12](https://www.rfc-editor.org/rfc/rfc8725.html#section-3.12)。账号令牌和签名许可不得放入进程命令行、普通日志、浏览器响应或发布包。

### 19.3 D01 的下一步代码任务

按下面顺序完成，不重复推翻已有 TPM 初始化与授权会话：

1. **先验证许可初稿。** 云端覆盖默认拒绝、管理员允许、不同控制模式、跨账号、策略撤销、参数/用途混用及事务审计；客户端覆盖签名/签发方/账号/随机数/上下文不符、过期、时间回拨、重复交付和请求超时。增加真实 JYD 客户端消费 RH 测试接口的合同测试，不接真实服务器。
2. **完成原密钥定位规则。** 原 TPM 或软件密钥存在时只复用；记录缺失、损坏、提供程序故障或两边都有同名密钥时必须明确诊断，不自动选一个或生成第二个身份。关联记录只能辅助定位，不能本身授予权限。
3. **设计并实现辅助进程交接。** 使用限定用途的本机通信，把许可交给受控初始化器；通信方式尚未定稿。辅助进程独立复核原进程、原用户及签名许可，不信任浏览器传入的 SID 或 `software_approved=true`，不把普通工作台整体提权。
4. **再接显式页面操作。** 只有管理员允许且用户确认时初始化；显示软件保护等级。取消、超时和失败不创建重复身份，不自动重复弹权限确认，不自动恢复任务。原密钥访问修复与重新登记保持两个入口。
5. **验证免重复激活。** 同一系统上重启、换程序目录、修复权限、清空授权缓存和多次覆盖更新后，原公钥摘要、`device_id`、`grant_id` 保持不变。真实 TPM/软件模式和不同 Windows 用户分别记录，不以模拟测试替代。

D01 完成后继续第 12.1 节 D02～D06；仅实现软件许可并不足以交付防转拷版本。

### 19.4 交付与更新的硬性判据

- **使用者体验：** 首次申请、后台批准一次；正常更新只替换程序并自动重新校验原授权。账号登录过期可要求重新登录，但不能误提示“新机器”。
- **开发者证据：** 每个工作包交付文件清单、接口变化、测试报告、未覆盖项及免激活回归记录。源码模拟、浏览器模拟、真实 EXE、真实两机测试分别标注。
- **发布要求：** 服务器授权功能涉及独立签名配置和 `0049/0050` 迁移，不能只发客户端 ZIP 就认为完成。软件许可初稿本身未增加新的迁移；实际部署仍须核对整段待发布变更。
- **当前边界：** 本轮仅更新这份文档，不提供绕过信任根的打包命令，不推送或部署。完成发布公钥、入口覆盖、整包检查和实际验收前，不把当前构建发给用户当作正式防复制包。

## 20. 受控软件初始化与稳定原密钥定位（2026-09-01）

### 20.1 原设备身份在更新中保持稳定

- 新增 JYD `device_identity_store.py`，实际交互适配器与辅助入口使用 `MachineDeviceIdentity`。固定注册表位置为 `HKLM\SOFTWARE\PublicVideoWorkbench\DeviceIdentity`、值名 `Identity`；显式使用 64 位视图，保存 `publicvideo.machine-identity.v1`、提供程序与公钥摘要，不含私钥、账号令牌或授权状态。注册表视图隔离及显式选择见 [微软文档](https://learn.microsoft.com/en-us/windows/win32/sysinfo/32-bit-and-64-bit-application-data-in-the-registry)。
- 正常启动、刷新和升级只读原定位记录并打开原提供程序的固定机器密钥，核对完整摘要；不会写定位记录或切换提供程序。已记录的软件设备不因 TPM 状态变化切换身份。
- 没有定位记录时只读检查两个提供程序，兼容此前已有密钥：确认仅一个时复用；两边都存在、记录损坏、记录与公钥不符、原钥匙缺失或提供程序无法可靠读取时拒绝自动初始化。无法确认 TPM 密钥不存在不等于可以创建软件密钥。无 TPM 实机的提供程序行为仍须验证，当前不承诺任意无 TPM 电脑均可初始化。
- 只有显式辅助初始化/访问修复才保存定位记录；现有记录不同则拒绝覆盖。密钥已创建但记录写入失败时保留原钥匙，显式重试找到原钥匙后补记，不重新建钥。
- 创建/访问修复使用固定机器级互斥锁，串行处理不同进程、不同账号的初始化。锁异常或等待未完成不删除密钥；异常退出后的下一次初始化重新检查实际状态。普通启动不申请写锁或弹 UAC。

### 20.2 软件保护的显式申请流程

1. 页面只在未登记状态提供可展开的软件兼容选项，同时要求确认“当前处理机申请”和“较弱软件保护”。`POST /api/new/device-authorization/apply-software` 只接受备注、版本及两项严格布尔确认；保留网站身份、同源和专用请求头检查，不接收提供程序、SID、私钥或许可字符串。
2. 会话先检查原钥匙。原钥匙存在则复用，不因用户选择兼容模式换钥。确实没有钥匙时，读取实际原进程/创建时间/Windows 用户上下文，向云端申请第 19.2 节的 120 秒专用签名许可；管理员未允许时，不启动辅助程序或创建密钥。
3. `device_initialization_channel.py` 建立随机名称、单实例、只出站的 Windows 命名管道，禁止远程客户端；ACL 仅开放原操作用户、SYSTEM 与管理员。服务端核对辅助进程的实际 PID，辅助端核对原处理机 PID；原进程创建时间、同一 EXE/会话及 TokenUser 仍由原辅助入口验证。
4. 管道只发送一份不超过 16 KiB 的专用 JSON，不接受命令、任意签名请求或 pickle。签名许可不放进网页、文件或进程参数；参数新增的只是经过严格格式校验的随机通道定位符。读写有超时/取消与句柄收尾，父进程仍保留尚未结束的辅助进程句柄，不因超时重复启动。
5. 辅助程序独立使用编入的服务器公钥验证许可用途、账号摘要、上下文及到期时间；取得机器级初始化锁后，在实际建钥前再次验签检查时间。允许软件建钥不等于设备批准；随后仍以原用户重新打开、签名、登记并等待管理员批准。
6. 正常页面轮询、登录、后台刷新和程序更新不申请软件许可，也不自动降级。软件申请完成或失败后清空兼容确认；页面明确显示“软件兼容保护（较弱）”。访问修复现在沿稳定记录定位原 TPM 或软件密钥，不改变批准关系。

许可签发后最多 120 秒内的本地建钥许可不是即时撤销通道；管理员随后取消软件策略时，云端审批、交换与业务准入仍按最新策略拒绝。不能把建钥成功或签名许可当作业务放行。

### 20.3 验证证据与剩余边界

- 软件许可、原授权与真实两端合同初轮 `89 passed`；加入实际会话→辅助分派→内存 CNG→服务器登记/审批及版本复用流程后，软件专项 `57 passed`，报告 `device-software-bootstrap-contract.xml`。覆盖未允许不提权、无效许可及等待锁后过期不建钥、先待审批、批准后 V1→V4 复用原公钥/设备/批准关系。UAC 和 CNG 为模拟，不是正式 EXE 升级验收。
- 原身份定位/初始化专项 `70 passed`；加入管道及协调器后 `86 passed`，报告 `device-software-channel-unit.xml`。包含一项真实 Windows 临时管道测试，只读取当前 TokenUser 用于管道 ACL、传输模拟 JSON 并关闭句柄，没有创建/读取 CNG 私钥或写实际注册表。
- 工作台授权、初始化、后台、H3、启动器、页面相关回归 `291 passed`，报告 `device-software-jyd-regression.xml`；报告均在 RH `tests/.runtime/`，不进入发布包，重叠集合不相加。
- 本轮网站当前完整工作树回归 `764 passed`、0 失败/错误/跳过（345.64 秒），报告 `device-software-server-full.xml`。这是隔离数据库与模拟供应商的源码回归，不是生产部署或正式包验收。
- 内置浏览器独立模拟页面验证：两项确认缺一不可、管理员未允许的明确提示、失败后清空确认、提交后待审批且保护等级真实、仅一次提交请求；无脚本错误，桌面截图已检查。此处不使用真实账号或 UAC，也不替代跨电脑验收。
- 仍待完成 D01 的真实 TPM/无 TPM/不同 Windows 用户验证，D02/D03 的合法独立 Agent/CLI 与其余私有/付费入口，D04/D05 的发布信任根和整包保护，以及 D06 的真实两机与连续 EXE 升级。当前发布信任根仍为空，不打包分发或启用生产强制模式；没有推送、部署、生成真实授权密钥或执行付费任务。

## 21. 命令行受控执行与独立 Agent 审计（2026-09-01）

### 21.1 本阶段已接入的入口

| 入口 | 实际接入 | 范围与边界 |
| --- | --- | --- |
| `JianyingRenderServer.exe --render-job` | `processor_windows.main()` 在渲染调用外建立本命令专属账号/设备会话 | `run_render_job` 仍在真正执行前按配置决定草稿/导出权限；没有合法授权时不调用渲染体 |
| `tools/jobs/run_render_job.py --job` | 复用同一 `command_authorization` | 不接受任务 JSON 中的授权字段作为凭据；任务文件不保存登录令牌 |
| `tools/probe/run_probe.py` / `jyd_probe.cli.main()` | 有 `--output-root` 时在复制/修改草稿前建立 `local:draft` 执行单元 | 无输出目录的读取、诊断及素材元数据导出保持原行为；不将已交付素材变为不可复制 |
| 核心直接调用 | 继续由 `device_local_execution` 和建草稿/导出核心验证 | 直接调用不自动弹登录、不创建身份；调用方必须建立可信进程内授权上下文 |
| 独立 Agent / 其他脚本 | 本阶段仅核对，未接通合法执行 | 不能把暂时拒绝领取或部分核心会报错当作整个 D02 已完成 |

新增 `device_command_authorization.py` 的职责是账号接入和会话生命周期，不是第二套授权服务器。权限仍由 `LocalDeviceAuthorizer`、云端签名策略及原设备签名凭据决定；普通配置不能直接切换为 OFF。已准入执行单元的安全收尾规则不变。

### 21.2 登录、原密钥复用与免重复激活

1. 命令行支持 `--device-user 用户名`，密码随后隐藏输入；不能从明文命令参数接收密码。没有可隐藏输入的终端时明确失败，不回退到回显密码。
2. 自动化调用可显式使用 `--device-token-stdin`，从非交互标准输入读取一行已有网站令牌。拒绝与用户名模式混用；令牌最多 8192 字符，拒绝空白/控制字符，不放入参数、环境变量、配置或任务文件。调用方也不能将真实令牌写成会留在终端历史的命令字面量。
3. 先按程序编入的信任根核对服务器，再提示输入或发送请求。账号登录/验证只访问该服务器的 `/api/auth/center/login`、`/api/auth/center/verify` 两个固定接口；禁止重定向、限制响应大小、严格解析 JSON，错误不回显响应正文或令牌，不自动重试登录。
4. 账号 ID 只能来自服务器确认的账号。随后使用 `MachineDeviceIdentity` 读取本机原钥匙及固定定位记录，创建本次命令的 `DeviceAuthorizationSession`。该路径不调用 register、初始化、提权、删除或换钥。
5. OFF/OBSERVE 仅按服务器签名策略工作，不自动登记；ENFORCE 必须取得本账号、本设备的有效授权和实际持钥证明。只有草稿权限时不能借此导出视频。
6. 命令正常结束或执行异常都退出进程内上下文并关闭会话/密钥句柄，不删除持久设备密钥。重新启动命令可能需要重新登录，但不等于重新激活，不增加设备或批准记录。

日后完成发布公钥和实际验收后的调用形态为 `JianyingRenderServer.exe --render-job job.json --device-user 你的网站用户名`；此处只说明参数，不是当前可分发包的打包/部署指令。首次设备申请与访问修复仍在本机工作台页面显式完成。

### 21.3 独立 Agent 的下一步，不能用旧接入密码放行

第 21 阶段审计时，`render_agent.py` 的注册、领取、心跳、完成/失败上报使用中央服务的共用接入密码，队列没有实际执行账号筛选。后续已增加第 22 节的协议和可选存储筛选参数，但中央 HTTP 路由尚未调用它们；实际 Agent 仍未接通合法设备授权。在启用源码信任根或冻结包时，`claim_agent_job` 仍暂时拒绝领取，避免旧接入密码替代设备证明，但这不是合法授权通路。

后续必须同时完成：

- 执行机通过自己的账号会话和原设备密钥获准，中央队列按原任务账号筛选；不把中央电脑的授权转交为另一台机器的权限。
- 领取及任务上报有针对中央服务、Agent、任务、请求内容和时效的持钥验证与重放防护；不能接受一个自报 `device_id` 或可复制的永久布尔标志。
- 保留单机单任务、原租约及领取回执；响应丢失不能重复启动。过期/停用时暂停新任务，原已准入任务可按合同安全收尾。
- 凭据不进入任务正文、Agent 配置或日志；共用接入密码只用于连接中央队列，不能代替网站账号和设备审批。
- 按 OFF/OBSERVE/ENFORCE 分别测试合法路径；新旧 Agent、跨账号、另一台设备、重启/重放、授权失效以及上报失败均需覆盖。其余私有工具仍需逐项审计，未覆盖功能不得擅自移除。

### 21.4 验证及发布状态

- 工作台授权、初始化、后台刷新、核心执行、启动器、前端和 H3 相关回归 `346 passed`，报告 `device-command-jyd-regression.xml`。命令行初轮专项 `108 passed`；随后增加正式处理机允许已批准执行、草稿权限不能导出的验收测试，最终命令行/核心/启动器专项 `110 passed`，报告 `device-command-final.xml`。测试集合重叠，不相加。
- 服务端新增 `test_device_command_contract.py`，真实网站登录/验证、客户端授权会话和云端设备接口合同 `9 passed`，报告 `device-command-contract.xml`。验证四个新会话复用原设备/批准记录、复制令牌到无钥匙机器失败、不同账号不继承授权、待审/暂停/撤销拒绝、账号停用先于密钥会话失败，以及 OFF/OBSERVE 不自动建钥。
- 测试使用临时进程内密钥、隔离数据库和模拟渲染体；四会话测试不是四个正式 EXE 覆盖更新，不是真实双机复制验收。没有运行真实 CNG 初始化/UAC、付费业务、打包、推送或部署。
- D02 只完成上表三处命令行接入，独立 Agent 和其余入口仍待继续；D01 实机、D03 其他付费路径、D04 发布公钥、D05 整包保护及 D06 真实验收仍未完成。`TRUSTED_ISSUERS` 仍为空，继续禁止当作正式防转拷包分发。

## 22. v1.13 交接快照：Agent 组件阶段（2026-09-01）

本节依据本地源码与已有测试报告整理。只记录可核对的进展，不把协议组件可用写成处理机已经可以正常领取、渲染和回报，也不改变第 1、11 节的“普通更新免重复激活”要求。

本节是接线前的历史快照；后续已完成的实际 HTTP/运行接线见第 23 节。

### 22.1 已有实现与仍缺少的连接

| 层次 | 已有代码 | 未完成或必须保留的边界 |
| --- | --- | --- |
| 云端许可 | `app/services/device_auth/agent_permits.py`、`app/routes/workbench_device_auth.py` | 提供 Agent 专用短时许可；不创建设备、批准关系或生成任务 |
| 请求协议 | JYD `device_agent_protocol.py` | 签名和请求上下文校验组件已存在；不是普通云端访问令牌，不接受客户端自报授权 |
| 本机请求授权器 | JYD `device_agent_client.py` | 可复用既有账号会话/原密钥获取并验证许可，但 `AgentApiClient` 尚未调用 |
| 中央验证器 | JYD `device_agent_gate.py` | 一次性挑战与持久消费组件已存在，但中央应用尚未安装挑战入口及验证流程 |
| 队列归属 | JYD `device_agent_queue.py`、`task_store.py` | `claim_job(..., authorization=...)` 可按账号/范围筛选，现有 HTTP 领取尚未传入已验证身份 |
| 实际执行与回报 | JYD `web_api.py`、`render_agent.py` | 仍待接入账号登录、持钥验证、本地执行上下文和幂等收尾；不能解除旧 Agent 的临时领取拦截后直接发布 |

这些改动不新增云端 Alembic 迁移，当前源码仍以 `0049/0050` 为授权基础。中央验证器构造时会建立本地 `device_agent_challenges` 表；当前实际应用尚未构造该验证器，不能说运行中的中央服务已经启用了新表和协议。发布时仍需核对整个待部署差异，不能由本段推导出“只做代码部署即可”。

### 22.2 Agent 单请求授权合同

目标流程为：执行机准备请求 → 向中央服务取得本请求挑战 → 向可信云端申请短时许可 → 用本机原密钥签署该请求 → 中央验证并消费挑战 → 在同一业务流程中校验任务归属及权限。

其中云端许可和两端验证组件已存在，中央挑战 HTTP、实际请求调用与业务接线待做。

- 云端接口：`POST /api/workbench/device-auth/agent-permit`；正文严格为 `nonce`、`context_hash`、`intent`，`intent` 只能为 `execute` 或 `report`。账号、设备、模式和权限来自服务端验证结果，不接受正文注入。
- 响应只含 `agent_permit`。签名用途为 `workbench-agent-request+jwt`，受众为 `PublicVideoWorkbench:agent-request`，协议为 `publicvideo.agent-request.v1`；最长有效 90 秒，可能因原凭据或授权到期而更短。
- 请求上下文绑定中央服务的完整目标 URI、POST 方法、Agent ID、用途及规范化正文摘要；涉及任务的路径也绑定具体任务 ID。许可不能换服务、换账号、换 Agent、换任务或换正文使用。
- 本机证明使用独立的 `workbench-agent-proof+jwt`，绑定许可摘要、同一上下文、挑战、签名时间和唯一请求号。客户端只使用原机器密钥，不把任意载荷代签接口暴露给网页。
- 中央挑战最长 120 秒，仅保存摘要、上下文、期限和消费状态；验证通过后在数据库中原子消费，并发重放只能有一次成功。验证器重建会使旧未消费挑战失效；实际接入应在中央服务生命周期中维护一个验证器，不能每次请求新建。多进程运行需另行验证生命周期约束，当前不宣称支持。
- 云端许可与本机证明不进入任务 JSON、普通日志或 Agent 配置。已有共用接入密码可继续承担中央连接限制，但不能替代网站账号、设备授权或持钥验证。

此处是针对本项目中央 Agent 的专用协议；不把它称为完整 OAuth 或标准 DPoP 实现。向云端请求许可所用的既有 DPoP 与向中央发送的 Agent 专用证明，是两个不同用途。

### 22.3 新执行与原任务回报必须分开

| 用途 | 云端当前规则 | 中央接入时必须额外验证 |
| --- | --- | --- |
| `execute` | ENFORCE 下要求原账号有效、设备绑定凭据及持钥证明、当前有效授权和本地执行范围 | 真实任务属于该账号；获准范围覆盖实际建草稿/导出行为；执行前本机核心再次授权 |
| `report` | 不授予本地执行范围；ENFORCE 下允许有效账号凭原密钥证明申请，不要求原设备关系仍可开始新任务 | 当前任务已分配给同一账号与同一原执行密钥；只能回报既有任务，不能借此领取或启动新任务 |

`report` 许可本身不证明该账号拥有某个中央任务。原任务匹配必须在中央实际写入心跳/结果的事务中完成，不能只在页面、客户端或辅助函数里检查。目前这部分 HTTP/写入接线仍待完成。

OFF/OBSERVE 也只按可信云端签名的模式工作，不由普通配置自报；尚无设备身份时不自动登记或初始化。账号停用不能通过“任务收尾”绕过，无法回报时保留结果等待有权处理，不重复渲染。

已存在的存储组件会按服务端保存的原任务账号筛选，不相信任务载荷里的 `user_id`；原任务来源记录与 Agent 执行关联分别保存。受保护分配的租约到期会进入 `agent_recovery_required`，保留原分配且不自动重排、不增加重试次数。它解决的是存储层误重排，尚不代表已解决实际 Agent 重启、回执丢失和结果回报幂等。

### 22.4 下一阶段代码任务与顺序

1. **中央 HTTP 接入。** 新增受限挑战入口，从真实请求重建上下文，接入一个生命周期受控的验证器；注册、空闲心跳、领取、任务心跳、完成/失败上报逐项验签。许可拒绝后不得降级为旧密码放行。业务参数校验及任务归属检查仍在服务端执行。
2. **Agent 登录与网络请求。** 接入现有网站账号及本机原密钥会话，保持账号令牌只在内存；复用 `AgentRequestAuthorizer`，限制请求目标和响应大小、拒绝携带凭据的重定向，错误输出脱敏。CLI 和 GUI 均需可用，不以只拒绝非法调用代替正常流程。
3. **领取与真正执行。** 将已验证的决定传入队列，保持单机单任务、原任务 ID 和真实权限计算；Agent 调用 `run_render_job` 时建立自己的本地授权上下文，不能借用中央电脑的设备授权。
4. **持久回执与安全恢复。** 在真实渲染前记录任务/执行关联；明确区分“尚未启动、执行中、结果已生成待回报、已确认”。完成回报丢失只重传原结果，不再次渲染；执行是否开始不明确时暂停恢复，不自动重新领取。中央结果写入须有幂等判据，重复完成不能覆盖不同结果，迟到失败不能把成功任务改回失败。
5. **补全实际链路测试。** 测试必须穿过真实中央 HTTP 路由和 Agent 调用流程，包括合法领取完成、不同账号/设备拒绝、权限不足、停用后仅回报、请求重放、程序重启、领取/开始/完成响应丢失和原结果恢复。供应商与渲染体继续使用 mock，不产生费用或操作真实剪映。

特别风险：当前 `render_agent.py` 把渲染与完成回报放在同一个异常处理块中，完成回报失败可能被当作渲染失败报告。后续接线必须拆分两类失败并保留已生成结果；仅增加签名请求头不能解决这个问题。

D02 的这部分完成后，再按第 12.1 节推进 D03 的剩余付费入口、D04 发布公钥、D05 包检查和 D06 双机/连续升级验收。涉及 LTX 等未确认范围，先给出影响说明，不自行改变其业务。

### 22.5 已有验证记录与本次文档边界

本次只读取以下本地报告，未重新运行测试；报告位于不入库的 `tests/.runtime/`，不应加入交付包：

| 报告 | 已记录结果 | 可以证明的范围 |
| --- | --- | --- |
| `device-agent-contract.xml` | 42 项，失败/错误/跳过均为 0 | 初轮云端许可、客户端协议和中央验证器组件合同 |
| `device-agent-queue-contract.xml` | 47 项，失败/错误/跳过均为 0 | 扩展账号归属、权限筛选、原执行关联及租约不自动重排；覆盖前一集合，不能相加 |
| `device-agent-jyd-regression.xml` | 316 项，失败/错误/跳过均为 0 | 当时所选的工作台队列、授权、初始化、核心、启动器、前端和 H3 相关回归；不是工作台全量 |

报告是生成时的源码测试证据，不是当前每个未提交文件均已重新验证的保证；也不是实际中央 HTTP/Agent 完整链路、正式 EXE、真实 TPM 或跨电脑测试。此次整理不对当前网站全量回归作结论。

当前发布门槛保持不变：`device_trust_roots.py` 的 `TRUSTED_ISSUERS` 仍为空；真实首次初始化、剩余入口、发布配置与包检查以及至少两台电脑和三个连续升级步骤的验收尚未完成。不可把“文档已写好”或“47 项组件合同通过”理解为完整包已能防转拷。本次没有改程序代码、创建真实密钥、打包、推送、部署或修改生产数据。

## 23. 独立 Agent 接入与持久执行回执（2026-09-01）

### 23.1 实际接口与执行机登录

- `web_api.py` 已安装 `device_agent_routes.py`，中央应用生命周期中只有一个 `AgentAuthorizationGate`。新增 `POST /api/agents/device-authorization/challenge`，严格接收 `agent_id/path/payload`；中央根据真实访问地址和请求重建上下文，不能由客户端提供“已授权”结果。
- 原注册、空闲心跳、领取、任务心跳、完成/失败接口在受保护构建中都需要 Agent 许可/持钥证明；仅旧共用接入密码明确拒绝。空信任根的源码开发路径仍保留旧测试用途，冻结 EXE 不能借此放行；携带新协议头的请求即使在源码环境也必须完整验证。
- 新增 `POST /api/agents/{agent_id}/jobs/{job_id}/start`，用途为 `execute`，请求包含 32 位十六进制 `execution_id`。中央原子检查原任务账号/设备、当前执行范围、取消状态及原启动回执；首次记录后才确认启动，同一编号可幂等取回回执，不同编号不能重复启动原任务。
- `AgentApiClient` 由 `device_agent_transport.py` 实现，每个请求先取中央挑战，再向可信云端取许可，最后只发送一次实际请求。禁止重定向；成功响应上限 8 MiB，错误正文只有限解析安全错误码，禁止回显凭据或供应商正文。
- Agent CLI 增加 `--device-user` / `--device-token-stdin` 和 `--device-auth-server-url`；授权网站与中央队列地址分开。GUI 增加网站授权地址、账号和本次密码；仅保存网站地址/用户名，不保存网站密码/令牌。后台线程用一次性容器交付密码，登录后不让线程参数长期保留原密码。
- CLI/GUI 复用 `account_authorization`、原机器密钥和网站批准关系，不调用设备登记、初始化或 UAC。Agent 内部编号在基础编号后附稳定网站用户 ID，以区分同机不同账号；这不是设备 ID，也不会新增激活名额。

### 23.2 队列事务与一次执行

`device_agent_operations.py` 处理注册归属、启动与回报事务。已绑定的 Agent 编号不能被另一账号/密钥直接覆盖；签名观察模式下尚无密钥的空闲登记，可由同一账号升级为其实际密钥绑定，不能反向降级覆盖原持钥身份。

领取只选择服务端记录属于本账号、尚未分配且权限满足的任务。一个已验证设备公钥在同一个中央队列中最多有一个运行任务，不能换 Agent 名称或网站账号绕过这一槽位。没有实际设备密钥的 OFF/OBSERVE 兼容路径不提供同等的物理设备并发识别保证。

实际渲染通过 `AuthorizedAgentRunner` 建立执行机自己的 `LocalDeviceAuthorizer` 和本地执行单元，按真实载荷决定草稿/导出范围。中央账号或任意任务 JSON 都不能替代本机持钥授权。

本地回执位于 `%LOCALAPPDATA%\JianyingRenderAgent\execution-receipts.sqlite3`，与 EXE 目录/版本无关；只保存原任务、执行编号、阶段、输入和结果，不保存授权令牌或私钥。更新及打包保护还必须把此目录纳入“保留但不分发”的检查。相同 Windows 用户下使用文件锁阻止多个 Agent 同时控制剪映；跨 Windows 用户的真实行为仍须实机验收。

| 本地阶段 | 含义 | 重新启动后的行为 |
| --- | --- | --- |
| `prepared` | 已持久保存原任务和执行编号，尚未调用渲染器 | 以原编号查询/取得中央启动回执，再记录执行状态后才执行；不能换输入或编号 |
| `executing` | 在调用渲染器之前已经持久记录 | 执行是否完成不明确，暂停自动处理；不能重新渲染，人工核实恢复工具仍待补齐 |
| `report_pending` | 渲染成功或明确失败的原结果已保存 | 使用 `report` 许可只补报原结果；不必先取得新任务注册/领取许可 |
| `acknowledged` | 中央已确认原结果，或确认启动前已经取消 | 不再次执行；中央异常返回同一已完成任务时明确冲突 |

中央启动确认丢失时，原本地回执仍为 `prepared`，可以复用同一编号继续；一旦本地记录为 `executing`，即使实际上尚未来得及调用剪映，也宁可等待核实，不能猜测重做。没有本地旧回执时，新生成编号也会被中央原启动记录拒绝。

### 23.3 收尾、取消与恢复安全

- 渲染与结果回报分为两个错误处理阶段。结果生成后先持久化；回报 HTTP 失败只留下待回报记录，不能转成“渲染失败”或重新调用渲染器。
- 完成/失败写入在中央事务中同时核对原账号、原执行密钥、原任务和执行编号。相同结果摘要重复回报只返回原回执；不同结果或迟到失败不能覆盖已有成功，也不再次延长保留期限。
- 设备暂停/撤销后，有效账号仍可用原密钥回报原已准入任务；该许可无执行范围。原网站账号失效则保留本地结果等待合法处理，不借“收尾”绕过账号规则。
- 启动回执尚未建立时，用户取消可以原子结束已领取但未执行的任务并释放分配；Agent 收到取消回执后不调用渲染器。已经开始的单元继续安全收尾，不强杀剪映。
- 租约过期不自动把已启动任务交给另一台机器。明确尚无启动回执的原领取可以续接；已有执行记录时保持不确定状态，原结果可按原身份回报。
- 导入旧状态文件不能覆盖已有数据库状态；数据库缺失时，带 Agent 授权关联的历史活动记录保留原执行关联并标为待核实，不导入成可自动领取的 `pending`。切换内嵌/Agent 模式也不能把这些记录改成“失败，请重新提交”。
- 结果提交已成功后的状态文件同步或清理失败不能把任务改回失败。数据库为准，后续清理与原结果回报分开。

### 23.4 验证记录与尚未完成项

- 真实云端许可 → 中央实际 HTTP → Agent 运行与本地核心上下文合同 `66 passed`，报告 `device-agent-live-http-final.xml`。包含原 47 项组件合同，集合不能相加。覆盖正常执行、原账号/原设备、请求改写与重放、跨账号/跨密钥拒绝、领取/启动/完成响应丢失后的重启、执行中崩溃拒绝重做、停用后的原结果补报、最小权限组件、取消、同设备不同名称并发和旧状态恢复。
- 新增网络/回执/Agent 命令行测试连同原命令行、线程与队列回归 `80 passed`，报告 `device-agent-runtime-unit.xml`。随后补充 GUI 模拟控件登录和一次性密码交接，本模块最终 `18 passed`，报告 `device-agent-gui-unit.xml`。验证网站密码/令牌不进配置，登录先校验授权网站；这是模拟控件，不是真实 Windows GUI 布局验收。
- 较早的中央旧接口/队列/本地核心回归 `54 passed`，入口与启动器回归 `70 passed`；均为本阶段过程中生成的报告，不能当作最终全量或与上述集合累加。
- 扩大回归：网站完整测试集 `839 passed`（610.73 秒），报告 `device-agent-server-complete.xml`；工作台选定的授权、Agent、中央队列、启动器、前端及 H3 相关回归 `341 passed`（255.69 秒），报告 `device-agent-jyd-complete.xml`。两份报告均无失败、错误或跳过，位于 RH `tests/.runtime/`；工作台这份是选定集合，不是全部测试。随后 GUI 一次性密码交接的最终修改另由上面的 18 项模块测试覆盖。各报告集合重叠，不相加，也不替代真实硬件、GUI 或完整发布包验收。
- 本阶段主要修改 JYD 接线和测试，没有新增云端迁移、H3 算法或付费调用。待部署的整个工作树仍含此前的云端授权接口、依赖及 `0049/0050`，不能只更新 Agent EXE 就声称发布完成。
- **D02 仍有剩余工作：** `executing` 不确定状态的受控人工核实/回执恢复工具、其他私有脚本/旧入口审计、GUI 实机与多 Windows 用户验证；不能要求用户删回执库或改 SQL 来解除安全暂停。完成正常 Agent 路径不等于 D02 整体完成。
- **发布限制不变：** D01 真实设备初始化、D03 剩余付费路径、D04 发布公钥、D05 包检查、D06 真实两机与连续 EXE 更新仍未完成。未生成真实密钥、打包、推送、部署、修改生产数据库或运行真实剪映任务。

## 24. 中断任务核实恢复与本次交付要求（2026-09-01）

本节更新第 23 节的交接状态：人工恢复已有源码及自动化验证，不再是待设计功能；真实 Windows 窗口、跨用户、硬件与发布验收仍未完成。历史章节的“未接通”描述只代表当时状态。

### 24.1 人工核实恢复，不重新执行任务

- Agent 1.2.0 增加“核实中断任务（不重新渲染）”窗口以及 `--recover-list`、`--recover-job`、`--recover-reports` 命令。入口复用原网站账号与原机器密钥，不登记新设备，不弹 UAC，也不调用渲染器、领取或重新排队。
- 中央新增 `POST /api/agents/{agent_id}/jobs/{job_id}/recovery/prepare` 和 `recovery/resolve`。两个接口都要求专用 `report` 许可与原设备持钥证明；旧共用密码不足以放行。核对原任务、原账号、原公钥、原执行编号与原任务输入摘要。
- 核实视图包含当前状态摘要 `review_hash`。只有原任务仍在执行状态且租约已过期时，才能人工接受现有成品或明确结束；当前租约仍有效时拒绝。已经完成/失败的任务只能同步原结果，不能用新结论覆盖。
- GUI 要求分别确认“原执行已经停止”和“已检查完整成品”，再经过默认取消的二次确认；检查文件和网络请求在后台线程进行。CLI 修改操作要求交互终端输入包含完整任务号、执行编号的确认短语，没有自动 `yes` 参数。
- 恢复正文严格限定执行编号、视图摘要、请求编号、处理方式、结果、错误和两项布尔确认。中央变更、重新心跳、输入或结果变化后，旧视图不能继续用于首次确认；必须重新核实。
- 本地先在同一事务写入 `recovery_pending` 和 `recovery_audit`，再发送确认。响应丢失或本地确认写入失败时只补发原请求编号及原结论；中央幂等返回原回执。明确的租约/视图冲突恢复为待核实；网络结果不明时保留待回报状态，不猜测失败或重新渲染。
- 设备已被撤销但网站账号仍有效时，只允许原设备收尾原任务，不能领取新任务。跨账号、跨密钥、旧接入密码和不同执行编号均被拒绝。原回执缺失时仍安全暂停，不支持通过删除回执库或修改 SQL 来恢复。

### 24.2 原文件验证与保留规则

`device_agent_recovery.py` 只检查原任务明确指定的草稿和导出路径；不让用户随意选另一文件充当原结果，不猜测动态草稿名或导出路径。拒绝相对路径、路径穿越、符号链接/重解析点、空文件和超出限额的文件。

核实草稿 JSON 与需要导出的 MP4 时记录文件大小、修改时间和 SHA-256；检查前后比对文件身份及元数据，提交前再次计算证据并要求与显示时一致。MP4 的基础容器检查不是完整解码或质量验收，仍由操作人实际观看完整视频、核对声音与内容。缺少可确定的原输出时不能选择“接受成品”，只能核实后关闭原任务或同步中央已有结果。

Windows 回归发现 `os.fstat()` 与 `Path.stat()` 的 `ctime` 表现不一致，会误报文件变化。修正为同一打开句柄前后仍检查 `ctime`，跨接口检查稳定文件标识、大小和修改时间；内容摘要检查保留。真正的句柄内变化仍拒绝。

人工恢复不删除草稿、视频或回执，不执行即时批次模板清理；正常独立保留策略仍适用，不能理解为永久保留。更新程序必须保留 `%LOCALAPPDATA%\JianyingRenderAgent`，但不得将此目录打进发给其他电脑的包。

### 24.3 补齐旧工具的合法入口

- `tools/jobs/simple_job.py`、`swap_video_subtitle_job.py` 接入同一网站账号/原密钥命令行会话，并在草稿修改前校验权限。
- `tools/jobs/local_mp4_loop.py` 只读取一次任务配置，再按实际任务决定草稿或导出权限；直接导出函数也校验 `local:render`，不能凭草稿权限调用导出。拒绝时不提前创建业务文件。
- 不把账号令牌放进任务 JSON、命令行参数或配置；会话结束关闭句柄但不删除持久密钥。上述修改未改变视频生成、字幕或 ASR 算法，没有新增云端迁移。

### 24.4 本阶段验证记录

报告位于 RH `tests/.runtime/`，是分阶段源码测试，集合有重叠，不能累加，也不能代替最终全项目回归：

| 报告 | 结果 | 范围 |
| --- | --- | --- |
| `device-agent-recovery-http.xml` | 27 passed | 新恢复合同及实际 HTTP/Agent 运行合同 |
| `device-agent-recovery-final-contract.xml` | 61 passed | 恢复安全、原设备撤销、跨账号/密钥、丢失确认、原协议回归 |
| `device-agent-recovery-gui.xml` | 29 passed | 恢复控制器、CLI、文件证据、模拟窗口与 Agent 运行 |
| `device-agent-recovery-jyd-regression.xml` | 153 passed | 当时选定的恢复、队列、本地核心和启动器回归；不包括随后旧工具最后修改 |
| `device-private-command-entries.xml` | 64 passed | 命令行授权及三处旧工具的新增拒绝/合法调用用例 |

完成本节后的最终回归与打包工具验证记录：RH 全量 `853 passed`；JYD 全量首次为 `1201 passed / 3 failed`，三个失败均为测试/文档合同问题：两个真实子进程没有显式继承项目 `src` 路径，一个断言仍要求旧文案。修正测试隔离和稳定合同短语后，原失败模块及新增只读发布诊断 `21 passed`。Pu/LTX 加入发布检查后的全量 `61 passed`。报告分别为 `release-rh-full-20260901.xml`、`release-jyd-full-20260901.xml`、`release-jyd-fixed-20260901.xml` 和 `release-ltx-final-20260901.xml`；测试集合不相加。完整 JYD 通过的 1201 项未因测试隔离修正重跑，新增/修正范围已专项复验，仍须在正式公钥到位并生成 EXE 后做包级启动验收。

窗口使用模拟控件验证双确认和线程规则，未操作真实剪映或申请真实设备授权。早期失败已用来修复文件误报和测试读取方式；后续全项目测试仍须重新记录最终结果。

### 24.5 用户要求的最终测试与两个包

1. 核对当前实际代码，不只检查设备授权新增测试：至少运行 RH、JYD 和 Pu/LTX 项目的完整自动化测试集，修复回归后复验；区分环境依赖缺失、测试失败和未获准的真实业务检查。
2. 更新包沿现有程序更新合同，包含可执行程序、前后端代码及程序运行必需文件，不包含业务数据、素材库、ASR 模型和环境全量。不是拿源码 ZIP 冒充可覆盖更新的程序包。
3. 完整部署包包含 Pu、受保护工作台、所需程序依赖、本地 ASR/模型及必要工具；排除业务视频、图片/贴图素材库、模板私有数据、草稿、生成结果、账号配置、令牌、设备状态和 Agent 回执。模型与程序界面图标是运行依赖，不属于业务素材。
4. 正式构建必须使用获准的服务器公钥；服务器私钥只由管理员保管，不能进入源码或客户端包。复用旧 dist、基础 ZIP、嵌套归档同样要检查，不以 `SkipBuild` 绕过版本和敏感文件验证。
5. 交付记录包名、大小、SHA-256、包含/排除项及启动验证结果；仍需两台实际电脑验证首次批准、复制后拒绝及连续升级免激活。测试数据库通过不等于生产授权服务已配置可用。

用户随后明确接管代码更新包，当前只交付完整环境包。生产签名根初始化另经用户明确授权；该授权不包含部署业务代码、修改设备授权数据库、开启 `ENFORCE`、推送 Git 或运行付费任务。

实现进展：`release_guard.py` 已接入两个统一构建脚本，拒绝空/测试信任根、私钥字段、旧基础包复用、路径穿越、重解析点、数据库、设备/Agent 状态、业务音视频、素材数据和嵌套归档夹带。完整包只从本次程序产物、干净默认配置及显式 ASR 环境组装；ASR 复制移除 `.env`、示例音视频、缓存日志、Python/依赖测试目录、依赖开发代理文档、空 wheel 及明确列出的示例数据压缩文件，同时保留运行库、开源许可证声明和两类模型权重。正式公钥已经编入并完成本轮完整环境包；代码更新包不在本轮交付范围。

## 25. 签名根工具与非 LTX 付费入口准入（2026-09-01）

本节更新第 24 节的发布交接。源码工具和自动化测试已经完成，不代表管理员已经创建正式密钥，也不代表可以跳过实机验收直接开启 `ENFORCE`。

### 25.1 服务器私钥初始化、轮换与客户端公钥编入

新增 `deploy/device_auth_keys.py`，提供四个互斥操作：

- `init`：只允许在 Linux、精确确认词和全新目录中创建 P-256/ES256 服务器私钥、公钥清单及服务器环境变量路径文件；拒绝覆盖、符号链接/重解析路径及源码目录内输出。
- `rotate`：读取上一份严格公钥清单，生成新的活动私钥，并在新清单中同时保留旧、新公钥；不复制旧私钥，也不自动切换服务。
- `inspect`：只读校验公钥文档 schema、HTTPS issuer、Key ID、P-256 曲线点，并可核对活动私钥与指定公钥是否匹配。
- `compile-client`：只接受本地严格公钥文档，要求独立确认词，确定性重写 JYD 的 `device_trust_roots.py`。只编入 `kty/crv/x/y`，拒绝私钥字段、重复来源、空根和临时文件碰撞。

公钥文档固定为 `publicvideo.device-trust.v1`、`production` 环境和 `<HTTPS 根域名>/workbench-device-auth` issuer。服务器私钥必须保存在 `/opt/runninghub-video` 之外；客户端构建电脑只接收公钥 JSON，不能接收 PEM 或生成的服务器环境文件。完整命令、轮换先后顺序和 `.env` 字段见 `deploy/README.md`。

工具不会部署、修改设备/授权数据库、打开强制模式或登记设备。2026-09-01 经用户明确授权，在生产服务器独立目录初始化 `production-20260901-01` 正式签名根；只导回 `device-auth-public-keys.json` 并编入 JYD，服务器私钥留在受控目录。初始化期间没有修改应用 `.env`、数据库或服务，四个现有服务保持运行，业务代码未部署。

### 25.2 普通工作台与声音付费任务

`workbench.py` 的下列非 LTX 新操作已接入同一账号、本机持钥证明和 `cloud:generate` 范围：内容/视觉分析、官方声音首次试听、自定义声音激活、声音制作与保存、声音批次创建/付费重试、普通数字人交接/付费重试以及 SeedVR2 历史补跑。声音试听等已落盘缓存、已完成回执、只下载恢复和只合并恢复不创建新的准入操作；内容/视觉分析即使命中服务端缓存也仍要求当前设备有效，但不会再次调用供应商。

`workbench_device_operations` 和 `workbench_device_work_bindings` 现在还可关联 `audio_generation_task` 与 `voice_creation_task`。声音批次、声音制作及普通数字人任务把设备准入和业务变更放在同一事务；音频切分产生实际 `GenerationTask` 时继承原交接操作，不接受客户端提交操作 ID。

三个后台执行器分别在领取和真正执行前复核当前授权：

1. RunningHub 视频 Worker：覆盖 H3 与 `new_workbench` 普通数字人任务；已有远程 ID 或提交结果未知时继续安全查询/收尾，绝不因撤销重提。
2. MiniMax 音频 Worker：只约束 `new_workbench` 来源；已有异步供应商任务 ID 时继续查询原任务，未提交任务暂停在本地队列。
3. 声音制作 Worker：依靠新加入的 `source_channel` 区分新工作台与旧网页；旧网页任务不被改成设备授权任务。未开始的工作台声音任务在暂停/撤销时等待，恢复原授权后继续原任务。

`task_management.py` 的共享付费重试边界按数据库中的真实来源判断，防止普通工作台借旧网页重试接口绕过。服务器根据远程 ID、增强阶段和状态区分“只恢复下载”与“再次调用供应商”，不接受客户端自报 `download_only`。

LTX 没有在本阶段改造。其多阶段准备、分段生成与历史恢复需要单独设计稳定的批次准入继承；依据用户此前明确范围，本轮没有修改 LTX 算法或把未配套恢复入口的强制拦截半接进去。开启全产品 `ENFORCE` 前仍须完成 LTX 审计，或明确产品级隔离策略。

### 25.3 本阶段验证与剩余发布门槛

当前新增/相关验证均未调用真实 MiniMax、RunningHub、剪映或生产数据库：

| 报告 | 结果 | 范围 |
| --- | --- | --- |
| `device-paid-entry-regression-20260901-2.xml` | 113 passed | 普通声音、音频 Worker、分析、共享重试、H3 与历史补授权回归 |
| `device-paid-workers-20260901-2.xml` | 4 passed | 绑定声音重试/声音制作，暂停后拒领、恢复后续领 |
| `device-paid-migrations-20260901.xml` | 17 passed | 全迁移链、0050 新字段/约束及设备迁移保留 |
| `device-key-tool-final-20260901.xml` | 44 passed | 签名工具、严格公钥文档、编入确认及原设备授权回归 |
| `device-retry-lock-fix-20260901.xml` | 38 passed | 修复同一会话未提交失败状态被误判为并发变化，同时保留跨会话旧对象拒绝 |
| `device-auth-rh-full-rerun-20260901.xml` | 882 passed | RunningHub 服务端修复后全量回归 |
| `device-auth-jyd-full-final-20260901.xml` | 1206 passed | JYD/剪映处理器、设备身份、H3、音频和更新合同全量回归 |
| `device-auth-ltx-full-final-20260901.xml` | 61 passed | Pu/LTX 启动器、更新安装器和发布保护全量回归 |

上述专项集合与全量集合有重叠，不能相加。首次 RunningHub 全量为 `881 passed / 1 failed`，失败来自 `prepare_task_retry` 在同一 ORM 会话刚记录供应商失败、尚未提交时，用自动更新后的 `updated_at` 对比旧快照，误报并发变化。修复为先刷新本会话拥有的变更再拍摄乐观锁快照；专项与全量复验均通过。三套全量测试未调用真实 MiniMax、RunningHub、剪映或生产数据库。

### 25.4 正式签名根、正式根回归与完整环境包

- 正式公钥文档规范摘要为 `c468ee0c73491902396a127bca4e4c919425d78813f3ad85a36658c12e19774e`；编入 JYD 后的发布信任负载摘要为 `0775f321d94b98c3ee453c7b21434bdd4a9c9672eb205a50e2bf9ac2a4bdfafa`。两者计算对象不同，不能互相替代。客户端只包含 P-256 公钥坐标，不含私钥字段。
- 正式根下 Pu/LTX 全量 `61 passed`；RH 全量 `882 passed`；JYD 首次因正式根使九个旧业务单测误触生产授权而出现 22 个测试隔离失败，修正为仅在这些旧业务测试模块清空信任根后，专项 `165 passed`，最终 JYD 全量 `1206 passed`、0 失败/错误/跳过。生产代码的正式根和安全测试没有被关闭。
- 第一份完整环境包 `release/PublicVideoWorkbench-full-asr-device-auth-production-20260901-01.zip` 虽通过内容扫描、公钥诊断和 ASR 导入，但随后从最终 ZIP 全新解压启动时，JYD 因空语义素材 catalog 带 UTF-8 BOM 而退出。该包不是合格交付，曾隔离为 `.rejected`，在修正版验收通过后连同旧组装/解压目录一并删除；不得继续使用本节记录的旧哈希。
- 包中没有业务视频/音频、贴图素材库、草稿、项目、生成结果、账号配置、令牌、设备状态、Agent 回执、数据库或服务器私钥。第三方 Python 自测/示例数据也已从打包逻辑排除，开源许可证声明保留。按用户最新要求，不再重建或压缩代码更新包。

下一步仍需：

1. 在两台真实 Windows 电脑验证原机首次批准、复制到另一台电脑拒绝、同机连续覆盖升级免重复激活，以及 TPM/软件模式、UAC、ACL、不同 Windows 用户和 Agent GUI。
2. 补齐 LTX 付费入口及仍未审计的私有入口，再决定何时把生产控制从 `OFF/OBSERVE` 切到 `ENFORCE`。启用前必须先部署服务器迁移和业务代码并做兼容验收，本次没有执行这些操作。
3. 公钥可以进入客户端，服务器私钥继续只留在受控服务器；后续轮换按“先发布双公钥客户端、再切服务器活动私钥、最后观察清退旧公钥”的顺序执行。

### 25.5 修正版完整环境包与实际启动证据

- 根因是 Windows PowerShell 5.1 的 `Set-Content -Encoding UTF8` 会写 BOM，而 JYD catalog 读取器按严格无 BOM UTF-8 读取。生成端已改用 `.NET UTF8Encoding(false)` 同时输出干净 `processor_config.json` 与 `catalog.json`；JYD 读取端改为 `utf-8-sig`，使旧 BOM 配置也能兼容读取。发布扫描同时新增“默认配置禁止 BOM”的失败关闭门槛。BOM 专项 `2 passed`，发布构建/扫描规则 `28 passed`。
- ASR 打包现在由正式复制逻辑自动排除 Python/第三方测试目录、`.agents` 开发文档、空 wheel、缓存和明确的示例压缩数据；本次复制 29,161 个运行文件，自动排除 422 项，不再依靠手工清理。运行库、两类模型和开源许可证声明保留。
- 修正版为 `release/update-production-device-auth-20260901-02-full.zip`，1,806,275,745 字节，SHA-256 `5fdf573058ba1462f332f365ed51240e34dbcfb6648ceb231ffceebe1f316b0e`。最终 ZIP 递归扫描 33,016 项、展开 4,277,859,408 字节，无业务素材、账号/设备状态、数据库或私钥。
- 从最终 ZIP 解压到从未部署过的目录后，ZIP 与目录清单均为 30,653 个文件、3,268,025,479 字节，路径和大小零差异。启动器在独立端口实际拉起数字人和 LTX：两个 `/api/runtime/status` 均返回正常；数字人页面首跳 `303` 到本机登录页，LTX 首页 `200`；包内处理器返回正式信任摘要 `0775f321...dfafa`；便携 ASR 导入 `fastapi/funasr/scipy/sklearn/soundfile/torch` 成功。测试结束后两个端口均已关闭。
- 当前 H3 入口再次静态核对后，设备准入、历史队列恢复与报价生命周期专项 `92 passed`。新生成、实际付费重试和主动重生成在 HTTP/业务边界绑定设备操作，RunningHub Worker 在 queue/dispatch/submit 三个阶段复核；查询、取消、原结果下载和已有远程任务收尾没有被错误改成新付费，也不能借此创建新任务。
- 这证明包可解压和启动，不证明真实设备已经激活或复制到另一台电脑一定被拒绝。A01/A02/A06/A10 仍必须在至少两台实际 Windows 电脑、正式账号审批和后续受控 `ENFORCE` 试点中记录证据。

### 25.6 受控模式切换与 A/本机、B/250 验收工具

- `deploy/device_auth_control.py` 默认只读当前模式、修订和设备数量。写操作要求当前修订号、操作人、原因及逐修订精确确认词；只允许相邻状态切换，禁止 `OFF → ENFORCE` 和 `ENFORCE → OFF`。进入 `ENFORCE` 前核对至少一个有效授权、全局影响确认以及正式私钥/公钥匹配；成功切换与 `device.control_mode_changed` 审计在同一事务提交。
- `app.services.device_auth.service.change_control_mode` 在数据库控制锁内再次复核修订和状态机，不把 CLI 参数直接映射为 SQL。操作人属于服务器运维身份，写入审计详情，`actor_user_id` 保持为空，不冒充网站管理员。
- `deploy/device_auth_acceptance.py capture` 只读服务器权威数据库，以授权备注精确选择 A 或 B；证据只含账号 ID/名称、包摘要、构建名、控制修订、公开设备摘要、设备/授权状态和有限审计动作。输出独占创建、不覆盖，不保存密码、令牌、私钥、供应商 Key、本地路径或硬件序列号。
- `verify-copy` 要求 A=当前开发电脑、B=250，同一账号、同一包、额度 1、不同公钥/设备/授权编号，A 仍为有效，B 未获批准，且 B 证据取自 `ENFORCE`。`verify-upgrade` 要求 A 至少四份不同构建和包摘要，公钥、`device_id`、`grant_id` 及有效状态始终不变。
- 新增操作工具专项 `6 passed`；同时覆盖非法跳级、旧修订/错误确认、无有效授权开启强制、签名配置检查接线、审计、A/B 复制报告、B 误批准/非强制拒绝、四构建身份稳定和证据禁止覆盖。设备授权/迁移/准入相关当前回归 `67 passed`，部署资源回归 `27 passed`；加入工具后的 RunningHub 当前完整工作树 `888 passed`、0 失败/错误/跳过（845.88 秒），报告为 `tests/.runtime/device-auth-operator-rh-full-20260901.xml`。这些集合重叠，不能相加。测试使用隔离数据库，没有创建真实设备、运行付费任务或修改 250。
- 服务器逐步命令和证据文件位置见 `deploy/README.md` 的“设备授权模式切换与 A/B 双机验收”。2026-09-01 只读复核生产 `/api/workbench/device-auth/status` 仍返回 `404 application/json`，证明设备授权业务代码和迁移尚未部署；不能仅凭工具存在开始实机验收。后续必须先经用户明确批准，使用完整部署流程提交当前代码、迁移和四项签名环境配置，保持 `OFF` 验证兼容，再进入 `OBSERVE`。
