# 数字人与剪映工作台集成变更记录（2026-08-03）

## 当前结论

本轮修改已经在本地代码中完成并通过自动化测试，尚未部署到服务器。生产环境数字人网站及其现有账号、数据库均未改动。

系统采用“数字人网站负责账号和数字人生成，剪映工作台负责后期”的边界。工作台会拉取当前登录账号自己的数字人任务，但不会在后台自动导入、自动导出或自动发布；是否导入及最终渲染仍由操作人确认。

## 产品分流

| 输入与结果 | 后处理模式 | 工作台处理方式 |
| --- | --- | --- |
| 文本生成语音，最终只有一个未切分视频片段 | `AUTO_POSTPROCESS` / `AUTO_READY` | 可一键导入视频和精确字幕时间戳，再选择 BGM、字幕样式和保存目录 |
| 文本生成语音，实际时长超过 45 秒并产生多个片段 | `MANUAL_EDIT_REQUIRED` / `MANUAL_READY` | 下载全部原始片段，人工粗剪后再上传或选择成片 |
| 用户直接上传音频 | `MANUAL_EDIT_REQUIRED` / `MANUAL_READY` | 下载生成素材，人工检查和粗剪后进入剪映工作台 |

多片段自动拼接只保留为顺序和内容检查用的“快速预览”，不作为正式成片。原因是图生视频各子任务的首尾画面不保证连续，程序无法可靠替代人工选择切点、删帧和转场。原始生成片段始终保留，供审核人重新下载处理。

## 账号与环境设计

- 数字人网站是普通用户账号的唯一来源，工作台不再维护第二套普通账号。
- 本地联调时，工作台默认连接 `http://127.0.0.1:8000`，使用本地数字人数据库中的测试账号。
- 正式部署时，只把工作台的 `digital_human_server_url` 改为正式数字人网站 HTTPS 地址；服务器现有账号无需迁移。
- 工作台不会保存用户密码。登录由数字人网站验证后签发 12 小时短期令牌。
- 用户停用、删除或修改密码后，旧工作台令牌立即失效。
- 工作台自己的素材、机器和运行参数仍由独立技术管理员维护，不与普通用户账号合并。

## 数字人网站新增接口

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/auth/center/login` | 验证数字人账号并签发工作台令牌 |
| `POST` | `/api/auth/center/verify` | 校验令牌并返回当前用户 |
| `POST` | `/api/auth/center/handoff` | 创建 60 秒有效的一次性交接码 |
| `POST` | `/api/auth/center/handoff/consume` | 消费交接码并建立工作台会话 |
| `POST` | `/api/workbench/tasks` | 拉取当前用户的数字人后处理任务 |
| `POST` | `/api/workbench/tasks/{item_id}` | 获取一条任务的后处理清单 |
| `GET` | `/api/workbench/tasks/{item_id}/videos/{video_index}` | 鉴权下载指定原始视频片段 |

任务接口只返回令牌所属用户自己的 `digital_human` 工作流记录。视频下载也执行同样的归属校验，不能通过猜测任务编号读取其他账号素材。

## 页面与状态修复

- “查看后处理清单”改为面向人的中文页面：`/batches/{batch_id}/items/{item_id}/postproduction`。
- 原有 `/api/batches/{batch_id}/items/{item_id}/postproduction` 保留为机器读取的 JSON 接口，不再直接拿它给操作人查看。
- 批次详情同时显示“当前阶段”和“最终处理方式”，避免生成中的任务提前显示成 `AUTO_READY` 或“需要人工处理”。
- 每个成功的原始片段都提供直接下载入口。
- 所有子任务均取消时，批次状态正确归为 `CANCELLED`；取消批次会清理陈旧的拼接状态。

## 字幕、时长与本地文件

- 文本语音保留 MiniMax 异步任务返回的原始字幕时间戳；单片段任务导入工作台时按原时间戳生成字幕轨道。
- 上传音频没有可信的文本时间轴，因此按人工分支处理，可在剪映中识别字幕并调整。
- 工作台允许预览和编辑字幕；一旦改了字幕文字、视频源或时间轴，导入的精确时间戳会标记为失效并重新生成。
- 对 RunningHub/LTX 偶发的尾部截短增加了受限尾垫处理，修复“请求约 31 秒、结果只有约 29 秒”的已知情况，同时保持单任务不超过 45 秒。
- 数字人网站与工作台本地端口分别为 `8000` 和 `8010`，可同时启动。
- 工作台输出仍保存在本地，保存目录可选择，并会记住上一次选择；本轮不接入阿里云上传表。

## 本地验证

- 数字人网站完整测试：`156 passed`（另有 1 条既有 warning）。
- 剪映工作台完整测试：`183 passed`（另有 30 条既有 deprecation warnings）。
- 已检查 Python 语法和 Git diff 格式。
- 测试未发起付费 RunningHub 生成任务，也未修改生产账号数据库。

## 建议的本地验收顺序

1. 启动数字人网站 `http://127.0.0.1:8000`，确认旧本地测试账号仍能登录。
2. 启动剪映工作台 `http://127.0.0.1:8010/app`，使用同一账号登录。
3. 分别准备三类任务：未切分文本、已切分文本、上传音频。
4. 在工作台“数字人任务”中确认三类状态和操作按钮符合上表。
5. 对单片段任务执行“一键导入工作台”，检查视频预览和字幕时间轴。
6. 对多片段或上传音频任务逐个下载原始片段，验证人工粗剪入口。
7. 选择本地保存目录，完成一次测试渲染；确认本轮不会自动发布。

## 尚未执行

- 未部署或重启正式服务器。
- 未把正式工作台指向云端数字人地址。
- 未自动发布视频，也未恢复阿里云上传方案。
- 未迁移、合并或删除生产数字人账号数据。

