# 更新记录

## 2026-09-03

### 工作台与历史任务页请求风暴治理（本地完成，未部署）

- JYD 工作台账号校验新增 5 秒成功缓存、同 token 并发合并、请求内用户复用和独立短时熔断；
  阻塞式账号中心校验移入受控线程池，慢请求不再占住本地页面事件循环。
- 网站 `/tasks` 改为服务端分页，默认 20 条、最大 50 条；只检查当前页缩略图，缺失或非法路径直接
  显示占位内容，存在的图片使用懒加载和一小时私有缓存。
- 历史页运行状态改为最多 50 个 ID 的轻量批量接口和局部更新；同一时刻只保留一个请求，页面隐藏
  时暂停，失败按 5/10/20/30 秒退避，进入终态后停止，不再每 5 秒整页刷新。
- 批次详情轮询补齐同样的单请求、页面隐藏暂停、卸载取消和失败退避约束，保留原有局部内容更新。
- 新增分页、缺图、图片缓存、权限隔离、批量状态与轮询行为专项测试；没有新增数据库迁移，没有识别
  物理电脑，也没有部署服务器或构建更新包。

### 封面标题与字幕处理彻底解耦（本地完成，未部署）

- 内容规划返回后立即持久化音乐、视觉和两行封面标题，再进入独立字幕请求；
  字幕请求迟缓、超时或进程中断不再丢失已合法生成的封面标题。
- 中间快照保持 `cacheable=false`，字幕阶段未收尾时，后续请求仍必须继续修复字幕，
  不会把只有标题的中间状态误当成完整缓存。

### 豆包请求全链路并发治理（本地完成，待迁移与灰度部署）

- 内容分析和语义视觉接入同一 `ArkRequestManager` 专用公平队列，活动硬上限 10、等待队列有界；
  异步路由等待 Future，不再让长排队占用通用 Web 线程。新增 540 秒总预算、120 秒排队预算、
  连接/读取分离、两次规划尝试、full jitter、`Retry-After` 和按安全配置指纹熔断。
- 新增 `0051_ark_analysis_operations` 幂等账本；同操作 ID/同业务进行中请求合并，重启后未完成
  操作标记为需人工恢复，不自动重放付费请求。账本只保存哈希、状态、错误和缓存关联。
- Ark HTTP 按专用工作线程和配置指纹复用 Session；配置/Key 变化淘汰旧连接，应用退出显式关闭。
  新增 `/healthz/ark` 队列与时延诊断以及稳定的队列满、排队超时、总预算、熔断错误码。
- 默认灰度开关为关闭；开启时只允许单 Web 进程，`ARK_MAX_CONCURRENCY` 不能超过 10。本轮仅修改
  本地代码、迁移、测试和文档，没有调用真实豆包、修改生产数据库或部署服务器。

## 2026-09-01

### 受控模式切换与 A/本机、B/250 验收工具

- 新增服务器专用 `device_auth_control.py`：只允许相邻的 `OFF / OBSERVE / ENFORCE` 切换，要求当前修订、精确确认词、操作人、原因和开启强制的全局影响确认；进入 `ENFORCE` 前检查有效授权及正式签名根，模式和审计同事务写入。
- 新增只读 `device_auth_acceptance.py`：按设备备注生成无秘密 A/B 权威快照，校验复制到 250 后公钥/设备/授权均不相同、A 仍有效、B 未批准且处于强制窗口；连续更新报告要求 A 至少四个不同构建和包摘要保持同一设备/授权。证据文件拒绝覆盖。
- 操作工具专项 `6 passed`；设备授权/迁移/准入相关当前回归 `67 passed`，部署资源回归 `27 passed`，加入工具后的 RunningHub 当前完整工作树 `888 passed`、0 失败/错误/跳过，报告为 `tests/.runtime/device-auth-operator-rh-full-20260901.xml`，集合有重叠不相加。只读生产复核仍为设备授权状态接口 `404`。当前只修改本地源码、测试和文档；没有连接 250、部署生产、修改生产设备数据库、切换模式或运行付费任务。

### 完整环境包全新解压启动修复

- 首份正式根完整包虽然通过递归内容扫描，但从最终 ZIP 全新解压启动时，JYD 因 Windows PowerShell 生成的空语义素材 catalog 含 UTF-8 BOM 而退出；旧包曾隔离为 `.rejected`，修正版通过后已连同旧组装/验收目录删除，禁止交付。
- 完整包生成端改为 BOM-less UTF-8；JYD catalog 读取端兼容 `utf-8-sig`，发布扫描新增默认配置 BOM 拒绝门槛。BOM 专项 2 项、发布规则 28 项通过。ASR 复制规则自动排除测试/示例项，本次复制 29,161 个文件并排除 422 项，不再依赖手工删文件。
- 修正版 `update-production-device-auth-20260901-02-full.zip` 为 1,806,275,745 字节，SHA-256 `5fdf573058ba1462f332f365ed51240e34dbcfb6648ceb231ffceebe1f316b0e`。最终 ZIP 扫描 33,016 项；全新解压后的 30,653 个文件与 ZIP 清单零差异。
- 从修正版解压目录实际启动双工作台：数字人/LTX 健康接口正常，数字人未登录首跳和 LTX 首页正常，正式公钥诊断及便携 ASR 核心依赖导入通过；测试端口已关闭。该证据不替代真实双机、TPM/UAC/ACL、连续覆盖升级和生产 `ENFORCE` 试点。
- 重新核对 H3 新付费/重试/重生成及 Worker 三阶段准入，设备准入、历史恢复和报价生命周期专项 `92 passed`；查询、取消与原任务收尾仍保留原恢复语义。

### 正式签名根与无素材完整环境包（尚未启用生产强制）

- 经明确授权在生产服务器独立受控目录初始化 `production-20260901-01` ES256 签名根；只导回公钥并编入 JYD。服务器私钥未进入源码/客户端；未改 `.env`、设备授权数据库，未重启服务、部署业务代码或开启 `ENFORCE`。
- 正式根下 RunningHub、JYD、Pu/LTX 全量分别为 882、1206、61 项通过。包内处理器信任摘要与源码一致，本地 ASR 的 FastAPI/FunASR/SciPy/scikit-learn/SoundFile/Torch 导入通过。
- 首次生成包的内容扫描结果保留为历史记录；该包随后在全新解压启动中发现 BOM 问题并已隔离，不能交付。可用修正版及新哈希见本日期上方条目。
- ASR 复制规则新增第三方测试目录、开发代理文档、空 wheel 和明确示例压缩数据的排除；保留运行依赖、模型和开源许可证声明。发布安全测试 27 项通过。按用户最新要求，本轮不再压缩或交付代码更新包。

### 设备签名根工具与非 LTX 付费准入（开发中）

- 新增受控 Linux ES256 私钥初始化/轮换/只读核对工具，以及只接受严格公钥文档的 JYD 信任根编入命令；私钥拒绝写入源码目录，工具不自动部署或启用强制模式。
- 非 LTX 新工作台的内容/视觉分析、声音试听/激活/制作/保存、声音批次及重试、普通数字人交接/重试和 SeedVR2 补跑接入设备准入。RunningHub、MiniMax 音频和声音制作 Worker 在领取/执行前复核当前授权；已有远程 ID继续原任务，不因撤销重复付费。
- `0050_device_work_admission` 在尚未发布的同一迁移中补充声音任务来源与声音/音频关联类型；旧网页来源不套用工作台设备规则。LTX 依照当前范围未改算法或半接强制拦截，仍列为发布前待办。
- 修复同一数据库会话刚记录供应商失败时，受保护任务手动重试被 `updated_at` 乐观锁误判为并发变化的问题；跨会话旧对象仍会拒绝重复重试。
- 相关回归 113 项、付费 Worker 专项 4 项、迁移 17 项、密钥工具/原授权 44 项通过（集合重叠）；修复后 RunningHub、JYD、Pu/LTX 三套全量分别为 882、1206、61 项通过。本条记录的是签名根初始化前状态；正式根和完整环境包的后续结果见本日期上方条目。

### 独立 Agent 授权与原执行回执（开发中）

- JYD 实际中央接口接入设备验证；Agent CLI/GUI 复用网站账号和本机原密钥，不重复激活。中央按原任务账号、真实范围及设备单槽位领取，不以共用密码替代设备授权。
- 加入持久启动/结果回执：领取、启动或完成响应丢失后复用原编号，完成回报失败不伪装渲染失败；相同结果幂等，迟到失败不能覆盖成功。原密钥可在撤销后回报旧结果，但不能领取新任务。
- 执行中崩溃或回执丢失保持待核实，不自动重做；旧状态恢复和模式切换不重排可能已执行的任务。启动前取消不渲染并释放分配。
- 跨端/实际 HTTP/运行合同 `66 passed`，网络/回执等相关专项 `80 passed`，补充 GUI 模拟后的模块专项 `18 passed`（集合重叠，不相加）。真实设备/GUI、人工核实恢复工具、其他入口、发布公钥与整包保护仍未完成；未打包、推送或部署。
- 本阶段扩大回归：网站完整测试集 `839 passed`（610.73 秒）；工作台选定的授权/Agent/队列/启动器/前端/H3 回归 `341 passed`（255.69 秒），不是工作台全量。报告为 `tests/.runtime/device-agent-server-complete.xml` 和 `device-agent-jyd-complete.xml`；后补 GUI 修改由上述 18 项模块测试覆盖，不将不同快照和重叠集合累加为整包验收。

### 设备授权开发文档 v1.13（仅文档）

- 完善主文档的当前状态、代码落点、Agent 单请求许可合同、剩余 HTTP/运行接线任务和验证边界；保留首次审批及正常覆盖更新免重复激活的硬要求。
- 核对已有组件合同 47 项和工作台相关回归 316 项报告，仅作为各自生成时的历史证据，不相加、不声称当前完整工作树或实际处理机链路已验收。
- 本次没有修改程序、重跑测试、生成真实密钥、打包、推送或部署。Agent 实际运行、其他入口、发布公钥/整包检查及真实双机/连续升级验收仍待完成，不可作为正式防转拷包分发。

### 命令行原设备授权接入（开发中）

- 正式处理机单任务入口、渲染脚本和 probe 建草稿操作接入网站账号与原设备授权；密码隐藏输入，自动化令牌仅显式标准输入，不保存凭据、不触发重新激活。
- 真正建草稿/导出核心继续检查签名策略、账号、设备和权限；读取诊断不变。独立 Agent 的共用接入密码尚未替换为合法持钥协议，仍列为待实现。
- 工作台相关回归 `346 passed`，两端合同 `9 passed`；均为隔离数据/模拟密钥和执行，不是实际 EXE 升级或跨机验收。本阶段没有新增服务端业务代码/迁移/配置，未打包、推送或部署。

### 受控软件初始化与稳定原密钥定位（开发中）

- 云端仅向管理员明确允许的账号签发 120 秒、独立用途的软件初始化许可，不创建设备批准、任务或已激活名额。OFF/OBSERVE 不隐含软件许可。
- JYD 新增固定机器级密钥定位及跨进程初始化锁；原 TPM/软件密钥按记录复用，故障、缺失、冲突和损坏不自动换钥。原密钥权限修复兼容稳定定位。
- 显式兼容申请经受限本机单向管道交给辅助进程独立验签，取得机器锁后再次检查过期；页面双确认，初始化完成仍待设备审批。普通更新/登录/刷新不申请软件许可。
- 管道/初始化专项 `86 passed`，软件真实代码跨端专项 `57 passed`，工作台相关回归 `291 passed`；含一次仅传输模拟 JSON 的原生 Windows 管道测试及浏览器模拟交互/桌面截图。无真实 CNG 密钥、UAC、注册表写入、付费调用或部署；其余入口、发布保护和实机验收仍待完成。
- 本轮网站完整工作树回归 `764 passed`、0 失败/错误/跳过（345.64 秒），报告 `tests/.runtime/device-software-server-full.xml`；不代表正式 EXE 或实机激活已验收。

### 工作台 TPM 初始化与原密钥访问修复（开发中）

- JYD 首次申请接入同一正式 EXE 的固定提权辅助入口；只在明确申请、密钥确实不存在时初始化。正常读取、刷新、更新不换身份、不提权。
- 增加原密钥访问修复页面和同源确认接口，按原处理机进程的 Windows 用户合并有限权限；不信任网页传入的 SID/命令，不替换密钥或云端批准关系。
- 取消、并发、超时及查询异常分开处理，尚在运行的辅助进程保留句柄，不自动重开。当前用户重新打开并签名验证后再联网校验原授权，不恢复业务队列。
- 模拟专项 `54 passed`、工作台相关回归 `200 passed`、两端合同 `57 passed`。浏览器模拟交互及请求验证通过，截图未获取；无真实提权、密钥创建、付费调用或部署。软件兼容、其他私有入口/Agent/CLI、发布公钥/包保护与实机验收仍待完成。

### H3 历史队列补授权与设备会话后台刷新（开发中）

- H3 恢复接口按原账号列出已确认但缺少有效设备关联的未提交分段；逐批查看、明确确认原计划费用后绑定到当前获准设备。包含后续依赖段，不重置已提交/结果未知/取消/失败任务，不直接调用供应商，不创建新生成任务或改变失败历史。
- 恢复使用状态摘要防止过期确认，控制事务锁和原请求键保证并发/重复确认只记一次。复用现有操作/审计表，无新增迁移；恢复后的 Worker 继续检查当前授权。
- JYD 授权页提供分页、逐段确认、响应丢失原键重试；状态变化要求重新查看，旧勾选不继承。新后台服务仅刷新已有账号设备会话，与前台共用时钟/租约，断网不延期、退出不删机器密钥，也不自动恢复业务任务。
- H3 阶段网站完整回归 `707 passed`（303.95 秒），工作台相关回归 `202 passed`；后台刷新专项/接入 `109 passed`，更新后两端合同 `52 passed`，均无失败。独立模拟浏览器验证通过，不代表真实 TPM/跨机/EXE 连续升级验收。
- 首次初始化、其他私有入口和独立 Agent/CLI、发布信任根/包隔离及实机验收仍待完成；没有生成真实密钥、付费生成、打包、推送、部署或开启生产强制模式。

## 2026-08-31

### 本地核心执行与授权等待恢复（开发中，暂不发包）

- 云端新增账号/nonce 绑定的签名本地策略接口，最多 300 秒，复用独立 ES256 签名密钥；不是设备许可，也不登记设备。工作台只能信任编译公钥，普通 JSON/环境变量不能关闭正式包检查。
- JYD 本地建草稿、内容替换、实际导出和内嵌队列增加真实执行检查；已启动单元安全收尾，下一单元重新检查。授权失效保持原任务等待，不改供应商失败/重试次数；数据库为权威来源，状态文件锁不导致任务丢失。
- 授权页面列出当前账号待恢复本地任务，只有明确点击才用有效本机授权继续原任务 ID；重新登录/刷新不重新激活，也不自动执行旧任务。
- 网站完整回归 `663 passed`（274.73 秒）；工作台相关广泛回归 `258 passed`，收尾专项 `61 passed`，两端真实客户端/本地策略合同 `36 passed`，均无失败，集合重叠不可相加。模拟页面确认恢复拒绝/成功及 390px 布局，无真实授权、渲染或付费调用。
- 独立 Agent 持钥协议仍待实现，目前受保护环境临时拒绝旧接入密码领取；其他私有入口、云端历史队列补授权、后台刷新、发布公钥/打包及真实两机/EXE 更新验收未完成。信任根为空，当前不要打包分发。未生成真实密钥、未启用生产强制模式、未推送或部署。

### H3 客户端持钥请求与设备授权页面（开发中，默认 OFF）

- JYD `AuthCenterClient` 接通本处理机、当前网站账号的设备凭据与 DPoP，仅对已接入的 H3 路径生效；正文移除旧令牌，保留业务幂等键。请求发出后不因设备拒绝而降级重发，设备证明不跟随下载重定向，不发送到 RunningHub 直达地址。
- 账号菜单新增设备授权页面，明确确认后才申请；批准后换取签名凭据，普通刷新/登录不换密钥。到期、撤销、TPM/权限异常、升级、账号登录和授权刷新分别提示；网络失败仅显示原有效短期状态，不重置宽限期。
- 工作台相关回归 `126 passed`（85.53 秒）；两端真实客户端合同和云端队列准入 `35 passed`（31.35 秒）。独立模拟页面的申请、拒绝、凭据失效、短期断网及窄屏浏览器检查通过，无真实密钥创建或付费调用；本轮未重跑网站全量。
- 本地执行、历史无关联队列补授权、其他私有入口、独立后台刷新、发布公钥与包扫描、真实 TPM/跨机/EXE 升级验收仍未完成。没有启用强制模式、打包、推送或部署，尚不是整包防拷贝完成版。

### H3 设备授权业务与排队准入（开发中，默认 OFF）

- H3 路由接入账号/设备绑定身份和持久 DPoP 防重放；新生成、付费重试、主动重生成检查当前授权，幂等回执、归属查询和原远程 ID 的结果重下仍可恢复。
- 新增 `0050_device_work_admission`，独立保存操作和分段关联（包括 soft_chain 后续段）；不改账号/任务父表。Worker 在领取、上传和付费提交前复核；授权暂停独立记录，不伪造供应商失败，不丢失已付费任务或结果未知的提交记录。
- 通用任务重试/重生成服务的 H3 分支补充新请求身份检查，防止旧入口借用原任务设备授权付费重试；LTX 等工作流业务规则未改。
- 初轮 H3 路由/Worker/费用预览回归 `46 passed`，新增准入与迁移专项 `43 passed`。补齐旧重试入口后完整工作树回归 `655 passed`、0 失败/错误/跳过（347.44 秒）；报告 `tests/.runtime/device-admission-full.xml`。这些模拟测试不替代真实 TPM、跨电脑复制及 EXE 升级验收。
- 本地业务、激活 UI、历史无关联队列补授权及整包保护仍待完成。未启用强制模式、未生成真实密钥、未推送或部署。

### H3 未提交费用预览的安全撤销与并发确认

- 快照增加 `quote_recovery` 能力及绑定令牌；新增仅限所属账号的 `/api/workbench/h3-batches/{batch_id}/quote/cancel`，需要明确确认和请求键。
- 确认与撤销共用数据库条件更新：只接受未确认、无生成任务且所有行/分段均未提交的批次；兼容 soft_chain 后续段在报价时已处于 `WAITING_DEPENDENCY` 的状态。竞争只有一方生效；重复确认返回原任务，撤销回执可幂等查回，不删除素材或调用供应商。
- 隔离模拟回归：预览生命周期、原 H3 接口、Worker、PNG 重试和直达下载合计 `62 passed`，不调用付费供应商。
- 配套 JYD 按稳定行 ID 和输入指纹恢复/确认旧预览，旧版无指纹预览需要明确撤销重算。先更新云端再发布工作台；无新迁移，未自动部署生产。

### 工作台设备授权客户端接入（开发中，尚未启用业务限制）

- JYD 新增固定机器密钥、ES256 信任根校验、DPoP 证明、账号隔离会话及短期凭据刷新；独立缓存不保存登录或云端访问令牌，缓存复制和时钟回拨不能扩展授权。
- 本地 `/api/new/device-authorization` 状态、申请与刷新接口已接入网站账号；申请/刷新要求同源及专用请求头，不能从网页注入设备 ID、私钥或软件批准。
- 工作台相关回归 `63 passed`，真实两端代码合同及原服务器授权/迁移回归 `29 passed`；使用临时模拟密钥，没有真实 TPM/跨电脑验收或付费调用。
- 客户端接入后网站完整工作树回归 `626 passed`、0 失败/错误/跳过；新增客户端模块编译及格式检查通过。
- 发布信任根当前为空；待完成激活 UI、UAC/ACL 实机验证、现有业务/Worker 准入和打包保护。未生成真实设备密钥、未启用生产拦截、未打包/推送/部署。

### 工作台设备授权基础（开发中，尚未启用业务限制）

- 新增独立设备身份、账号授权、额度、审批、原子换机、挑战/重放记录及审计表，迁移为
  `0049_workbench_devices`；增量建表，不重建账号或任务父表。
- 新增设备登记、状态、设备绑定凭据交换/刷新和网站管理员页面；软件保护需明确批准，
  版本号变化不重新登记，服务器签名密钥与现有应用登录密钥分离。
- 新依赖 `PyJWT[crypto]>=2.10.1,<3`。当前仍待接 Windows 持久密钥、业务入口及打包保护，
  尚不是可交付的防拷贝版本；未推送、打包、部署或修改生产数据。
- 本地使用隔离 PyJWT 2.10.1 依赖验证：新增授权/迁移测试 24 项，相关回归 `54 passed`，
  网站完整 mock 回归 `602 passed`；新增代码格式、Python 编译和差异检查通过。

### H3 工作台声音提交中断恢复与发布测试修复

- 新增只读声音回执查询 `/api/workbench/audio-batches/lookup`，按当前账号、来源和原请求键
  查回批次及输入摘要，不创建任务或调用供应商。查询无结果不作为重新付费提交的依据。
- JYD 在进程退出或回执丢失后校验原始输入并接回原任务，关联与音频切换同一事务提交；
  查不到时保留原素材、显示待核对并禁止盲目重提。先更新服务器，再更新工作台，无数据库迁移。
- 修复 3 项视频回归的执行账号测试夹具；测试默认配置隔离本机 H3 直达开关，专项直达测试仍显式开启。
  不放宽视频账号权限或修改实际 `.env`。网站全量回归 `578 passed`，包含两端真实序列化/恢复合同测试。
- 本轮不涉及 LTX，未调用真实付费生成、改动生产数据库、打包、推送或部署。

### H3 远端 PNG 素材缺失的人工重试保护

- 最近一次明确的 `LoadImage / FileNotFoundError` 通过同次上传回执或历史内容哈希精确定位
  丢失 PNG；只对临时副本增加不可见元数据，保留原图全部数据块、像素和冻结生成输入。
- 无法定位、PNG 完整性校验失败、上传文件名不合法或平台仍返回失效文件名时，在付费生成
  提交前停止。远端失败仍需人工重试，不改变旧 LTX MP4 恢复、下载重试和提交结果不明保护。
- 增加安全上传回执及失败历史绑定，并为 H3 人工重试增加数据库条件更新，防止重复点击
  覆盖已入队任务。不新增运行依赖或数据库迁移；未部署生产、未触发真实付费重试。
- 本次相关回归 `60 passed`（新增 29 项）；全量 `565 passed, 3 failed`，3 项非 H3 账号
  配置测试均在独立未修改 Git 基线复现，未纳入本次修改。Python/JS 语法及差异检查通过。

### 工作台声音生成与 RunningHub 视频配置解耦

- 工作台声音批次不再提前执行数字人视频预检，旧 RunningHub Key 为空、没有分配执行账号、
  执行账号停用、数字人工作流停用或 App ID 未填写均不阻止独立 MiniMax 声音生成。
- 保留 MiniMax 配置、音色归属/激活、语音参数及费用确认校验；重复提交继续幂等，声音完成后
  仍停在 `AWAITING_REVIEW`，可试听和下载，不自动创建视频任务。
- 视频默认参数继续兼容保存；原视频链路及旧网页完整流程的账号/工作流校验不变。
- 新增无 RunningHub 配置的声音提交、模拟 Worker 完成与下载，以及语音安全校验回归。
  声音、批次创建、视频创建与资源池 Worker 的独立测试库回归 `80 passed`。
  全量回归另行执行，但受 Windows 测试临时目录访问权限影响，收尾失败且未产出完整汇总，
  不计为全量通过；Alembic 当前版本、Python 编译、运行页 JS 语法及 diff 检查通过。
  本轮未调用真实付费接口，未修改业务账号数据、部署生产或新增数据库迁移。

## 2026-08-29

### H3 工作台视频直达交付（默认关闭）

- 新增 `H3_DIRECT_DELIVERY_ENABLED` 灰度开关；开启且片头裁剪关闭时，云端只保存 RunningHub
  HTTPS 下载地址和结果签名，不再保存成功分段 MP4。`soft_chain` 仅保留末帧连续性 PNG。
- H3 状态接口增加 `video_delivery`；新版 JYD 直接下载到本机分段缓存并计算本地 SHA-256，历史
  服务端文件继续兼容。旧 JYD 的 `/video` 调用由服务端无凭据流式转发，不重新落盘。
- 开关默认关闭且无需数据库迁移；安全升级顺序为数字人服务、JYD、最后灰度开关。本轮只实现
  工作台链路，旧数字人网页下载程序不在本次范围内，未部署生产环境。
- 数字人 H3 扩展回归 `110 passed`，JYD H3/页面回归 `73 passed`；另用既有 COS 结果完成一次
  1.30 MB、H.264 + AAC 的无付费直下探针，中文对象名编码和本地媒体完整性均通过。

### H3 生成前 RunningHub 余额硬保护

- H3 执行账号弹窗每次打开都实时读取 `accountStatus`，安全显示本次 RH 币余额和查询时间；余额
  为 0、未知、认证失败或临时读取失败的账号保留可见但不可勾选，也不会进入默认全选。
- Worker 在每个资源池任务上传和远程创建前再次读取余额；余额为 0 时不上传、不提交、不增加
  自动重试计数，释放当前预留并尝试本批冻结快照中的其他有余额账号。
- API 只增加已有安全余额摘要，不返回 API Key、凭据指纹、Base URL、工作流 ID或第三方完整响应。
- 余额/H3/Worker 服务端定向回归 `44 passed`，JYD 认证代理和可见弹窗回归 `51 passed`；扩展
  H3/资源池回归 `176 passed`，另有 2 项由当前未提交的声音批次账号前置校验改动触发的既有
  测试夹具失败。本轮未调用真实 RunningHub、未部署生产环境。

### H3 节点 248 默认改为六步采样

- RunningHub raw-workflow 节点 `248` 的 `steps` 从 4 调整为 6；冻结模板摘要、模板标识、
  模板版本和 H3 适配器同步升级，避免新任务复用旧四步任务结果。
- H3 Prompt 模板测试工具的界面、命令行和断点恢复默认值同步改为 6，仍可手动选择 4 或 8
  进行对比测试。
- H3 冻结模板、动态图、测试工具、页面、任务编排和后处理回归 `99 passed`；未部署生产环境，
  未发起真实 RunningHub 付费任务。

### 老数字人分段统一增加“重试此段”

- 老网页批次详情的每个终止态分段统一显示“重试此段”：失败、下载失败或取消的分段沿用原安全
  重试；已经成功但人工检查不满意的分段执行显式付费重生成。
- 成功分段不再受快速拼接预览状态限制。重试时只失效该段和旧拼接预览，同批其他成功分段保持
  不变，新结果完成后再按原顺序重新拼接。

## 2026-08-28

### H3 默认提示词画面一致性增强

- Ref2VA 的视频主导、图片主导和首尾同图分支统一增加“服装颜色保持不变”和“人物画面占比
  保持不变”两条独立约束。
- 默认 Prompt 版本升级为 `h3.prompt.ref2va.v9`，首尾同图版本升级为
  `h3.prompt.ref2va.loop_anchor.v2`；人工总体提示词仍使用独立的完整覆盖版本。
- H3 Prompt、费用冻结版本和管理页显示定向回归 `56 passed`，完整全 mock 回归 `488 passed`。

### JYD H3 逐片段人工检查与定向重试

- JYD 工作台原“变体”展示位改为“片段检查”入口；每条视频可查看当前 H3 批次的全部原始音画片段，
  由人工主观判断是否合格，不新增 OCR、自动字幕去除或自动质检流程。
- 检查弹窗桌面端每行约 5 个卡片，片段较多时自动向下换行并只做纵向滚动；窄屏依次降为
  4/3/2/1 列，不使用横向滑动或轮播。
- 每个片段只暴露一个“重试这段”动作：失败片段走原失败重试，已成功但主观不合格的片段走原
  主动重新生成；soft-chain 费用确认和下游连锁提示继续沿用既有保护。
- 新增登录与项目归属保护的本地 H3 原片段预览接口；H3 协调器、接口和新工作台定向回归
  `45 passed`。本轮未部署生产环境，也未发起真实 RunningHub 付费任务。

### 老网页 H3 人工总体提示词覆盖

- `/generate/batch` 的 H3 多参考区域新增批次级“人工总体提示词”输入；留空仍使用系统自动
  编译的六段式 Ref2VA Prompt，填写后每个自动分段原样使用人工文本。
- 人工覆盖启用时不再追加“整批补充方向”，页面明确提示系统不会自动补入分段台词、音频复用、
  人物一致性或首尾同图等默认约束，避免把完整覆盖误当作普通附加方向。
- 覆盖文本、`h3.prompt.manual-override.v1` 版本和 Prompt 摘要在费用预检时冻结，并贯穿确认、
  失败重试、人工重生成及 soft-chain 后段解锁；迁移 `0047_h3_prompt_override` 只增加可空列，
  历史批次继续使用默认编译路径。
- H3 Prompt、老网页、共享 prepare/confirm、Worker soft-chain 和迁移定向回归 `52 passed`，
  完整全 mock 回归 `488 passed`；未发起真实 RunningHub 付费任务。

## 2026-08-27

### H3 四步采样与画面一致性提示词

- RunningHub raw-workflow 节点 `248` 的 `steps` 从 8 调整为 4；工作流模板摘要、
  模板版本和 H3 适配器同步升级，防止新任务与旧 8 步输入摘要混用。
- Ref2VA 的视频主导、图片主导和首尾同图三个 Prompt 分支统一额外强调服装颜色与
  人物屏幕尺寸不变；版本升级为 `h3.prompt.ref2va.v9` 和
  `h3.prompt.ref2va.loop_anchor.v2`。
- H3 Prompt、动态图、准备/确认和管理页定向回归 `55 passed`；本轮未部署，未发起
  真实 RunningHub 付费任务。

### H3 分段片头裁切暂时禁用

- 根据真实成片复核，H3 每段开头的自适应 ASR 裁切暂时默认禁用；新生成分段保留完整 H3 原生
  音画，不再删除首字前内容，也不再在 ASR 失败时固定删除 300ms。
- 原有 FunASR 首字定位、40ms preroll、300ms fallback 及音视频同步裁切实现完整保留在
  `H3_HEAD_TRIM_ENABLED` 开关后，后续确认爆音严重时可重新启用，不需要重写算法。
- 远程媒体模式关闭该开关时不会创建 `h3_head_trim` 任务，本地模式也不会调用 ASR；生成前用于
  安全分段的 `h3_audio_alignment` 不受影响。
- 输出合同升级为 `h3.output.generated-av-head-trim-disabled.v4` 并继续参与内容摘要，防止新任务
  复用已经按 v3 裁切的旧结果。本轮未打包、未部署，也未发起真实 RunningHub 任务。

## 2026-08-26

### H3 生成前对齐与片头 ASR 接入统一远程媒体任务队列

- 修复生产远程媒体模式下，H3 页面上传音频的安全切段和 H3 成片后处理仍直接访问服务器
  `127.0.0.1:18084`、没有进入固定电脑任务列表的问题；新增统一持久化 `h3_remote_asr_jobs`
  保留已发布的 `0045_h3_remote_head_trim_jobs`，并由追加迁移 `0046_h3_remote_asr_jobs`
  安全转换旧任务数据。
- H3 页面计算费用前先创建 `h3_audio_alignment` 任务并轮询；节点回传结果带音频哈希、原稿哈希和
  暂存素材 ID 绑定，全部对齐成功后才创建 H3 分段费用快照，不会提前产生 RunningHub 付费任务。
- 媒体节点 Worker v4 新增 `h3_audio_alignment` 与 `h3_head_trim` 能力；后者按原有令牌和租约协议领取原始 H3 MP4，提取音轨、
  调用 FunASR 并回传经约束的首词裁剪决定；租约过期可重领，回传不能直接控制任意 ffmpeg 时间。
- 视频 Worker 在等待远程 ASR 时保留 RunningHub taskId 和已下载原片，收到结果后恢复同步裁剪与
  soft-chain；远程 ASR 明确失败仍安全降级 300ms，不自动重提付费 H3。
- H3 页面、后处理、远程 API、媒体节点和恢复链定向回归 `35 passed`，全新 SQLite 从 `0001` 升级到
  `0045` 成功；本轮未部署生产，也未发起真实 RunningHub 付费任务。

### RunningHub 账号、工作流和网站用户配置分层

- 新增系统级工作流配置页。数字人、视频对口型和 MiniMax 多参考三个工作流分别维护自己现有的
  Workflow ID / AI App ID、访问密码、实例、默认提示词和启用状态，所有 RunningHub Key 共用。
- RunningHub 执行账号页只维护 API Key、最大并发、启停、健康状态和余额；工作流配置不再按
  执行账号重复保存。
- 网站用户管理页负责 MiniMax 语音账号、豆包内容分析和可使用的 RunningHub 执行账号；H3
  产品权限仍单独控制。普通用户只会使用明确分配给自己的账号。
- 新增迁移 `0044_system_workflow_configs`，从历史配置初始化三条工作流；旧账号级字段和数据仅作
  兼容保留，运行时优先读取系统工作流配置。H3 初始化为 Plus 48G。

## 2026-08-25

### JYD H3 本机安全切点与逐行人物图

- JYD 正式 H3 入口可提交绑定冻结原稿、完整 MiniMax 音频和声音生成版本的
  `jyd.h3-safe-cut-alignment.v1`；服务端校验后直接规划 4～15 秒分段，不再要求生产服务器
  连接其自身不存在的 `127.0.0.1:18084` FunASR。
- 过长 MiniMax raw cue 的安全切点仍必须在费用确认前建立；绑定摘要、声音批次或版本不一致时
  明确失败，未提交新合同的旧入口继续保留兼容回退。
- H3 人物参考图支持按 JYD 稳定行快照提交；每行可沿用顺序循环或连续复用后的实际人物图，
  不再强制整批所有行共用同一组图片。

## 2026-08-23

### H3 v5 十行真实批次与付费重试硬保护

- 修复 Windows 后台 Worker 在系统 `%TEMP%` 清理 `runninghub-upload-*` 时被拒绝访问的问题；
  RunningHub 上传临时区改为应用自己的 `data/runtime/runninghub-uploads`，避免素材上传完成前被
  本地 `SUBMIT_FAILED` 截断。
- H3 远端失败改为终止并等待人工检查，不再继承数字人通用的三次自动付费重试；安全的本地
  提交前失败仍可在确认没有远端 taskId 后恢复。
- 使用 PromptProfile v5、raw 适配器 v3 完成徐博士 10 行真实测试：0～5 张人物图、15 个直接
  任务加 2 个 soft-chain 依赖任务，共 17 个独立 RunningHub taskId，17/17 成功。
- 第 7、10 行验证了首段成功后抽取 Picture 6 尾帧、再解锁后段的严格依赖链；没有第 18 次调用，
  没有 SeedVR2，也没有生产部署。
- H3 Worker 定向回归增至 `9 passed`；17 个本地标准化结果均经 ffprobe 验证同时包含视频流和
  音频流。

### H3 PromptProfile v5 与固定第六尾帧槽

- Ref2VA Prompt 按官方最少充分 ontology 重建为人物、环境、视频整体结构和完整音频四个跟踪
  单元；人物图片只提供脸部身份，人物稳定外观与抽象表演语言来自参考视频，Video 标签不再
  与人物/环境 Subject 重复承担可见内容。
- Retention 固定为人物/环境 `fully_preserved`、视频结构 `partially_preserved`、完整音频
  `fully_copy`；声音层明确禁止添加环境声、音效、其他声音和音乐。
- 用户图片继续紧密使用第 1～5 槽；soft-chain 尾帧固定使用 `<Picture 6>` 和
  `ref_images.ref_image_5`，不再随人物图数量移动。raw 适配器升级为 v3。
- Prompt/动态图/Worker 定向回归 `40 passed`；全部第三方调用为 mock。

### H3 历史 MiniMax 音频可复用

- `h3-audio-sources` 同时返回音频/raw cues 完整的待审核音频和已有审核记录的 `SUCCESS` 历史
  音频；已用于 H3 的 1–6 等历史测试音频不再从选择列表永久消失。
- 已审核音频可绑定新的 H3 批次；重复确认仅产生本次明确确认的 H3 调用，不重新合成语音，且不
  改写原审核时间、来源行状态或 MiniMax 音频版本。
- H3 定向全 mock 回归 `30 passed`；未调用真实 MiniMax/RunningHub，未部署生产或修改云端数据。

本文件记录每次推送到仓库后的主要功能变化、兼容性说明和验证结果。

## 2026-08-24

### H3 参考视频自动动作片段池

- 每行参考视频在无费用 prepare 阶段按固定 3 秒边界切成无音轨 MP4，最后不足 3 秒的尾段继续
  保留；工作台上传接口和页面不增加片段预览、手动选择或处理状态。
- 新增 `balanced_bag_v1` 确定性均衡分配，存在多个动作片段时避免相邻重复。每个 H3 分段冻结
  实际动作片段索引、路径和 SHA-256，输入复用、重试、重生成及 soft-chain 都不会重新排列。
- 新增迁移 `0043_h3_motion_reference_pool`，历史分段字段为空时回退到原始参考视频；本轮只运行
  本地自动化测试，没有发起真实 RunningHub 付费任务。

### H3 随机片头杂音自适应裁剪

- H3 单段后处理复用现有 FunASR 与冻结分段原稿的 token 映射，连续确认开头最多 3 个原稿字词
  后，以首字时间向前保留 40ms，音视频同步裁剪并共同归零。
- ASR 不可用、无时间戳、整体对齐失败或开头匹配不足时不再阻断视频任务，统一降级为固定裁剪
  300ms；降级模式、原因和匹配审计写入任务输出元数据。
- 输出合同升级为 `h3.output.generated-av-head-trim.v3` 并进入内容摘要，避免新任务复用 v2 的完整
  未裁剪结果；H3 音轨仍为权威音频，不回铺 MiniMax 输入音轨，soft-chain 使用裁剪后末帧。
- 本轮只修改本地代码、合同和测试，没有部署生产，也没有发起 RunningHub 真实付费任务。

## 2026-08-22

### H3 原生音画与 JYD v2 交接修复

- 修正 H3 后处理的媒体语义：节点 138 的 MiniMax 音频是 Ref2VA 输入条件，节点 387 已包含节点
  365 视频与节点 364 H3 解码音频组成的同步成片；不再丢弃 H3 音轨或把输入音频铺回输出。
- 云端输出合同升级为 `h3.output.generated-av.v2`，完整保留 H3 音画和输出时长，soft-chain 从完整
  结果末帧抽取 anchor；合同版本进入内容摘要，旧静音结果不会被新任务误复用。
- H3 PromptProfile 升级为 `h3.prompt.ref2va.v2`；soft-chain 对上一段尾帧的定义改为“完整 H3
  成片末帧”，不再描述已经取消的按输入音频时长裁尾。
- A/B 检查点可以直接从已下载的节点 387 原始 MP4 做零费用重建。现有两段 fast 原始结果已生成
  10.333333 秒修复版母版，视频轨和 H3 音频轨起点均为 0；本轮没有新增 RunningHub 调用。
- 本地 H3 工作台使用两遍 stream-copy 完成多段合并与分别归零，JYD v2 从同一母版拆出静音
  `base_video` 和 H3 权威音频。字幕按 H3 实际分段窗口约束，再由 FunASR 对齐冻结原稿。

### H3 fast 真实基线与累计调用安全恢复

- 用户把 H3 真实提交累计上限由 3 次提高到 4 次。A/B 脚本现在可在不改变素材、Seed 和 Prompt
  合同的前提下迁移旧检查点，并为首次无保存输出的第 1 段追加可审计替代调用；上限从检查点
  读取，不再写死为 3。
- 替代第 1 段和第 2 段 fast 重试成功，分别消耗 165/133 RH 币；第 2 段首次提交返回运行 0 秒、
  无可见费用的 `1000 Unknown error`，仍按一次真实提交计数。累计达到 4 后，脚本在 soft-chain
  创建远端任务前强制停止。
- 旧版曾生成 1376×768、24fps、8.750998 秒的 `fast_join.mp4`，当时丢弃 H3 音轨并铺回输入
  音频；该做法后来被证实会导致口型错位，文件只作为反例保留。完成 soft-chain 对照仍需一次
  明确新增授权。
- RunningHub 临时素材只在 20 小时安全窗口内复用；过期的参考视频、音频或 soft-chain 尾帧会
  保留上传历史并重新上传。累计额度不足时在尾帧上传之前停止，不产生无效外部 I/O。
- 授权迁移、零费用失败重试、相邻额度恢复、A/B 同源音频成片、上传 TTL 和 raw-workflow
  定向回归 `51 passed`；未部署生产或修改生产数据库。

### H3 独立工作台报价合同与适配器能力声明

- H3 账号摘要新增不含节点 ID、workflowId 和密钥的公开 `adapter_capability`；本地工作台据此
  渲染用户人物图上限、衔接模式、画面比例与 MP 范围，为后续替换工作流适配器保留稳定接口。
- 待确认报价的素材、逐行输入、默认参数或账号选择变化后，本地会作废旧批次引用并要求重新
  计算；付费确认后锁定费用相关输入，防止正在执行或已完成的远端结果失联。
- 本地页面新增账号隔离的待审核 MiniMax 音频选择、临时试听、台词/版本自动冻结，以及逐行
  补充方向、衔接、比例和清晰度覆盖；横屏枚举统一为已用于真实探针的 `16:9 (Widescreen)`。
- 图片/视频/试听音频的本地中转副本响应后立即清理；JYD 导入改为单事务并校验 handoff 摘要、
  raw cues 与冻结脚本，并发重复导入幂等、中途失败整体回滚。本地工作台 `16 passed`、云端
  H3/适配器/账号池/迁移定向回归 `67 passed`、JYD H3 交接与项目 API `19 passed`，浏览器
  假环境冒烟无控制台错误；本轮未新增真实付费调用。

### LTX 对口型分段硬上限进一步收紧为 20 秒

- 生产任务连续出现 `OOM_KILLED` 后，新旧 LTX 入口统一把单段硬上限由 30 秒收紧为 20 秒；
  正好 20 秒仍可作为单段，超过 20 秒的完整流程按 MiniMax/ASR 时间轴切分。
- 新 LTX 工作台优先在约 18 秒寻找句间切点并确保不超过 20 秒；旧网页单条入口超过 20 秒时
  明确要求改用可切分的完整流程。源视频不做分辨率、帧率或编码标准化。
- 数字人仍保持目标约 30 秒、口播硬上限 32.8 秒和 2 秒临时静音尾，不受本次修改影响。

### H3 私有工作流访问密码与 raw-workflow 输出兼容

- H3 执行账号能力新增可选工作流访问密码；迁移 `0040_h3_access_password` 使用可空文本列
  保存 Fernet 密文，空库升级、既有能力保留和降级均已验证。
- 浏览器安全摘要只返回是否已配置密码，绝不返回密文或明文；Worker 仅在 H3 raw-workflow
  提交前解密并注入顶层 `accessPassword`，任务输入、Prompt、检查点和普通日志均不保存密码。
- RunningHub 资源池管理页现在可为同一真实执行账号独立维护 H3 Workflow ID、实例类型、
  H3 并发、安全备注和启停状态；密码支持新增、更换、留空保留与显式清除，页面只显示配置状态。
- H3 动态图强制节点 387 保存输出；高级任务成功但 v2 文件列表为空时，客户端读取高级输出
  接口并归一化 `fileUrl/fileType`，输出尚未就绪时保持原任务查询，绝不重复付费提交。
- 账号池、密码、客户端与 H3 Worker 定向回归 `48 passed`；真实 A/B 首单发现官方导出图
  `save_output=false`，该单远端成功但
  无可下载文件，后续付费调用在修正发布图和追加授权前保持停止。

### LTX 对口型远端视频素材丢失自动换哈希重传

- RunningHub 已接收上传但执行容器找不到 `openapi/*.mp4` 时，LTX 下一次重试会先对本地
  视频生成临时无损重封装副本，再上传为不同内容哈希，避免普通重传持续命中同一个失效对象。
- 重封装使用视频流直接复制，不重新编码、不修改帧率或时长；原视频和持久化分段不覆盖。
  仅最新一次失败明确包含远端 MP4 `No such file or directory` 时启用，OOM 和其他错误不触发。
- 定向回归 `20 passed`、完整全 mock 回归 `424 passed`；真实 FFmpeg 冒烟确认 H.264、
  30fps 和 1 秒时长保持一致，同时输入输出 SHA-256 不同。未调用真实 RunningHub，未部署
  生产，未修改生产数据库。

## 2026-08-20

### 数字人分段改为 32.8 秒口播 + 2 秒临时静音收尾

- 数字人口播分段硬上限由 35 秒收紧为 32.8 秒；提交 RunningHub 前仅对临时上传副本追加
  2 秒静音，因此最长输入为 34.8 秒，仍低于模型 35 秒上限并保留 0.2 秒容器误差余量。
- 原始音频、审核音频和 raw cues 均不追加静音。新工作台合并视频时裁掉中间片段的静音尾，
  只在最后一个片段保留模型生成的收尾画面；老工作台继续交付独立片段，不新增正式自动拼接。
- 云端 manifest 记录口播时长和静音尾时长，本地 JYD 据此重建裁剪后的主时间线，使字幕、
  语义贴图和片段接缝继续使用同一套时间坐标。该次修改仅完成本地代码和测试，尚未部署生产。

### 老网站受控多机位图片 + 单音频批量入口

- 新增独立 `/generate/multi-camera` 页面和 `multi_camera_web_v1` 批次来源；多条上传音频可
  快速绑定一个或多个 `1～N` 有序图片组，支持统一应用、连续范围应用和按组每 N 条自动分配。
- 每条音频独立按自然停顿做全局均衡切分，单段硬上限 20 秒；不足 20 秒保持单段并只用机位 1。
  两机位严格交替，三机位按 `123、132、213、231、321、312` 展开，四机位以上按字典序惰性
  展开全排列。切分、图片哈希和机位选择在批次创建时冻结，失败重试不重新排列。
- RunningHub 合同和视频 Worker 不变：每个子任务仍是一张图片加一段音频的标准
  `digital_human` 任务。多机位批次不自动拼接、不进入旧批次列表，也不接入剪映工作台；页面
  支持状态轮询、单段重试/下载和带 `manifest.json` 的整批有序 ZIP。
- 新增迁移 `0037_multi_camera_web`。本地和服务器分别只把精确 `admin`、`Cx_ceshi` 映射为
  `multi_camera_user_access` 中的环境本地用户 ID；运行时只按 ID 授权，其他管理员仍返回 403。
  完整全 mock 回归 `380 passed`，格式化后的功能/权限/旧批量隔离定向回归 `40 passed`。
  未调用真实 RunningHub，未部署生产，也未修改生产账号数据库。

### 独立 LTX 工作台逐段接入 SeedVR2，并复用 JYD 正式服务器配置

- 仅对 `source_channel=ltx_workbench` 的独立 LTX 工作台任务固定启用 SeedVR2 `plus/48G`；
  每个 LTX 分段成功后先保存源片段，再创建一对一清晰化阶段，清晰结果完成后才将子任务标记
  `SUCCESS` 并参与基础视频合并。旧 LTX 网页不自动放大，老数字人工作台原有开关不变。
- SeedVR2 状态、远程 ID、usage、失败原因、原始 LTX 片段和清晰片段分别持久化并返回工作台。
  清晰化失败的人工重试复用已保存的 LTX 源片段，不清空或重新提交已经成功的 LTX 任务。
- 独立工作台正式入口复用 JYD `processor_config.json` 的 `digital_human_server_url`，缺省连接
  `https://video.lanyingjk01.com`；新增双击启动入口。本机 `127.0.0.1:8000` 只由
  `start_test.ps1` 或显式环境变量启用。
- 完整云端 mock 回归 `372 passed`；独立工作台 `9 passed`。
  未执行真实 RunningHub 调用，未部署生产，未修改生产账号数据库。

## 2026-08-19

### LTX 对口型分段硬上限收紧为 30 秒

- 新旧 LTX 入口统一把单段硬上限从 45 秒收紧为 30 秒；正好 30 秒仍作为单段，超过 30 秒的
  批量上传、长音频和独立工作台任务进入 ASR/时间轴切分。旧单条页面不能自动切分，因此会
  明确提示改用完整流程。
- 修正标点估算切分在文本估计点略超上限时过早跳过上限内真实静音点的问题，避免产生不必要的
  过短分段。数字人 35 秒上限、OOM 不自动重提和视频规格不自动标准化规则保持不变。
- 该次提交只修改本地代码、页面文案、测试和文档，当时没有接入 SeedVR2；此后已由
  2026-08-20 的独立工作台决策覆盖。没有提交真实 RunningHub 任务、没有部署生产或修改生产数据库。
  当次完整 mock 回归 `371 passed`。

### 上传音频 LTX 独立工作台云端合同

- 新增迁移 `0036_ltx_workbench_preparation`、`LtxPreparationJob` 和独立
  `app/routes/workbench_ltx.py`。第二套工作台每行只提交表格原稿、源视频和自定义音频；
  提示词固定由云端编译为 `一个人用中文说：“{分段原稿}”`，接口拒绝自定义提示词。
- 所有新工作台音频统一经过 FunASR 并与表格原稿对齐，45% 以下停止；持久化
  `ltx.aligned-script.v1` 原稿时间轴、脚本/音频摘要、分段原稿范围和匹配率。短任务也经过
  ASR，但未切分时保留上传的视频和音频文件，不额外转码。
- 创建前按同一行完整素材校验 `视频时长 + 0.05 秒 >= 音频时长`；不是按单个切片，也不是
  累加整个项目所有行音频。旧网页短 LTX 入口同步补齐相同校验。
- 新增准备取消/重试、分段取消、再次确认费用的幂等分段重试、原片段/基础视频下载和完整
  RunningHub 失败返回。新工作台 LTX 遇到 `OOM_KILLED` 直接保留终态失败，不自动重复付费。
- 全量本地 mock 回归 `370 passed`，另有独立工作台、老剪映精确字幕合同、串行渲染桥和
  账号隔离及变体任务相关回归 `7 passed`；没有调用真实 RunningHub、没有部署生产、没有修改生产账号数据库。

## 2026-08-15

### 统一视觉锚点隔离与非法单项降级

- 内容分析 provider 升级为 v5、Prompt 升级为 v18；视觉锚点改用独立 `VA` 前缀，避免模型
  把字幕 `B` 边界误作视觉选择。旧工作台提交的 `B` 视觉锚点仍会在服务端规范化兼容。
- Provider 返回未提供、重复或越权的单个视觉项时只丢弃该项，保留同次响应里的合法视觉项，
  不再让一个错误编号导致整条视觉分支失败。音乐、字幕和标题契约不变。
- 内容分析契约、服务及跨项目定向回归 `58 passed`；未调用真实 Ark、MiniMax 或 RunningHub，
  未部署生产。

## 2026-08-14

### 新版工作台末段表情收尾

- `new_workbench` 4A 只在整个任务最后一个 RunningHub 分段的整秒 `end_time` 上增加 1 秒；
  中间分段不增加，单段任务即为最后段。原 MiniMax 音频不补静音、不重新上传。
- 合并标准化和后处理清单同步保留新增的 1 秒画面，避免 4B 或变体按较短音频时长裁掉收尾。
- 云端相关全 mock 回归 `37 passed`；未调用真实 RunningHub/MiniMax，未部署生产或修改生产数据。

### 生产完整备份轮换与旧网页 SeedVR2 默认关闭

- 完整数据备份在新归档成功创建后自动轮换，默认保留最近 2 份；保留数可通过
  `RUNNINGHUB_BACKUP_KEEP_COUNT` 调整。轮换仅匹配标准完整归档，不触碰代码包、独立数据库
  快照或配置备份，避免生产磁盘因每日约十余 GB 的完整归档持续增长。
- 旧网页单条、批量和长音频数字人入口的 SeedVR2 48G 开关改为默认关闭，用户仍可按次开启；
  选择继续冻结到新任务，不迁移已排队、已提交或历史任务。新版工作台 4A 继续始终开启 SeedVR2。
- 完整 mock 回归 `345 passed`，并通过本机 Bash 语法检查及真实文件名轮换测试。本次只修改本地
  源码、部署资产、测试和文档，未删除线上备份、未部署生产、未修改生产数据库。

## 2026-08-13

### RunningHub 账号池 RH 币显示

- 扩展现有 `accountStatus` 容量查询，以 `Decimal` 同时解析并缓存 `remainCoins`、钱包余额、
  币种、Key 类型和远端任务数；相同真实 Key 按不可逆指纹共享一份缓存。
- 数字人池与 SeedVR2 池管理员页面显示剩余 RH 币并支持单账号手动刷新；新版工作台 4A 费用
  确认显示每个候选账号的 RH 币和缓存状态。所有接口仍只返回安全摘要，不返回 Key 或请求体。
- 新增迁移 `0034_runninghub_credential_balance`。云端完整 mock 回归 `336 passed`；工作台
  RH 币定向回归 `36 passed`，全仓回归因现有大规模语义素材子测试超过 15 分钟而终止，未出现
  RH 币相关失败。未调用真实 RunningHub、未修改生产数据库或部署。

### 新版工作台 4A 在 MiniMax 账号切换后的安全恢复

- 修复已生成、已落盘且已由用户批准的语音，在 4A 本地切分交接前因 MiniMax 当前账号发生
  变化而被错误标记为 `CONFIGURATION_ERROR` 的问题。已有音频的交接不再创建 MiniMax 客户端，
  不重新合成语音，也不产生新的 MiniMax 费用。
- 受控恢复入口只接受显式任务 ID，并只恢复同时具备本地音频、本地 4A 图片、当前版本
  `APPROVED`、尚未创建视频子任务且错误原因精确匹配该历史缺陷的任务；服务重启不会自行
  扩大恢复范围，缺少任一证据的失败任务保持原状。
- 工作台画面状态接口优先报告真实音频失败阶段，不再因残留 `reviewed_at` 把失败任务显示为
  `COMPOSITION_QUEUED`；本地工作台同步按 `failure_stage=audio` 回到声音重试入口。
- 定向全 mock 回归：云端音频/工作台接口 `11 passed`，本地工作台 4A 协调 `8 passed`；未调用
  真实 MiniMax 或 RunningHub。

## 2026-08-12

### 旧网页 SeedVR2 当次开关与数字人 24G/48G 管理员配置

- 旧网页单条、批量和长音频数字人入口新增默认开启的 SeedVR2 48G 放大开关；选择在任务
  创建时冻结，关闭后数字人源视频下载成功即完成，不创建或提交 SeedVR2，下一次重新默认开启。
- 管理员编辑用户页开放数字人 `default`（24G）与 `plus`（48G）实例配置。云端忽略单条
  页面伪造的数字人实例值，并把管理员当前配置冻结到新任务；已排队、已提交和历史任务不迁移。
- 新版工作台 4A 继续始终执行 SeedVR2；LTX 不受影响；SeedVR2 自身始终固定 `plus`（48G）。
- 新增迁移 `0033_generation_task_seedvr2_switch`，只直接增加带历史默认值的任务布尔列，不重建
  被增强表引用的 `generation_tasks`。定向兼容回归 `121 passed`、云端完整全 mock 回归
  `329 passed`；未调用真实服务或部署。

### 新版工作台 RunningHub 数字人池与 SeedVR2 独立双池

- 新增默认关闭的 `RUNNINGHUB_DUAL_POOL_ENABLED`、不可变用户 ID授权和批次执行模式快照；
  开关关闭继续 `same_account_v1`，开启且授权后才进入 `dual_pool_v1`。迁移为既有
  `Cx_ceshi` 记录受控测试授权，但不按用户名做运行时权限判断。
- 新增独立 SeedVR2 执行账号、成员授权、加密 Key、跨旧配置/两池指纹判重、管理员页面和
  工作台安全摘要。4A 分别选择数字人池和 SeedVR2 池，两组每次默认全选并冻结内部 ID。
- 双池 Worker 在数字人源片段落盘后释放数字人槽位，再独立原子预留 SeedVR2 账号。普通
  SeedVR2 失败留在原账号；只有确认凭据/权限/App/工作流不可用才在下一次尝试换号。
  远端 ID、取消、下载、恢复和看门狗始终使用原尝试账号，模糊提交继续保守占用且禁止重提。
- 云端完整 mock 回归 `322 passed`。工作台本需求和其余 389 项通过；完整套件另有 2 项
  语义素材库旧固定断言因用户当前 102 概念、多概念素材变化失败，本轮按边界未修改。
- 未调用真实 RunningHub/MiniMax，未部署、未修改生产数据库、未构建 `D:\JYD-new`、未推送。
- 4A 费用确认现明确显示“一控多/双池”；任务启动后云端按分段返回实际执行账号安全摘要，
  工作台持久化并显示。一控多标记同账号完成数字人与放大，双池分别显示两个阶段账号，未分配
  阶段不做推测；任何响应均不包含 RunningHub 凭据或工作流配置。

## 2026-08-11

### 数字人分段限制为最长 35 秒

- 根据生产任务审计，39.75 秒数字人源片段在 SeedVR2 48G 首次执行约 61 分钟后返回
  `805`，同一时长区间也出现长时间运行；因此数字人分段继续以约 30 秒为目标，但硬上限
  从 45 秒收紧为 35 秒，避免把接近 40 秒的完整片段直接送入清晰化。
- MiniMax 官方句级时间戳、上传长音频静音切分、旧单条上传和批量自动分流统一遵守数字人
  35 秒上限；LTX 对口型仍保持原来的 45 秒上限，不受本次策略调整影响。
- 已增加 39.75 秒时间戳回归和静音切分回归。本次只修改本地源码、测试和文档，未取消
  RunningHub 任务、未部署生产、未修改生产数据库、未推送 Git。

### 新版工作台管理员多 RunningHub 账号资源池

- 新增执行账号、管理员成员关系、批次账号快照和远程尝试绑定；相同 API Key 通过不可逆指纹
  判重，Key 继续只在云端加密保存。普通用户、旧网页、`legacy_web` 和 LTX 保持原逻辑。
- 新版工作台管理员可在每次 4A 费用确认中重新默认全选并按本次取消账号；云端重新校验
  权限、启用状态和工作流配置，前端只提交内部账号 ID。
- Worker 按真实凭据原子预留最多 5 条完整“数字人 -> SeedVR2”流水线。每个分段两阶段严格
  使用同一账号；查询、取消、下载、恢复和看门狗按逐次尝试绑定原账号。
- 数字人明确失败可在不可变快照内创建新账号尝试；SeedVR2 失败只用成功数字人账号和源片段
  重试。提交结果不明确时保留账号容量并禁止跨账号盲目重提。
- 工作台 4A 改为逐行持久化、最多 4 个本地后台启动线程和稳定行级幂等键；近百行请求快速
  返回，单行失败不阻断其余行，进程重启后由下一次已登录状态轮询继续未交接行。
- 云端全量 mock 回归 `298 passed`；工作台本需求相关回归全部通过，100 行后台启动专项通过。
  工作台全量另有 4 个与本需求无关的现有免责声明/用户语义素材断言失败，本轮未改动它们。
- 本轮只修改源码、迁移安全链路、测试和文档；未调用真实 RunningHub/MiniMax，未部署、
  未修改生产数据库、未打包 `D:\JYD-new`、未推送 Git。

### RunningHub 手动取消后的分阶段重新生成

- 工作台画面任务在 RunningHub 被手动取消后可再次点击“生成视频”，取消不再被误判为
  “没有可重试的失败阶段”。
- 若取消发生在数字人阶段，清除已关闭的 RunningHub 任务 ID，并按保存的图片、音频及工作台
  当前分辨率创建一个新的数字人任务；若取消发生在 SeedVR2 阶段，只复用已完成的数字人 MP4
  创建新的 SeedVR2 48G 任务，不重复数字人费用。
- 本地工作台把当前分辨率随重试请求提交；云端只在数字人阶段取消时采用该值，SeedVR2 阶段
  取消不改变数字人输入快照。云端拒绝重试时，本地操作立即回滚为失败，不再留下假的排队状态。
- 本次只修改源码、文档和自动化测试，未调用真实 RunningHub、未部署生产环境、未修改生产数据库。

## 2026-08-10

### 数字人分段统一接入 SeedVR2 48G 清晰化

- 旧网页单条/批量/长音频分段和新版工作台 4A 的每个数字人片段，在数字人成功后都会
  一对一进入 SeedVR2；6 个分段正常对应 6 次数字人和 6 次放大，LTX 不受影响。
- SeedVR2 固定 AI App `2064116518987845634`、`plus`（48G）、模型选项 `1` 和最大分辨率
  `1920`，浏览器及工作台不能覆盖，也不存在 24G 回退分支。
- 新增迁移 `0029_seedvr2_video_enhancement`，分别持久化源片段、清晰片段、SeedVR2 远端
  ID、执行账号和不可变尝试；历史已完成任务不自动补跑。
- 页面、合并和工作台默认消费清晰片段，数字人源片段继续保留并提供受权限保护的下载。
  工作台增加 `VIDEO_ENHANCING` 活动状态和 `seedvr2_upscaled` 素材元数据。
- 数字人与 SeedVR2 分阶段重试、取消和看门狗；清晰化失败不重做数字人，下载失败不重提
  SeedVR2，提交结果不明确时不自动重复付费。
- 完整 mock 回归：数字人网站 `288 passed`，本地工作台 `369 passed`。经用户授权的真实
  5 秒付费冒烟通过：数字人 576×1024、SeedVR2 1080×1920，最终输出节点 `97/mp4`，两阶段
  分别消耗 130/97 RH 币；未修改生产账号数据库。生产发布状态以安全发布脚本现场版本和
  健康检查为准。

## 2026-08-09

### 数字人生成统一使用 Plus 48G

- 旧数字人网页、新工作台 4A 画面生成、批量生成和长音频拆分任务统一固定使用
  RunningHub `plus`（48G）实例，不再以 `default`（24G）创建新的数字人任务。
- 已经进入本地队列、配方中仍保存 `default` 的历史数字人任务，在最终提交 RunningHub 时
  自动升级为 `plus`，无需修改生产数据库或重新创建任务。
- LTX 对口型仍保留独立的实例规格选择，不受数字人 48G 固定规则影响。
- 本次只修改源码、配置示例、文档和自动化测试，未调用真实 RunningHub，未部署生产环境，
  未修改云端账号数据库。

## 2026-08-08

### 新工作台换图后重新生成数字人画面

- 4A 画面请求现在同时绑定项目图片 SHA-256。相同图片的重复 HTTP 请求继续复用原任务；
  图片内容变化时只清除旧图片对应的 RunningHub 分段任务和合并结果，保留已审核的 MiniMax
  音频、字幕时间戳与声音费用结果，再为新图片创建画面任务。
- 云端任务清单返回当前绑定的图片指纹，本地工作台在下载基础视频前强制核对；旧服务器或
  异常响应不能再被当作新图片的生成结果。
- 新增迁移 `0027_audio_primary_sha256`，只为声音任务增加可空图片指纹列，不重建既有表。
- 同期修复工作台语义字幕把 `0.5到1公斤` 错拆成 `0.` / `5到1公斤` 后删除小数点的问题；
  数字范围在解释模型断点前会被识别为不可拆表达式。

### 数字人图片上传上限提高到 200 MB

- 旧单条页、旧批量页和工作台 4A 共用的数字人图片上传上限由 20 MB 提高到 200 MB；
  JPG、PNG、WEBP 格式校验及用户归属规则保持不变。
- 音频 100 MB、LTX 视频 500 MB 和批量暂存总量 5 GB 的既有限制不变；Nginx 继续使用
  550 MB 请求体上限。
- 已部署实例若在 `.env` 中显式保存了旧值，发布时还需把
  `MAX_IMAGE_SIZE_MB` 同步调整为 `200` 后重启 Web 服务。

## 2026-08-07

### 相同 MiniMax API Key 共享克隆音色

- 不同网站账号配置完全相同的 MiniMax API Key 时，会按不可逆凭据指纹共享已保存的克隆
  和融合音色；旧网页声音中心、旧批量生成页和新版工作台均能看到各自的本地音色副本。
- 克隆保存和首次正式激活会同步到同 Key 用户；同步的是同一个 MiniMax provider
  `voice_id`，不会再次克隆。每个网站用户仍保留独立素材 ID，删除只隐藏自己的副本，
  不影响其他用户。
- 新增数据迁移 `0024_shared_minimax_voices`，只为部署前已存在的同 Key 用户补齐自定义音色
  副本，不重建现有 SQLite 表、不改账号绑定约束，也不删除历史任务或音色。
- API Key 按“同一 MiniMax 账号轮换”更新时，本账号音色指纹随新 Key 更新；显式切换为新的
  MiniMax 账号时继续使用新的稳定绑定，旧账号音色不会混入。

### RunningHub 远程任务 4 小时安全看门狗

- 删除提交后满一小时就由本站抢先写入 `TASK_TIMEOUT` 的行为；RunningHub 的正常平台
  排队、执行和运行上限继续以其 V2 查询状态为准。
- 新增默认 4 小时的远程异常滞留看门狗。到期时先查询最新状态；仍为活动任务才调用
  RunningHub 官方取消接口，取消确认成功后以 `REMOTE_WATCHDOG_TIMEOUT` 关闭本地任务。
- 自动取消失败时保留远程任务 ID、并发槽位和活动状态，后续轮询继续查询并重试取消，
  不会直接失败、释放槽位或自动重新提交。旧的 `RUNNINGHUB_TASK_TIMEOUT_SECONDS` 停止
  读取，新配置为 `RUNNINGHUB_REMOTE_WATCHDOG_SECONDS=14400`。
- 本次不新增数据库迁移；新增边界测试与完整 mock 回归共 `239 passed`。未调用真实
  RunningHub/MiniMax，未部署生产环境或修改云端账号数据。

### 新版工作台日志第二阶段（实施顺序 4）

- 新版工作台增加当前项目的简洁运行记录，按操作显示步骤、行号、状态、更新时间、短关联号
  和错误码，不向普通用户展示技术日志正文或错误堆栈。
- 增加仅限项目所有者下载的项目级脱敏诊断包；包内仅包含安全项目摘要和与项目编号、操作号
  或关联号匹配的本机日志，排除脚本、素材路径、请求负载、凭据和其他项目日志。
- 云端管理员运维页增加“全部/旧网页/新工作台”来源筛选，并同时作用于首次加载、历史查询
  和实时追加。旧网页任务记录页面保持不变，原始日志下载继续导出完整服务日志。
- 本轮只修改源码、文档和自动化测试，未更新 `D:\JYD-new`、未打包、未部署生产环境或修改
  云端账号数据；长期结构化事件表仍属于实施顺序 5，未在本轮实现。完整 mock 回归为
  数字人网站 `233 passed`、工作台 `274 passed`。

### 新版工作台日志第一阶段

- 工作台 Processor、渲染队列、Collector 与独立 Render Agent 增加持久化轮转日志，默认
  单文件 10 MB、保留 14 天，并在落盘前统一隐藏常见令牌、API Key 和访问密码。
- 工作台项目操作新增独立 `correlation_id`，声音批次创建和 4A 画面生成会把同一关联号
  传给数字人服务；幂等键继续只负责防重复提交。
- 云端迁移新增 `0023_batch_correlation_id`。Web、语音、媒体和视频 Worker 事件现在可携带
  `source_channel`、批次、任务与关联号；旧网页和新版工作台的任务记录隔离规则不变。
- 本轮只修改源码和自动化测试，未更新 `D:\JYD-new`、未打包、未部署生产环境或修改云端
  账号数据。完整 mock 回归为数字人网站 `232 passed`、工作台 `272 passed`；当时管理员
  来源筛选、诊断包和长期事件表仍留待后续阶段。

### 新版工作台多片段叠化

- 新版 4A 多片段基础视频不再直接硬切；每个接缝使用 0.25 秒短叠化，并通过补上一段
  尾帧保持总时长不变。音频、MiniMax 字幕绝对时间轴和后续片段起点不提前。
- 旧网页快速拼接预览、单片段标准化、RunningHub 原始分段和付费任务流程保持不变。
- 工作台普通成片和变体导出会直接在两个原始 MP4 片段间写入同样的剪映原生叠化，
  不再插入尾帧图片片段，避免浏览器预览已有转场而最终下载又退回硬切。
- 完整 mock 回归：数字人网站 `232 passed`，工作台 `270 passed`；另用真实 FFmpeg
  两段纯色视频验证输出总时长保持 `5.000000` 秒且接缝中点为混合帧。未调用真实
  RunningHub/MiniMax、未启动剪映导出、未部署生产环境或修改云端账号数据库。

## 2026-08-06

### 旧版批量生成任务名称

- `/generate/batch` 的“任务名称”从高级设置移到生成方式区域，创建前始终可见、可修改。
- 上传音频默认使用首个音频文件名（去掉扩展名）作为任务名称；文案生成没有现成音频
  ID 时使用首个任务编号或画面文件名，多条任务自动附带数量。用户手动修改后不会被
  上传、排序或清单刷新覆盖。
- 任务记录列表将原“批次”表头改为“任务名称”，底部仍保留批次 UUID 便于精确排障。

### 内容分析真实 Ark 单次组合输出与字幕断点策略修复

- 真实 `doubao-seed-2-0-lite-260428` 调试快照显示，长脚本请求可能返回合法但被错误提升到
  顶层的裸 `music_intent`，同时遗漏 `schema_version` 与 `subtitle_units`。服务端现在把
  这种精确形态记录为 `SUBTITLE_MISSING`，保留合法音乐分支，不再笼统归为两分支版本错误。
- 继续坚持一次 Ark 请求同时分析音乐和字幕，不增加自动二次付费调用。Ark 内部响应改为
  `jyd.content-analysis.provider.v2`：模型只返回 `prefer_after`/`allow_after` 断点编号，不再
  重复返回拆分后的字幕文案、字符范围或标点；服务端按未修改的原脚本本地切片，再转换为
  工作台兼容的公开 `jyd.content-analysis.v1 subtitle_units`。
- 标点和空白断点由服务端本地确定；模型断点只是超宽时的优先/次选机会，不是强制切行。
  工作台仍先尝试保留整句，只有真实字体宽度放不下时才按 `prefer`、`allow` 顺序断行。
- Prompt 版本升级为 `jyd.content-analysis.prompt.v5`，避免继续命中旧策略缓存。系统 Prompt
  按角色、目标、输入、输出、业务规则、few-shot 六段组织；字段、类型、枚举及额外字段限制
  继续交给 strict JSON Schema，user message 删除重复的 schema_version、必需键和完成规则，
  只携带两份实际输入文本。模型仍只能选择请求中提供的 `B` 编号；数字、ASCII 单词及标点
  相邻位置不会作为可选断点提供。13 个全角中文字符等效宽度明确为上限而非目标长度；
  只为超宽片段返回最少量断点，并增加错误/正确对照，要求保留完整词语且不留下孤立助词。
- 730 字脚本的最大输出预算从 5840 提高到当前服务端安全上限 8192 token；短脚本仍至少
  保留 4096 token。该值是生成上限，实际计量仍取决于模型真实输出。
- 新增裸音乐真实响应形态、单次调用、长脚本预算、断点编号、标点本地处理和原文切片测试。
  内容分析定向回归 `48 passed`，数字人项目完整 mock 回归 `230 passed`。未发起真实 Ark
  请求；真实服务复验需重启 Web 后由用户显式执行一次分析。

### 内容分析模块 4：统一接口、分支校验、缓存与并发 10

- 新增工作台令牌保护的 `/api/workbench/content-analysis`，一次 Ark 请求返回音乐意图与
  字幕语义单元；两条分支分别校验、分别保存状态，部分成功不会降为完整失败。
- 新增迁移 `0022_content_analysis_cache`，按用户、脚本哈希、模型、契约和 Prompt 版本
  缓存合法结果；完整失败不粘滞，强制刷新失败不覆盖已有成功分支。
- 大模型字幕索引仅在单元文字按原顺序精确重建原脚本时确定性重算，不修正文案、空白、
  标点或语义属性。模型仍不能返回时间戳。
- 服务端 Ark 调用默认统一并发上限为 10，超出请求排队；429、超时、连接错误和明确 5xx
  继续有限退避。普通日志不记录 Key、完整脚本或豆包完整原始响应。
- 模块 4 定向测试 `11 passed`，模块 1+3+4 联合定向测试 `45 passed`，迁移测试
  `3 passed`，数字人项目完整 mock 回归 `213 passed`。
- 自动化测试全部使用 mock，未调用真实豆包、MiniMax、RunningHub 或剪映，未部署生产
  环境或修改云端账号数据库。

### 内容分析模块 3：豆包 Ark 配置与独立客户端

- 新增用户级豆包 Ark 配置和迁移 `0021_ark_configs`，管理员可在现有用户页面保存启用
  状态、加密 API Key、Base URL、模型、超时和有限重试参数；密钥不回显、不下发工作台。
- 新增可注入、可 mock 的 Ark OpenAI 兼容 HTTP 客户端，只封装传输和安全结构化异常；
  超时、429、明确 5xx 可有限重试，异常不包含 API Key、完整请求、完整响应或用户脚本。
- 本模块不包含内容分析 Prompt、业务接口、缓存、音乐/字幕解析或工作台接入，不调用真实
  豆包、MiniMax、RunningHub 或剪映，不部署生产环境或修改云端账号数据库。
- Ark 配置与客户端定向测试 `14 passed`，数字人项目完整 mock 回归 `202 passed`；
  Python 编译检查和 Git diff 格式检查通过。

### 内容分析契约模块 1

- 新增服务商无关的 `jyd.content-analysis.v1`，固定 `music_intent` 与
  `subtitle_units` 严格 JSON 结构；本期不包含前景图片关键词、字幕时间戳或本地音乐身份。
- 新增受控音乐标签体系、`music-matcher.v1` 100 分权重和自动选择前硬过滤条件，供后续
  46 首运行时标签库与本地 Top1 匹配器共同使用。
- 字幕字符位置统一采用 Python Unicode code point 左闭右开索引，并校验原文完整覆盖；
  漏字、改字、重复、乱序、越界、重叠和错误绑定均会拒绝。
- 增加 3 个 JSON 夹具和 20 项契约测试；定向结果为 `20 passed`，数字人项目完整回归为
  `188 passed`。本模块没有新增数据库、接口或第三方调用，未产生费用、未部署生产环境、
  未修改云端账号数据库。

## 2026-08-05

### 新旧批次来源与算法隔离

- 新增迁移 `0020_batch_source_channel`。所有迁移前的批次明确标记为 `legacy_web`，
  新工作台声音批次创建时标记为 `new_workbench`，不再根据名称或请求编号猜测来源。
- 旧网页 `/batches` 只展示 `legacy_web` 批次，新工作台批次不会混入原网页历史列表。
- 单片段按音频时长标准化、以及多片段逐段补帧/裁切严格限制到 `new_workbench`；旧网页
  单片段继续直接使用 RunningHub 结果，旧网页多片段继续执行原快速顺序拼接算法。
- 隔离定向回归 `35 passed`，数字人完整回归 `168 passed`。未调用真实
  RunningHub/MiniMax，未部署生产环境，未修改云端账号数据库。

### 新版工作台精确时间轴时长

- 新版工作台 4A 使用 MiniMax 时间戳时不再为 RunningHub 音频追加 `0.5` 秒静音尾垫；
  小数时长仍向上取整到完整秒，例如 `24.4` 秒使用 `25` 秒。
- 旧版上传音频和旧批量任务继续执行原尾垫规则，没有修改历史算法。
- 完整自动化回归：数字人 `163 passed`、剪映工作台 `217 passed`；未调用真实
  RunningHub/MiniMax、未部署生产环境、未修改云端账号数据库。

## 2026-08-04

### 新版工作台字幕与 BGM 模块 4B

- 音频完成后、4A 启动前仍允许工作台替换图片；4A 绑定最后一次选择的当前图片。
- 4B 由工作台现有本地剪映队列完成单行字幕、可选 BGM 和最终成片，本项目没有新增
  第三套后端、账号、RunningHub 任务或变体任务。
- 4B 失败只重试未完成的本地剪映行，不重新触发成功的付费 RunningHub 任务。
- 完整自动化测试：数字人 `162 passed`，工作台 `207 passed`；未调用真实付费接口，
  未部署生产环境，未修改云端账号数据库。

### 新版工作台画面模块 4A

- 新增工作台专用的画面启动、状态查询、失败阶段重试和基础视频下载接口；继续复用现有
  数字人账号、审核门、媒体 Worker、RunningHub Worker 和拼接 Worker。
- 修正模块边界：声音批次只提交脚本、音色和语音参数，图片延迟到用户启动 4A 时上传并
  绑定；内部幂等键固定为 64 字符以内，不再因项目编号与音色编号拼接过长返回 400。
- 新增迁移 `0019_deferred_audio_primary`，允许工作台声音任务在 4A 前不绑定图片；旧的
  完整流程任务仍可在创建声音任务时立即带入画面素材。
- 单片段与多片段都按已确认音频时长归一化；多片段自动按顺序拼接，明显短于音频超过
  1 秒时明确失败。原始 RunningHub 分段不删除。
- 4A 产物只定义为基础视频，不添加字幕/BGM，不调用剪映，也不自动生成变体。
- 数字人网站完整自动化测试：`162 passed`；工作台完整自动化测试：`205 passed`。
- 自动化测试全部使用 mock，没有产生真实 RunningHub/MiniMax 费用、部署生产环境或
  修改云端账号数据库。

### 新版工作台声音模块

- 新增工作台专用声音接口，复用当前数字人账号的 MiniMax 配置、三个产品官方音色、
  已保存自定义音色和既有克隆/融合制作队列。
- 官方音色首次试听要求显式费用确认并在成功后缓存；声音制作继续执行现有格式、时长、
  账号绑定、付费保护、试听后保存和失败恢复规则。
- 自定义音色保存与激活拆分：保存只生成未激活音色卡；用户二次确认激活时才执行第一次
  正式 TTS、触发 MiniMax 首次使用计费并开放给核心工作台。自定义音色卡新增确认删除，
  历史任务和历史音频继续保留。
- 新增工作台音频批次创建、状态查询、音频下载和单行重试接口。批次强制开启语音审核，
  MiniMax 成功后停在 `AWAITING_REVIEW`，不会自动创建 RunningHub 任务。
- 工作台专用接口继续执行令牌用户归属校验；幂等请求返回原批次，重新生成只处理指定
  行并保留历史音频版本。
- 更新新版后续产品决策：多片段自动拼接默认作为基础视频，接缝短暂闪烁可接受；原始
  片段、自动拼接版本和用户上传粗剪版本都保留，基础视频生成后不得自动创建变体。

### 验证

- 数字人网站完整自动化测试：`161 passed`。
- 剪映工作台完整自动化测试：`203 passed`（另有 54 条依赖库 deprecation warnings）。
- 自动化测试全部 mock MiniMax/RunningHub，没有发起真实付费任务、部署服务器或修改
  云端账号数据库。

## 2026-08-03

### 新增

- 后处理按输入来源和实际语音时长明确分流：只有 MiniMax 文本语音且最终未切分的
  单段视频进入自动 BGM/字幕分支；上传音频或多分段视频统一进入人工处理分支。
- 多分段全部成功后仍按顺序生成快速拼接预览，但它不再被当作正式成片；原始分段
  始终保留，画面接缝必须人工粗剪。
- 新增逐行后处理清单接口，输出有序视频、自动/人工模式和 MiniMax 原始字幕时间戳，
  供本地剪映工作台消费；手工改动时间轴后会标明原时间戳不再有效。
- 新增迁移 `0018_segment_video_merge`，持久化成片审核开关、逐行合并状态、完整视频
  路径、合并错误和审核时间。
- 工作台账号改为复用数字人网站账号，并新增带用户归属校验的任务列表、任务详情、
  原始片段下载和短期令牌接口；本地工作台连接本地数字人网站，不影响云端现有账号。
- 后处理清单增加面向操作人的中文页面，批次详情显示当前阶段与最终处理方式，并修复
  全部子任务取消后状态未正确归为 `CANCELLED` 的问题。

### 文档

- 新增统一的 `DEVELOPER_GUIDE.md`，集中说明当前系统边界、四进程架构、批量长音频
  自动分流、ASR/脚本对齐、状态机与重试语义、本地开发、数据库迁移、自动化测试、
  Windows 媒体节点完整包/小更新包、生产发布、排障、安全和工作流扩展方式。
- 根 README 新增文档导航；`PROJECT_STATUS.md` 明确改为阶段快照和历史交接，并修正
  已过期的“未接入 ASR”、固定分支和部署版本描述。架构或发布方式变化以后以开发者
  指南为主，用户可见变化继续记录在本文件，专项操作仍在部署与媒体节点文档维护。
- 新增文档一致性回归检查，防止开发者入口、当前迁移头、远程媒体模式、自动重试和
  FunASR 说明在后续修改中意外丢失或重新互相矛盾。
- 新增 `数字人网站与剪映工作台集成说明.md`，集中记录本轮产品分流、账号边界、接口、
  工作台行为、本地验收步骤和未部署事项。

### 验证

- 数字人网站完整自动化测试：`156 passed`（另有 1 条既有 warning）。
- 剪映工作台完整自动化测试：`183 passed`（另有 30 条既有 deprecation warnings）。

## 2026-07-31

### 新增

- 新增 Windows 独立媒体节点便携包构建器，可将已验证的 Python 环境、依赖、FFmpeg、
  节点程序和可选 ASR 模型缓存打成 ZIP；便携节点默认使用电脑名作为 Worker ID，
  支持多台电脑安全竞争领取不同媒体任务。

- 批量直接上传现在逐行自动识别长短音频：45 秒以内照常创建单个任务，超过 45 秒
  自动进入长音频处理，并在原批次行下生成多个可见子任务；图片或视频自动复用。
- 批量页新增“长音频拆分后先试听确认”开关。默认关闭时全自动继续，开启后只让
  实际超过 45 秒的行暂停试听，短音频行不受影响。
- 长音频入口同时支持视频对口型和数字人图生视频。对口型使用 ASR 时间戳对齐
  原脚本并同步切音频/视频；数字人不调用 ASR，只在自然停顿切音频并为所有子任务
  复用同一张图片和动作提示词。
- 长音频新增“分段后人工试听确认”开关，默认关闭并自动完成分析、切割和子任务创建；
  开启后才停在试听页供用户调整切点。
- 单任务与批次详情新增“取消生成”。尚未提交的本地任务立即取消；已提交的任务先
  调用 RunningHub 官方取消接口，只有远端确认成功后才写入本地取消状态。
- 数字人和 LTX 对口型批量创建支持画面素材一对多：同一图片或视频可通过“复用”、
  “补齐/填满”对应多条音频或脚本；任务行统一使用暂存素材 ID 绑定，同名文件不再
  产生歧义，复用大文件无需再次上传。
- RunningHub 失败响应中的 `failedReason` 会完整保存并通过任务 API 返回；任务页
  直接显示异常类型、失败节点、节点 ID 和具体说明，无需再到 RunningHub 查询。
- RunningHub 已明确返回 `FAILED` 的视频任务默认自动重试 3 次，按 60、120、240 秒
  递增等待；每次失败的远程任务 ID 和完整原因都会进入尝试历史，三次耗尽后仍保留
  人工重试入口。网络提交结果不明确时不会自动重提，避免生成重复付费任务。
- 长音频媒体处理新增 Windows pull Worker：笔记本或台式机通过 HTTPS 主动领取
  ASR 分析和音视频切割任务，不要求电脑具备公网 IP，也不开放本机入站端口。
- 新增远程任务租约、每分钟续租、断线自动回队、严格素材下载权限、分段 ZIP 清单
  与时长复核，以及节点名称、耗时、进程树内存和整机内存指标。
- ASR、FFmpeg 切割 Worker、独立环境、配置、安装脚本和统一启动入口现集中在
  `media_node/`；旧的独立“启动 ASR”与根目录媒体节点脚本已移除。
- 新增数据库迁移 `0013_remote_media_worker`、
  `0014_runninghub_failure_details`、`0015_runninghub_auto_retry` 和
  `0016_long_audio_workflows`、`0017_batch_long_audio`。

### 调整

- Windows 媒体节点发行包改为短名称和扁平 ZIP，避免资源管理器默认解压目录与包内
  目录重叠后触发路径过长；新增几 MB 级代码更新包和“一键更新”，固定的 Python、
  ASR 模型与 FFmpeg 无需每次重新分发，更新时保留 Token、配置和工作数据。
- 修复便携包构建验证可能错误复用 Windows 用户级 Python 包的问题；现在源环境和
  打包结果都会在禁用用户包的隔离模式下验证，包括 `mutagen` 在内的依赖缺失会在
  压缩前直接阻止构建。
- 修复批次进入语音审核、审核通过后开始切分、以及生成分段子任务时，操作列和子任务表
  必须手动刷新才出现的问题；批次页现在检测结构变化并自动局部更新，不会整页跳转。
- 素材尚在上传、确认没有创建 RunningHub 远程任务时发生的网络或上传响应错误，现已
  接入 3 次退避自动重试；提交请求结果不明确时仍禁止盲目重提，避免重复生成和扣费。
- RunningHub 账号状态、素材上传、任务提交/查询和结果下载发生网络异常时，
  管理员日志现在会记录脱敏后的异常类型、底层原因、目标域名、耗时、HTTP 状态，
  上传失败还会记录素材名称、槽位和 MB 大小；超过官方建议的 30MB 时只在后台给出
  明确风险提示，不阻止上传或改变现有工作流。这些信息会进入最近 7 天日志下载包。
- 修复 Windows PowerShell 5.1 将 Git 输出中的中文路径按旧代码页解码，导致部署预检
  把已批准删除的旧中文启动脚本误判为未知删除文件的问题。
- 修复长音频等待人工试听时取消批次后，操作列仍错误显示“正在自动处理”的问题；
  现在明确显示该行已在本地取消且尚未提交生成。
- 修复人工试听确认后页面局部更新，但“重新分析”按钮仍残留并可提交无效请求的问题。
- 双人数字人暂时从用户入口关闭，后端同步拒绝双人参数；底层工作流映射保留供后续
  低优先级开发。
- 长音频独立入口从主导航移除，试听确认仍通过批次详情进入；用户页面不再展示
  ASR、对齐器、处理节点和内存等实现细节。
- 视频对口型网页默认选择 Plus 运行（48G），仍可按需手动切换 Stand 24G。
- 修复 Windows FFmpeg/ffprobe 输出包含 UTF-8 字符时按系统 GBK 解码导致媒体任务
  实际处理完成却报 `UnicodeDecodeError` 的问题。
- 修复长音频页人工确认复选框受全局表单样式影响而与标签错位的问题。
- 修复远程媒体模式切换脚本在 systemd 重启后立刻执行单次健康检查，可能因 Web
  尚未开始监听而误判失败并回滚的问题；现在会逐秒等待，最多 20 秒。
- 修复远程模式 `.env` 备份误把项目备份目录改为 `root:root`，导致以 `rhvideo`
  运行的正式备份无法写入的问题；部署脚本现在会在备份前自动恢复目录属主和权限。
- 批次详情醒目标注所属网站账号，并为每一行直接显示当前 RunningHub 任务 ID 和
  实时失败原因；重试提示明确说明新一轮生成会获得新的 RunningHub 任务 ID，避免
  将不同账号下同名 TASK-001～004 或重试前后任务误认为映射错乱。
- 批次历史列表新增“删除记录”；终态批次以及没有活动视频/语音 Worker 的本地卡住
  批次均可手动删除，并清理从分段路径恢复出的上传目录。仍在排队或远程运行的任务
  禁止删除，避免留下失控的 RunningHub 计费任务。
- 生产更新脚本不再安装、启用、启动或健康检查服务器 FunASR。生产服务器只运行
  Web、语音 Worker、媒体调度 Worker 和视频 Worker 四个服务。
- 本地媒体 Worker 与远程媒体节点模式互斥，避免同一长音频任务被两处重复领取。
- 远程切割仍为单任务串行 FFmpeg，回传结果经服务器验证后才创建 RunningHub
  子任务。

### 验证

- 完整自动化测试：`142 passed`。

## 2026-07-29

### 新增

- 新增 Windows 生产安全更新脚本：默认只执行本地测试和服务器只读检查，只有显式
  使用 `-Deploy` 并通过两次 commit 确认才会备份、上传和更新；固定限制为本项目
  目录与三个 systemd 服务，并在发布前后验证任务队列和现有关键监听端口。
- 管理员账号列表新增安全删除：禁止删除当前登录账号；账号存在活动中的视频、语音或
  音色制作任务时拒绝删除；成功后只清理该账号 ID 对应的任务、素材、结果和音色目录。
- 未保存的声音克隆/融合试听新增到期清理。默认生成成功 48 小时后进入每日清理，
  最迟约 72 小时移除本地试听和参考音频，并在制作记录中标记为已过期。
- 批次详情支持将当前批次内已经成功生成的视频一次性打包下载；视频使用 ZIP
  原样归档，避免对 MP4 重复压缩占用 CPU。
- 管理员运行状态页支持直接下载最近 7 天的 Web、语音 Worker、视频 Worker 和
  本地总控日志；下载接口继续执行管理员权限校验，不会导出其他目录文件。
- 管理员用户编辑页新增 MiniMax 官方系统音色同步，实时验证当前账号可用音色并按
  站内用户、MiniMax 配置和稳定账号绑定隔离。
- 官方音色与克隆/融合音色分区展示：自定义音色始终优先，官方音色整体折叠并按
  语言分类；批量生成下拉框同步使用分组。管理员可从单个站内账号删除官方音色，
  后续同步不会自动恢复，同时保留显式恢复入口。
- 管理员运行状态页新增整机 CPU、内存、项目磁盘、项目进程内存、FFmpeg 数量和
  队列监控，并支持按账号、任务、服务和错误等级查询最近日志。
- Web 服务启动时接入与 Worker 相同的脱敏文件日志，生产后台可同时查看 Web、语音
  和视频事件。

### 调整

- 修复 MiniMax 官方音色目录个别 `voice_id` 带首尾空白时，整批同步被误判为
  “账号中找不到官方音色”并返回 502 的问题；同步失败现在会写入脱敏管理员日志。
- `0011_system_voice_categories` 支持从 SQLite 已新增字段、但 Alembic 版本尚未登记的
  中断状态安全续跑，避免一键启动因重复添加 `category` 字段持续失败。
- 生产环境运行状态页不再展示仅供 Windows 本地开发使用的“本地总控”，只监控
  systemd 实际管理的 Web、语音 Worker 和视频 Worker。
- 声音制作记录改为稳定的两列响应式布局，原生音频控件和保存按钮在宽屏、窄屏下
  均不会越出卡片边界。
- 完整音频结束时间改为向上取整到整秒，避免小数秒尾音被截断；例如 `28.1` 秒
  使用 `0:29`，整数秒保持不变。
- 修复数字人单人模式错误覆盖 AI App 左/右人物音频默认值的问题：单人模式不再提交
  `739` / `738` 节点，只有双人模式才提交实际上传的左右人物音频。
- 声音来源默认改为上传音频。数字人上传音频模板移除口播脚本；视频对口型统一
  只填写一次口播脚本，后台自动将它用于语音生成和/或视频正向提示词。
- 语音生成表格的两列统一命名为“任务编号、口播脚本”；旧版“脚本编号、脚本内容”
  仍可继续导入。
- 直接上传音频的 Excel/CSV 也精简为两列：数字人使用“任务编号、提示词”，视频
  对口型使用“任务编号、口播脚本”。素材不再要求填写文件名，改为按图片、视频及
  各类音频分组后的页面序号与表格行号对应；旧文件名模板继续兼容。
- 任务记录统一为“批次任务 / 单次任务”双向切换；从批次进入视频任务详情后，
  返回按钮会回到所属批次，不再跳入单次任务列表。
- 普通用户导航缩减为“生成视频、任务记录、声音”；快速创建与 Excel/CSV 表格导入
  保持并列一级入口，分辨率、实例和语音技术参数折叠到高级设置。
- 声音页优先展示可用音色，克隆和融合移入高级音色制作；管理员配置页按服务分组折叠。
- 日志默认保留从 14 天改为 7 天，并增加单文件 10 MB 上限；保留时间与单文件大小
  可分别通过环境变量配置。
- 部署模板统一为生产实际使用的 `/opt/runninghub-video`、`rhvideo`、
  `127.0.0.1:18083` 和 `runninghub-video-*` 服务名，音频 Worker 的 CPU、I/O、
  Nice 和内存限制纳入仓库。

## 2026-07-28

### 新增

- 完整流程支持导入 Excel/CSV 脚本清单，固定为“脚本编号、脚本内容”两列；图片或
  源视频继续按页面最终顺序与脚本行配对，并在创建前要求用户确认顺序。
- 每行脚本只调用一次 MiniMax 生成完整音频，再按约 30 秒、最长 45 秒切成可见子任务。
  数字人分段复用图片；LTX 按实际音频区间顺序切割源视频并为每段创建独立 RunningHub
  任务。第一版不自动拼接结果。
- 新增批次父子任务展示、分段音频下载、单段失败重试，以及 LTX 源视频不足时保留
  已生成音频、替换视频后继续切分的恢复入口。
- MiniMax 配置新增稳定的官方账号绑定标识。相同官方账号轮换 API Key 时继续复用
  已保存音色；管理员明确切换官方账号时隔离旧音色，同时保留凭证指纹用于审计。
- 直接上传音频的单条时长统一限制为 45 秒；完整流程由系统自动切分。
- 修复 Windows 一键启动器使用 `os.kill(pid, 0)` 导致误判后台服务未运行的问题；
  重复启动会打开现有页面，停止脚本也能正确通知启动器关闭全部子进程。
- 新增 MiniMax 官方系统音色查询与本地导入能力；系统音色明确标记为无 ¥9.9 克隆费，
  但仍提示文本合成用量。中国区默认 API 域名修正为 `https://api.minimaxi.com`。
- 新增迁移 `0007_full_script_pipeline` 和完整流程测试，完整 mock 测试为 73 项。
- 完整流程改用 MiniMax 异步长文本语音合成：远端 `task_id/file_id` 先持久化，
  Worker 可在重启后继续轮询；完成后立即下载音频与官方句级字幕，并按真实时间戳
  规划约 30 秒、最长 45 秒的子任务，不再用字符数估算脚本和音频的对应位置。
- 新增迁移 `0008_minimax_async_timestamps`，保存异步任务编号、本地字幕时间轴与提交
  时间；自动化测试覆盖结果包解析、轮询恢复、时间戳切段和 45 秒硬上限。
- 兼容 MiniMax 异步结果包真实的 TAR、`.titles`、`time_begin/time_end` 毫秒时间戳格式；
  已下载的失败结果包可在重试时直接复用，不会重新生成语音。
- 批量页新增“预览全部文案”，集中核对每行完整脚本与最终视频提示词；高级清单中的
  长文本改为多行编辑，避免单行输入框截断内容。
- 完整流程新增批次级 `pronunciation_dict.tone` 读音标注，直接接受 MiniMax 官方
  JSON 数组格式；Excel/CSV 脚本清单仍固定为“脚本编号、脚本内容”两列。
- 新增迁移 `0009_pronunciation_dict`，按语音任务保存读音规则，确保 Worker 延迟执行或
  重启恢复后仍使用创建批次时的设置。
- 完整流程新增可选的“语音生成后先审核”：默认仍自动进入视频队列；启用后先试听
  每行完整音频，可单条通过、重新生成或整批通过。重新生成会再次调用 MiniMax，并
  保留历史音频版本供对比。
- 新增迁移 `0010_audio_review`，持久化审核开关、当前生成版本、审核时间和历史语音版本。
- 管理员运行状态页新增事件实时流：每 2 秒按日志字节游标增量追加系统启动/停止、
  Worker 领取、第三方提交与状态返回、结果下载、成功、失败和自动重启事件，不整页
  刷新，也不显示成功的健康检查和轮询请求。
- 新增 `MAINTENANCE.md`，记录模块职责、两条任务数据流、状态与费用边界、SQLite
  迁移红线、注释规范和每次修改的最低验证清单；核心队列、批次计划和前端草稿状态
  补充变量含义与生命周期注释。
- 完成第一轮结构化维护：批次状态聚合、语音审核、批次重试/删除分别迁移到
  `batch_status.py`、`audio_review.py` 和 `batch_lifecycle.py`；声音克隆、融合、
  试听和保存迁移到 `speech/voice_jobs.py`，路由与 Worker 只保留任务编排。
- 新增 `.editorconfig`、统一 pytest 配置和架构边界测试，避免高风险业务规则重新
  回流到巨型路由或 Worker；新增声音制作任务的克隆试听及保存回归测试。

### 修复

- 修复分段音频下载因缺少安全路径函数导入而返回 500 的问题。
- Windows 调用 FFmpeg/FFprobe 时统一隐藏子进程窗口，生成和切分过程中不再反复弹出终端。
- 批量页移除面向开发过程的“完整流程”说明，并增大脚本与提示词输入区域。
- 批次详情改为后台轮询并就地更新状态，不再每 5 秒整页刷新；分段默认折叠，
  脚本和提示词使用摘要展示。运行日志页使用增量事件更新，保留阅读位置。
- 管理员日志页面隐藏成功的批次轮询和健康检查访问记录，错误与业务日志继续显示；
  `data/logs/` 中的原始日志保持完整。
- 修复批次详情中分段时间、RunningHub 状态、远程任务 ID 和操作按钮相互挤压的问题；
  长编号独立换行，状态使用中文标签，子任务表格保持可读的固定列宽。
- 修复 `0010_audio_review` 在 SQLite 上重建批次父表时触发级联删除的问题，改用原生
  `ADD COLUMN`，并新增带既有批次数据的迁移回归测试。本地受影响的 2 个批次已从
  数据库残留页、现存视频任务和分段音频恢复为 3 条父任务、7 条分段及其任务关联。

## 2026-07-27

### 新增

- 新增完整的 MiniMax 脚本转语音流程，并永久保留直接上传音频入口。语音完成后才
  创建原有视频任务，继续进入相同的按用户并发 FIFO 队列。
- 新增 MiniMax 账号配置、长期音色资产、语音任务记录和独立语音 Worker；API Key
  加密保存，不同官方账号的音色互相隔离。
- 新增独立声音管理页：声音克隆和音色融合分别制作、试听并保存，批量生成只选择
  已保存音色。已付费激活音色可以长期复用，原始样本默认只保留 48 小时。
- 语速、音量、语调、模型、语言和输出格式放在批量生成页按批选择，不作为固定用户
  配置。双人数字人继续支持直接上传音频，但不提供自动语音生成。
- 批量页面成为登录后的默认页面；单次生成仍保留为独立入口。
- 新增一键本地启动/停止脚本，后台统一守护 Web、语音 Worker 和视频 Worker；新增
  管理员运行状态与脱敏轮转日志页面，无需一直查看 PowerShell。
- Excel 和 CSV 高级清单继续与网页快速模式共用校验、权限、任务创建与队列逻辑。
- 新增迁移 `0005_minimax_audio_pipeline`、`0006_voice_studio`，服务器 systemd 模板
  增加独立语音 Worker。
- 新增数字人和视频对口型批量生成页面、清单解析 API、逐文件暂存 API、严格预检 API
  与幂等批量创建 API。
- 新增默认“快速批量生成”入口：图片/视频和音频按页面编号顺序配对，支持拖动、
  上下移动、移除文件和创建前确认。
- Excel/CSV 调整为高级导入入口，并提供精简模板；统一参数不再在每一行重复填写。
- 数字人的分辨率与单/双人模式、视频对口型的 Stand/Plus 改为批次统一参数；
  批量数字人的起止时间由完整音频时长自动计算。
- 视频对口型正向提示词统一为“人物 + 语言 + 音频中的完整台词”，移除动作、镜头和
  画面描述方面的错误引导。
- 每个清单行创建一条独立 `PENDING` 任务，按清单顺序进入原有按用户并发调度的
  FIFO 队列，不直接调用 RunningHub。
- 新增批次历史、派生进度统计、失败项重试和整批删除。创建采用全有或全无校验，
  运行结果允许部分成功；整批删除只允许在全部子任务终态后执行。
- 新增批量暂存素材记录及过期清理，默认单批 50 行、待使用素材合计 5 GB、
  暂存 24 小时。
- 新增迁移 `0004_batch_generation`，以及覆盖 Excel/CSV 解析、单/双人映射、
  原子创建、字段隔离和批次管理的测试。

### 修复

- 修复 RunningHub 私有工作流开启“加密访问”后，视频对口型任务因请求体缺少
  `accessPassword` 而提交失败的问题。
- 管理员可以为视频对口型工作流配置访问密码；密码使用 Fernet 加密保存，
  页面不回显原文，留空保持原密码，也可以显式清除。
- Worker 仅在提交 LTX 私有工作流时解密密码，并把 `accessPassword` 放在
  RunningHub 请求体顶层；密码不会写入任务输入、日志或 Git。
- 任务历史页在存在等待中或运行中任务时每 5 秒自动刷新；任务全部进入终态后停止刷新。
- 任务历史页为生成失败和下载失败任务提供“失败重试”，为所有终态任务提供
  “删除记录”。
- 普通生成失败会检查并复用原上传素材重新排队；仅下载失败会保留远程 Task ID，
  只重试结果下载，避免重复生成费用。
- 删除终态任务会同时删除数据库记录、上传素材和本地结果；活动任务禁止删除。
- 上传素材保留期从任务创建后 3 天调整为任务结束后 48 小时。
- 任务历史页新增按北京时间起止日期检索，并统一用北京时间显示任务时间。
- 任务历史页支持逐项多选、全选当前筛选结果和批量删除；批量操作包含活动任务时
  整批拒绝，避免部分删除。
- LTX 密码修复本身沿用 `workflow_configs.settings_json` 保存密文；批量功能另新增
  `0004_batch_generation` 迁移。

## 2026-07-25

本次更新把原来只支持数字人的本地 MVP，扩展为支持两个工作流、持久化队列，
并具备单服务器部署前准备的版本。

### 新增

- 新增“视频对口型”工作流，与数字人共用同一个创建任务页面、任务列表和 Worker。
- 视频对口型支持上传源视频、自定义音频和视频正向提示词。
- RunningHub 客户端支持私有 Workflow V2 提交端点。
- 视频对口型可按任务选择 Stand 运行（24G）或 Plus 运行（48G）。
- 新增按用户并发数调度的持久化 FIFO 队列。例如并发数为 2 时提交 8 个任务，
  Worker 会先处理 2 个，空出槽位后继续处理后续任务。
- 新增 CSRF 防护、可信域名校验和 `/healthz` 数据库健康检查。
- 新增 Ubuntu 单服务器部署文件，包括 Nginx、Web/Worker systemd 服务、
  定时备份、过期文件清理、生产预检和恢复脚本。
- 新增 `.env.production.example` 和完整服务器部署手册。
- 新增持续维护的 `PROJECT_STATUS.md`，记录工作流节点、当前决策和后续边界。

### 修改

- 数字人工作流移除旧的“总体模式”节点，不再提交节点 `752`，使用 RunningHub
  工作流默认的稳定模式 v2。
- 数字人实例固定为 Stand 运行（24G），前后端均拒绝 Plus。
- 统一生成页面可在“数字人视频”和“视频对口型”之间切换。
- 前端精简实现说明，并统一 Stand/Plus 的中文运行规格显示。
- 上传校验增加 MP4、MOV、WEBM 视频类型和独立视频大小限制。
- Worker 改为按用户并发槽位持续补充任务；两个工作流共享同一用户的并发额度。
- 生产环境强制要求随机应用密钥、有效 Fernet Key、HTTPS Cookie 和明确域名。
- README 从本地启动说明扩展为本地运行、数据安全和生产部署入口说明。

### 当前工作流参数

- 数字人：参考图片、总参考音频、时间范围、单/双人音频、提示词和最长分辨率；
  固定 `instanceType=default`。
- 视频对口型：源视频、自定义音频、正向提示词；音频在工作流内部控制视频长度；
  可选 `instanceType=default/plus`。

### 数据与兼容性

- 没有新增数据库模型，本次不需要新建 Alembic 迁移。
- 当前迁移头仍为 `0003_workflow_adapters`。
- 服务器部署模板已经加入仓库，但尚未连接真实服务器、域名或 HTTPS 证书。
- 当前账号由管理员创建，尚未提供自助注册、找回密码和支付功能。

### 验证

- 完整 mock 测试：`39 passed`。
- Python 编译检查通过。
- 自动化测试不会真实调用 RunningHub，不产生生成费用。

## 2026-08-07 工作台语义前景图片云端接口

- 新增独立视觉语境分析契约和工作台 API，支持具体对象、成语、比喻、否定、词语引用、
  顺带提及和不确定语境的结构化判定。
- 新增用户隔离的精确缓存与迁移 `0026_visual_analysis_cache`；素材目录、候选、模型或 Prompt
  变化均不会误用旧结果。
- 严格拒绝越界字符、未知/重复/遗漏候选、越权概念和任何模型生成的时间或路径字段。
- 视觉分析失败保持独立，不影响现有智能内容分析、语音、RunningHub 和任务数据。
- 完整 mock 回归 `272 passed`；未调用真实 Ark、MiniMax、RunningHub 或剪映。
