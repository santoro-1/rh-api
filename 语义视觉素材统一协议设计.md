# 语义视觉素材统一协议设计

> 状态：recipe v2 与 sentence-v1 已实现；catalog v3 正式迁移待人工批准
> 更新时间：2026-08-13
> 适用范围：语义前景图片、语义贴图、语义前景视频
> 关联项目：云端数字人网站 `runninghub_mvp`、本地剪映工作台 `jyd_plain_json_probe`

> 本文档只定义 catalog、asset、recipe、路径和 renderer 等底层协议。单次大模型调用、字幕边界
> 复用、精简 `visual_plan`、时间轴调度、相关素材和空镜规则，以
> 《语义视觉素材库与智能编排开发文档》为准。

## 1. 设计目标

建立一套同时支持图片和视频的语义视觉素材底层协议，使两类素材共享：

- 概念与 aliases。
- 本地关键词候选召回。
- 云端大模型语境判断。
- 素材标签和本地素材选择。
- 自动密度、重复规避和人工锁定规则。
- 浏览器预览和冻结配方。
- 素材库相对路径、版本计算和失效机制。

图片和视频只在资源描述、默认参数和最终渲染适配器上不同。

## 2. 核心设计结论

### 2.1 语义判断与具体文件分离

统一内容分析的大模型只判断：

- 哪些已编号语义位置值得进入视觉计划。
- 应展示哪个 `concept_id`。
- 本地冲突选择所需的离散优先级。

大模型不接收时间戳、全部图片和视频文件，也不直接选择 `asset_id`、媒体类型、位置或持续时间。
本地素材选择器在模型确定 concept 后，再从该 concept 可用的图片或视频中选择具体素材。

### 2.2 图片和视频共享资产模型

所有语义视觉素材统一称为 `semantic visual asset`，通过以下字段区分：

- `media_type`：媒体类型，例如 `image`、`video`。
- `renderer`：本地渲染适配器，例如 `jyd_sticker_bundle`、`video_overlay`。
- `resource`：对应媒体的相对资源路径和技术信息。
- `defaults`：该媒体的默认位置、尺寸、时长和播放规则。

### 2.3 资源与运行路径必须可搬迁

catalog、recipe 和 bundle 中不得保存运行所需的电脑绝对路径。所有正式资源路径都相对于语义视觉素材库根目录。

### 2.4 图片与视频均落在统一协议上

图片通过 catalog v2 进入真实剪映 photo 轨道；视频通过同一 catalog 和 recipe 进入原生剪映
video material/segment。两类媒体共享选材、时间占用、人工锁定和提交前校验，仅在最终 renderer
适配器中分开。

## 3. 素材库目录

```text
semantic_visual_library/
├── catalog.json
├── bundles/
│   ├── egg_boiled/
│   │   ├── sticker.json
│   │   └── resources/sticker/singleImage.png
│   └── ...
└── videos/
    ├── beef_cooking_01/
    │   ├── video.mp4
    │   ├── poster.jpg
    │   └── metadata.json
    └── ...
```

约束：

- 图片 sticker 资源继续放在 `bundles/`。
- 语义视频放在 `videos/`。
- catalog 只使用相对路径。
- 每个资源目录发布后不可原地覆盖。
- 修改视觉内容时创建新的 `asset_id` 和资源目录。

## 4. catalog v2

### 4.1 根结构

```json
{
  "schema": "jyd.semantic-visual-catalog.v2",
  "library_id": "jyd.semantic-visual-library.default",
  "concepts": [],
  "assets": []
}
```

字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `schema` | string | 固定为 `jyd.semantic-visual-catalog.v2` |
| `library_id` | string | 素材库永久唯一标识，迁移目录时不变化 |
| `concepts` | array | 语义概念列表 |
| `assets` | array | 图片和视频统一素材列表 |

## 5. concept 协议

```json
{
  "concept_id": "food.beef",
  "label": "牛肉",
  "description": "作为食物、食材或明确餐食示例出现的牛肉",
  "aliases": ["瘦牛肉", "牛肉片", "牛肉"]
}
```

规则：

- `concept_id` 永久唯一，发布后不修改、不复用。
- aliases 用于本地最长关键词召回。
- aliases 较长、较具体的词放在前面。
- concept 不包含图片或视频类型信息。
- concept 至少对应一个可用 asset，否则加载后不公开给候选召回。

## 6. 统一 asset 协议

### 6.1 公共字段

```json
{
  "asset_id": "beef.boiled.01",
  "concept_ids": ["food.beef"],
  "name": "水煮牛肉",
  "description": "一盘切片水煮牛肉",
  "media_type": "image",
  "renderer": "jyd_sticker_bundle",
  "tags": ["熟食", "牛肉", "照片"],
  "resource": {},
  "defaults": {}
}
```

公共字段规则：

| 字段 | 类型 | 说明 |
|---|---|---|
| `asset_id` | string | 永久唯一素材 ID |
| `concept_ids` | string[] | 素材可服务的一个或多个 concept |
| `name` | string | 素材中文名称 |
| `description` | string | 素材画面描述，不写营销承诺 |
| `media_type` | enum | `image` 或 `video` |
| `renderer` | enum | 当前为 `jyd_sticker_bundle` 或 `video_overlay` |
| `tags` | string[] | 用于本地素材筛选，不参与关键词召回 |
| `resource` | object | 按 renderer 区分的资源描述 |
| `defaults` | object | 按媒体类型区分的默认渲染参数 |

### 6.2 为什么允许多个 `concept_ids`

一张早餐餐盘可能同时适用于：

- `meal.breakfast`
- `food.egg`
- `food.corn`
- `food.nuts`

素材可以被多个 concept 选中，但一次 overlay 仍只保存本次模型决定的一个 `concept_id`，方便解释和人工审核。

### 6.3 tags 的职责

tags 只用于 concept 确定后的本地选择，例如：

- 内容：`熟食`、`生食`、`饮品`、`早餐`
- 风格：`照片`、`信息图`、`透明抠图`
- 动态：`烹饪过程`、`成品展示`、`可循环`
- 构图：`竖图`、`横图`、`主体居中`

tags 不作为脚本文字 aliases，避免本地召回不可控。

## 7. 图片 asset

### 7.1 图片 sticker bundle 示例

```json
{
  "asset_id": "beef.boiled.01",
  "concept_ids": ["food.beef"],
  "name": "水煮牛肉",
  "description": "一盘切片水煮牛肉",
  "media_type": "image",
  "renderer": "jyd_sticker_bundle",
  "tags": ["熟食", "照片", "主体居中"],
  "resource": {
    "bundle": "bundles/beef_boiled_01",
    "preview": "bundles/beef_boiled_01/resources/sticker/singleImage.png"
  },
  "defaults": {
    "corner": "top_left",
    "scale": 0.26,
    "opacity": 1.0,
    "duration_us": 1800000
  }
}
```

校验规则：

- `media_type` 必须为 `image`。
- `renderer` 必须为 `jyd_sticker_bundle`。
- `bundle/sticker.json` 必须存在。
- preview 必须存在且位于素材库根目录内。
- `corner` 允许 `top_left`、`top_right`、`bottom_left`、`bottom_right`、`bottom_center`、`center`。
  口播下方图片和动作小窗优先使用 `bottom_center`，与画面中轴对齐；`center + scale>=0.95`
  保留为全屏 B-roll 判定。
- scale 范围 `0.05~2.0`。
- opacity 范围 `0~1`。
- duration 必须大于 0。

## 8. 视频 asset

### 8.1 视频 overlay 示例

```json
{
  "asset_id": "beef.cooking.video.01",
  "concept_ids": ["food.beef"],
  "name": "牛肉烹饪过程",
  "description": "牛肉在锅中烹饪的近景视频",
  "media_type": "video",
  "renderer": "video_overlay",
  "tags": ["烹饪过程", "动态", "横图", "可循环"],
  "resource": {
    "video": "videos/beef_cooking_01/video.mp4",
    "preview": "videos/beef_cooking_01/poster.jpg",
    "metadata": "videos/beef_cooking_01/metadata.json",
    "duration_us": 6200000,
    "width": 1920,
    "height": 1080,
    "has_audio": true
  },
  "defaults": {
    "corner": "top_right",
    "scale": 0.32,
    "opacity": 1.0,
    "duration_us": 3000000,
    "source_start_us": 0,
    "mute": true,
    "loop": false,
    "fit": "cover"
  }
}
```

预留校验规则：

- `media_type` 必须为 `video`。
- `renderer` 必须为 `video_overlay`。
- video 和 preview 必须存在且位于素材库根目录内。
- metadata 为可选相对路径；提供时必须存在，并参与 catalog version 计算。
- 视频实际时长、宽高和音轨情况应在导入时探测，不能只信 manifest。
- `source_start_us` 不得小于 0。
- 非 loop 模式下，截取范围不得超过视频实际时长。
- `mute` 默认必须为 true，避免语义视频破坏主口播。
- `fit` 预留 `cover`、`contain`。

## 9. 素材选择策略

### 9.1 项目级媒体偏好

```text
image_only
video_only
prefer_image
prefer_video
mixed
```

当前默认：

```text
image_only
```

### 9.2 选择顺序

1. 模型返回 SHOW 和 concept_id。
2. 找出 `concept_ids` 包含该 concept 的素材。
3. 按项目媒体偏好过滤 image/video。
4. 按近期素材重复、concept 冷却和媒体可用性过滤。
5. 同一 concept 有多个素材时按稳定轮换选择。
6. 选择结果冻结为具体 asset_id。

### 9.3 mixed 模式的初始规则

当前 mixed 策略：

- 短促强调、抽象信息图优先图片。
- 动作、制作过程、变化过程优先视频。
- 同一时间只展示一个语义视觉素材。
- 图片和视频共享每 60 秒最多 24 条、20 秒同 concept 密度冷却和成片级 asset 去重；普通素材
  从关键词所在句段开头开始并最短 2 秒，顿号列举在完整句段内速切，时间区间仍不得重叠。
- 视频素材失败时允许回退到同 concept 图片。

## 10. recipe v2

### 10.1 根结构

```json
{
  "schema": "jyd.semantic-visual-recipe.v2",
  "library_id": "jyd.semantic-visual-library.default",
  "catalog_version": "sha256:...",
  "media_policy": "image_only",
  "timing_policy_version": "sentence-v1",
  "used_asset_ids": [],
  "overlays": []
}
```

### 10.2 图片 overlay

```json
{
  "overlay_id": "visual_...",
  "concept_id": "food.beef",
  "asset_id": "beef.boiled.01",
  "media_type": "image",
  "renderer": "jyd_sticker_bundle",
  "resource_path": "bundles/beef_boiled_01",
  "start_us": 1000000,
  "duration_us": 1800000,
  "corner": "top_left",
  "scale": 0.26,
  "opacity": 1.0,
  "enabled": true,
  "manual": false,
  "locked": false,
  "requires_review": false,
  "timing_source": "minimax_raw_cue_interpolation"
}
```

### 10.3 视频 overlay

```json
{
  "overlay_id": "visual_...",
  "concept_id": "food.beef",
  "asset_id": "beef.cooking.video.01",
  "media_type": "video",
  "renderer": "video_overlay",
  "resource_path": "videos/beef_cooking_01/video.mp4",
  "start_us": 1000000,
  "duration_us": 3000000,
  "source_start_us": 0,
  "corner": "top_right",
  "scale": 0.32,
  "opacity": 1.0,
  "mute": true,
  "loop": false,
  "fit": "cover",
  "enabled": true,
  "manual": false,
  "locked": false,
  "requires_review": false,
  "timing_source": "funasr_word_timestamps"
}
```

### 10.4 recipe 路径规则

- recipe 保存相对于 `library_id` 对应根目录的资源路径。
- 生成具体本地渲染任务时才解析为绝对路径。
- 浏览器公开 payload 不返回本地绝对路径。
- 已发布资源目录不得原地覆盖，以保证历史冻结配方稳定。
- `timing_policy_version`、`used_asset_ids` 及 overlay 的句段/列举/接缝字段均为 recipe v2 可选
  扩展；历史 v1/v2 缺少这些字段时仍按原冻结参数读取。

## 11. 渲染适配器接口

内部统一接口建议：

```python
class SemanticVisualRenderer:
    renderer_name: str

    def validate_asset(self, asset, library_root): ...
    def build_preview_payload(self, overlay, asset): ...
    def add_to_render_job(self, overlay, asset, library_root, job): ...
```

### 11.1 `jyd_sticker_bundle`

- 复用当前 `StickerAddition`。
- 从相对 bundle 路径解析 `sticker.json`。
- 每张图片继续生成独立贴纸轨道。
- 当前图片 MVP 先实现此适配器。

### 11.2 `video_overlay`

当前适配器职责：

- 创建视频 material。
- 创建独立视频轨道和片段，单个素材失败时作为可选增强跳过。
- 设置目标时间和 source timerange。
- 默认静音。
- 支持 cover/contain、scale、opacity 和 corner。
- 按 loop 拆分循环片段或限制 duration。
- 小窗视频与下方图片处于同一层级；全屏 B-roll 位于固定人名牌上方、字幕下方。
- 单个视频失败按可选增强隔离。

## 12. 浏览器预览

前端统一读取 overlay：

- `media_type=image`：使用 `<img>`。
- `media_type=video`：使用 `<video muted playsinline>`。
- 两者共享位置、缩放、透明度、开始时间和持续时间控件。
- 视频额外显示 source start、静音、循环和 fit 控件。
- 当前前端已实现图片与鉴权 `<video>` 预览，读取同一冻结配方。

## 13. catalog version

catalog version 继续使用 SHA-256 聚合，但 v2 应覆盖：

1. catalog 规范化 JSON。
2. 每个图片 bundle 的 `sticker.json` 和 preview。
3. 每个视频文件、poster 和 metadata。

任意实际运行资源变化都必须改变 catalog version。

## 14. v1 兼容策略

加载器同时接受：

- `jyd.semantic-visual-catalog.v1`
- `jyd.semantic-visual-catalog.v2`

v1 asset 在内存中转换为：

```json
{
  "concept_ids": ["原 concept_id"],
  "media_type": "image",
  "renderer": "jyd_sticker_bundle",
  "resource": {
    "bundle": "原 bundle",
    "preview": "原 image"
  },
  "defaults": {
    "corner": "原 default_corner",
    "scale": "原 default_scale",
    "opacity": "原 default_opacity",
    "duration_us": 1800000
  }
}
```

兼容原则：

- 现有 v1 catalog 在迁移前仍能加载。
- 内部业务逻辑只使用统一规范化对象。
- public payload 明确返回实际 schema。
- v1 recipe 仍可渲染；新分析结果生成 recipe v2。

## 15. 云端协议影响

目标方案不再保留一次内容分析之外的独立视觉模型调用。现有内容分析的一次 provider 请求同时
返回音乐意图、字幕断点和精简视觉计划。

每条视觉计划只返回：

- `anchor_id`
- `concept_id`
- `priority`

未返回的候选视为跳过。脚本摘要、catalog 版本和 schema 版本由服务器包装；时间、具体 asset、
media_type、位置和持续时间由本地工作台确定。这样既复用字幕语义边界，也避免素材数量增加后
扩大模型输出。

## 16. 实施顺序

### 阶段 A：协议与兼容加载器

1. 增加 catalog v2 常量和字段校验。
2. 保留 v1 loader。
3. 将 v1/v2 都规范化为统一内部对象。
4. 增加图片和预留视频的路径安全校验。
5. 增加 catalog version v2 计算。

### 阶段 B：图片 recipe v2

1. 将素材选择从单一 `concept_id` 改为 `concept_ids` 包含判断。
2. 默认媒体策略设为 `image_only`。
3. 生成 recipe v2 图片 overlay。
4. 渲染任务阶段解析相对路径。
5. 保持前端单条编辑能力。

### 阶段 C：迁移现有图片素材

1. 将现有 7 个素材写入 catalog v2。
2. 将已经复制的 12 个新食物 bundle 写入 catalog v2。
3. 验证 18 个 concept、19 个 asset。
4. 运行本地与云端回归测试。
5. 重启 Processor 并做浏览器/剪映草稿验证。

### 阶段 D：视频接口与适配器

1. loader 已校验 video asset。
2. selector 已按媒体策略过滤视频。
3. recipe 已冻结视频字段。
4. `video_overlay` renderer 已写入原生剪映 material/segment；单项失败仍按可选增强隔离。

### 阶段 E：真实视频渲染

1. 已实现视频轨道适配器。
2. 已实现浏览器鉴权视频预览。
3. 已实现视频截取、静音、循环和 fit。
4. 已增加混合媒体密度、互斥、回退和轨道层级测试。
5. 已导入正式运动动作视频和相关 B-roll，并生成真实剪映验收草稿；界面肉眼确认仍作为最终
   验收步骤。

## 17. 当前状态

2026-08-31 补充：下列早期 v2 数量为历史基线，不代表目前图库。JYD 当前旧库是 v3，
包含 927 张图片和 527 条视频；本地测试新增文件夹来源，保留 v3 asset 与 recipe v2 协议，
仅 recipe 增加可选 `selection_seed`。旧 JSON 显式一次性整理到分类目录和 SQLite 后，
测试运行时不再读取或混用它；默认正式 JSON 模式不变。兼容缓存使用素材库相对路径，
按内容版本保留，旧 frozen 配方不因源文件删除/修改而重选。新文件夹视频默认短片静音小窗，
不能自动取得全屏/接缝权限。详见 JYD `docs/SEMANTIC_VISUAL_LIBRARY.md`。

- 正式 catalog 已迁移为 `jyd.semantic-visual-catalog.v2`，`library_id` 为 `jyd.semantic-visual-library.default`。
- 默认库现有 37 个 concept、40 个 asset：38 个图片 asset 和 2 个视频 asset；全部使用素材库
  相对路径，原始素材均未被覆盖。
- 阶段 A 已完成：loader 同时接受 v1/v2，并统一规范化图片与视频 asset。
- v2 已校验 `library_id`、多 concept、媒体/renderer 配对、相对路径、图片 bundle、视频技术字段和默认截取范围。
- 图片、视频、poster 和可选 metadata 均纳入 catalog version；公开 payload 不泄露本机绝对路径。
- 当前统一分析选择器使用 `mixed`；静态概念优先图片，动作概念优先视频，并允许缺失媒体回退。
- 阶段 B 已完成：新分析和人工审核配方使用 recipe v2，支持 `image_only`、`video_only`、`prefer_image`、`prefer_video` 和 `mixed`。
- recipe v2 冻结 `media_type`、`renderer` 和相对 `resource_path`；只有生成本地渲染任务时才解析绝对路径。
- v1 recipe 仍可读取；旧的人工锁定图片在重新分析时会转换为 v2 相对资源描述。
- 视频字段、浏览器预览和剪映轨道适配器已经落地。正式视频包括明确语义触发的胯下击掌小窗，
  以及带 `相关素材/b-roll/enrichment` 标签的腹部核心全屏 B-roll；后者保留 42.766341 秒原片，
  通过 `source_start_us=12000000` 读取 5 秒源区间。
- 普通明确语义选材优先排除 enrichment 专用资产，实际至少间隔 6 秒的通用补充只从获准资产中
  选择；catalog v3 严格区分 `full_screen_broll` 与 `seam_broll`。所有用途共享一次云端内容
  分析、本地互斥调度和成片级 `used_asset_ids`。
- 当前下一步是由人工在剪映界面确认真实验收草稿的最终构图和播放效果，再根据观感微调素材
  或展示参数。
