[Unit]
Description=Check the three-day RunningHub backup schedule

[Timer]
# The service's persistent interval gate only permits a backup every three days.
OnCalendar=*-*-* 03:30:00
Persistent=true
RandomizedDelaySec=15m
Unit=runninghub-video-backup.service

[Install]
WantedBy=timers.target
