# 数字人与剪映工作台集成变更记录（2026-08-03）

## 当前结论

本轮修改已经在本地代码中完成并通过自动化测试；生产发布状态必须通过安全发布脚本现场确认，不能仅凭本文判断。现有账号无需迁移，也不通过本次功能修改账号数据。

系统采用“数字人网站负责账号和数字人生成，剪映工作台负责后期”的边界。工作台会拉取当前登录账号自己的数字人任务，但不会在后台自动导入、自动导出或自动发布；是否导入及最终渲染仍由操作人确认。

> 2026-08-04 后续产品决策：多片段在新版流程中允许自动拼接并默认作为当前基础视频；
> 2026-08-07 起接缝改为 0.25 秒保时长叠化。全部原始片段和自动拼接版本必须保留，用户上传粗剪成片后
> 只切换当前视频、不删除历史。该决策由后续画面合成/视频替换模块实现。基础视频完成后不得自动生成
> 变体。

> 2026-08-09 资源规格补充：数字人工作流在旧网页、新工作台 4A、批量生成和长音频拆分
> 场景中统一固定使用 RunningHub `plus`（48G）实例。历史队列中保存为 `default`（24G）的
> 数字人配方会在最终提交时自动升级为 48G；LTX 对口型的实例规格仍独立选择。

> 2026-08-10 清晰化补充：所有现行数字人新旧入口的每个分段在数字人成功后都会一对一
> 进入 SeedVR2，放大流固定 `plus`（48G）与最大分辨率 `1920`。页面、合并和工作台默认
> 消费清晰片段；数字人源片段继续保留。历史已完成任务不自动补跑，LTX 不接入放大。
> 经用户授权的真实 5 秒付费冒烟已确认：数字人源片段为 576×1024，SeedVR2 清晰片段为
> 1080×1920，真实输出节点 `97`、类型 `mp4`，音轨和 4.84 秒时长保持一致。

## 产品分流

| 输入与结果 | 后处理模式 | 工作台处理方式 |
| --- | --- | --- |
| 文本生成语音，最终只有一个未切分视频片段 | `AUTO_POSTPROCESS` / `AUTO_READY` | 可一键导入视频和精确字幕时间戳，再选择 BGM、字幕样式和保存目录 |
| 文本生成语音，实际时长超过 45 秒并产生多个片段 | 旧收件箱仍为 `MANUAL_*`；新版按项目状态聚合 | 自动拼接并默认选为基础视频，保留全部原始片段，可上传粗剪成片替换当前版本 |
| 用户直接上传音频 | 旧收件箱仍为 `MANUAL_*` | 保留原始片段；新版后续画面模块按同一基础视频版本规则处理 |

多片段自动拼接在新版流程中默认成为当前基础视频，接缝使用 0.25 秒保时长叠化。它不是
不可替换的最终成片：原始生成片段和自动拼接版本始终保留，用户上传粗剪成片时只新增
素材版本并切换当前基础视频。

## 账号与环境设计

- 数字人网站是普通用户账号的唯一来源，工作台不再维护第二套普通账号。
- 本地联调时，工作台默认连接 `http://127.0.0.1:8000`，使用本地数字人数据库中的测试账号。
- 正式部署时，只把工作台的 `digital_human_server_url` 改为正式数字人网站 HTTPS 地址；服务器现有账号无需迁移。
- 工作台不会保存用户密码。登录由数字人网站验证后签发 12 小时短期令牌。
- 用户停用、删除或修改密码后，旧工作台令牌立即失效。
- 工作台自己的素材、机器和运行参数仍由独立技术管理员维护，不与普通用户账号合并。

## 数字人网站新增接口

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/auth/center/login` | 验证数字人账号并签发工作台令牌 |
| `POST` | `/api/auth/center/verify` | 校验令牌并返回当前用户 |
| `POST` | `/api/auth/center/handoff` | 创建 60 秒有效的一次性交接码 |
| `POST` | `/api/auth/center/handoff/consume` | 消费交接码并建立工作台会话 |
| `POST` | `/api/workbench/tasks` | 拉取当前用户的数字人后处理任务 |
| `POST` | `/api/workbench/tasks/{item_id}` | 获取一条任务的后处理清单 |
| `POST` | `/api/workbench/content-analysis` | 按精确原始脚本获取缓存或一次执行音乐/字幕/视觉语义分析 |
| `GET` | `/api/workbench/tasks/{item_id}/videos/{video_index}` | 鉴权下载指定 SeedVR2 清晰片段 |
| `GET` | `/api/workbench/tasks/{item_id}/videos/{video_index}/source` | 鉴权下载对应数字人源片段 |
| `POST` | `/api/workbench/voices` | 返回当前账号可用官方/自定义音色和声音制作任务 |
| `POST` | `/api/workbench/voices/import` | 核对当前 MiniMax Key 下的克隆 `voice_id` 并登记为待激活音色 |
| `POST/GET` | `/api/workbench/voices/{voice_asset_id}/preview` | 显式生成或鉴权下载官方音色试听 |
| `POST` | `/api/workbench/voices/{voice_asset_id}/activate` | 明确确认后首次正式使用自定义音色并标记激活 |
| `POST` | `/api/workbench/voices/{voice_asset_id}/delete` | 从用户音色库移除自定义音色卡，保留历史记录 |
| `POST` | `/api/workbench/voice-creations` | 复用现有克隆/融合声音制作队列 |
| `POST` | `/api/workbench/voice-creations/{task_id}/save` | 保存声音制作结果 |
| `GET` | `/api/workbench/voice-creations/{task_id}/preview` | 鉴权试听声音制作结果 |
| `POST` | `/api/workbench/batch-assets` | 在 4A 启动画面生成时暂存当前图片 |
| `POST` | `/api/workbench/audio-batches` | 幂等创建强制审核的 MiniMax 音频批次 |
| `POST` | `/api/workbench/audio-batches/{batch_id}` | 查询当前账号音频批次及时间戳 |
| `GET` | `/api/workbench/audio-batches/{batch_id}/items/{item_id}/audio` | 鉴权下载生成音频 |
| `POST` | `/api/workbench/audio-batches/{batch_id}/items/{item_id}/retry` | 显式确认费用后重试单行 |

所有工作台接口只返回令牌所属用户自己的声音、批次和 `digital_human` 工作流记录。
官方音色无需激活；自定义音色保存后为 `READY`，首次正式 TTS 激活成功后才变为 `ACTIVE`
并允许工作台生成音频。保存动作本身不触发音色首次使用费用。外部工具取得的克隆
`voice_id` 可先通过 import 接口登记；接口会调用 MiniMax 音色列表确认它属于当前 Key，
但不会执行 TTS 或触发首次使用费用。若用户确认它已在同一 MiniMax 账号成功调用过 T2A，
可随导入请求声明 `already_activated: true` 并直接登记为 `ACTIVE`；否则保持 `READY`，之后
仍需用户显式激活。
音视频下载执行同样的归属校验，不能通过猜测编号读取其他账号素材。新版音频批次固定
停在 `AWAITING_REVIEW`，工作台同步 MP3 和 MiniMax 原始时间戳前不会创建 RunningHub
视频任务。
声音批次只接收脚本、音色和语音参数，不接收图片，也不会创建 RunningHub 子任务。
工作台在用户启动 4A 时才上传当前图片，并随 composition 请求绑定到已审核音频。
声音批次和 composition 请求均可携带 `resolution`（数字人画面最长边像素，默认
`1024`）。composition 启动前允许工作台用项目当前值覆盖声音批次初始值；一旦付费画面
交接已经开始，该值即锁定，避免同一任务在不同分辨率间静默漂移。

## 页面与状态修复

- “查看后处理清单”改为面向人的中文页面：`/batches/{batch_id}/items/{item_id}/postproduction`。
- 原有 `/api/batches/{batch_id}/items/{item_id}/postproduction` 保留为机器读取的 JSON 接口，不再直接拿它给操作人查看。
- 批次详情同时显示“当前阶段”和“最终处理方式”，避免生成中的任务提前显示成 `AUTO_READY` 或“需要人工处理”。
- 每个成功的原始片段都提供直接下载入口。
- 数字人生成和拼接之间新增 `VIDEO_ENHANCING`；默认预览、下载和合并使用
  `quality_variant=seedvr2_upscaled` 的清晰片段，源片段只作为次要诊断入口。
- 所有子任务均取消时，批次状态正确归为 `CANCELLED`；取消批次会清理陈旧的拼接状态。

## 字幕、时长与本地文件

- 文本语音保留 MiniMax 异步任务返回的原始字幕时间戳；单片段任务导入工作台时按原时间戳生成字幕轨道。
- 上传音频没有可信的文本时间轴，因此按人工分支处理，可在剪映中识别字幕并调整。
- 工作台允许预览和编辑字幕；一旦改了字幕文字、视频源或时间轴，导入的精确时间戳会标记为失效并重新生成。
- 对 RunningHub/LTX 偶发的尾部截短增加了受限尾垫处理，修复“请求约 31 秒、结果只有约 29 秒”的已知情况，同时保持单任务不超过 45 秒。
- 数字人网站与工作台本地端口分别为 `8000` 和 `8010`，可同时启动。
- 工作台输出仍保存在本地，保存目录可选择，并会记住上一次选择；本轮不接入阿里云上传表。

## 本地验证

- 数字人网站完整测试：`288 passed`。
- 剪映工作台完整测试：`369 passed`（依赖库 deprecation warnings 不影响结果）。
- 已检查 Python 语法和 Git diff 格式。
- 测试未发起付费 RunningHub 生成任务，也未修改生产账号数据库。

## 建议的本地验收顺序

1. 启动数字人网站 `http://127.0.0.1:8000`，确认旧本地测试账号仍能登录。
2. 启动剪映工作台 `http://127.0.0.1:8010/app`，使用同一账号登录。
3. 分别准备三类任务：未切分文本、已切分文本、上传音频。
4. 在工作台“数字人任务”中确认三类状态和操作按钮符合上表。
5. 对单片段任务执行“一键导入工作台”，检查视频预览和字幕时间轴。
6. 对多片段任务检查自动拼接基础视频、原始片段下载和上传粗剪替换入口。
7. 选择本地保存目录，完成一次测试渲染；确认本轮不会自动发布。

## 尚未执行

> 2026-08-05 工作台补充：最终变体按 `D:\auto\月.日\批次号` 归档，原始脚本文件与
> MP4 同目录保存，成果库按数字人账号归属查询工作台本地索引。数字人后端接口、付费任务
> 和云端账号数据库均未修改；下载接口不会开放成果根目录之外的任意本机文件。

- 未部署或重启正式服务器。
- 未把正式工作台指向云端数字人地址。
- 未自动发布视频，也未恢复阿里云上传方案。
- 未迁移、合并或删除生产数字人账号数据。

## 2026-08-08 换图重生成与字幕数字范围补充

- 工作台 4A 现在以图片内容 SHA-256 绑定每次画面生成。同图重试继续幂等；换图会保留已经
  审核的 MiniMax 音频和原始时间戳，只废弃旧图片的 RunningHub 子任务及合并结果，然后
  重新进入画面任务队列。
- 云端清单返回绑定图片摘要，本地在接收分段和基础视频前校验当前图片版本；未升级云端时
  不再静默下载旧视频，而是明确报告 `REMOTE_IMAGE_VERSION_MISMATCH`。
- 字幕本地排版在解释模型标点断点前先合并数字范围，模型错误返回 `0.` / `5到1公斤` 时，
  最终仍保留完整 `0.5到1公斤`。ASR 逐字复核继续作为最终安全门。
- 本轮只修改本地源码、迁移、文档和 mock 测试，未调用真实 MiniMax/RunningHub，未部署
  生产环境，也未修改生产账号数据库。

## 2026-08-04 模块 4A 补充

- 新版工作台在用户显式确认 RunningHub 费用后，通过工作台服务端调用本项目新增的画面
  启动接口；继续复用现有账号、审核门、媒体/视频 Worker 和任务表。
- 任务清单新增 `composition` 状态摘要及基础视频下载地址。单/多分段都会按已确认音频
  时长标准化，多分段按 `segment_index` 拼接并使用 0.25 秒叠化；原始 RunningHub 分段
  继续提供下载。叠化不缩短总时长，也不移动 MiniMax 字幕绝对时间轴。
- 失败阶段重试仅重置失败的 RunningHub/下载任务或拼接，不重做成功的付费子任务。
- 音频完成后、4A 启动前仍允许工作台替换图片；启动接口绑定最后一次选择的当前图片。
- 4A 不添加字幕/BGM、不调用剪映、不创建变体。

## 2026-08-04 模块 4B 补充

- 4B 完全由工作台后端复用其现有本地剪映 `RenderJobQueue` 完成；本项目没有新增第三套
  后端、账号、4B 队列或浏览器直连入口。
- 工作台使用 MiniMax 原始 cues 派生单行字幕，真实字体测宽，最大画面宽度 80%，中心
  距底部约 30%；可选 BGM 来自工作台现有素材库。最终结果登记为
  `composition_video`，基础视频和全部 RunningHub 原始分段保留。
- 多分段 4B 和变体任务在两个真实 MP4 之间直接写入 0.25 秒剪映原生“叠化”，不创建
  尾帧图片片段，也不变速；目标时长长于素材时按素材实际时长使用。
- 4B 失败只重试未完成的本地剪映行，不重新调用 RunningHub，完成后不自动生成变体。
- 完整自动化测试为数字人 `162 passed`、工作台 `207 passed`，未产生真实费用，未部署
  生产环境或修改云端账号数据库。

## 2026-08-05 新版时间戳时长补充

- 新版工作台的 MiniMax 时间戳音频在启动 4A 时标记为精确时间轴模式；RunningHub 结束
  时间只按整秒能力向上取整（如 `24.4 -> 25`），不追加 `0.5` 秒静音尾垫。
- 旧版上传音频和旧批量生成没有该标记，继续保留原尾垫算法。本次没有修改旧入口、分段
  算法、基础视频规范化、封面或变体算法。
- 完整自动化回归为数字人 `163 passed`、工作台 `217 passed`。

## 2026-08-05 新旧批次隔离补充

- 新增 `generation_batches.source_channel` 和迁移 `0020_batch_source_channel`。迁移前的
  所有批次归为 `legacy_web`，新工作台声音批次在创建时显式归为 `new_workbench`。
- 原网页 `/batches` 只展示 `legacy_web`；工作台任务收件箱仍可读取同账号历史数字人
  任务用于兼容，不通过请求编号或名称猜测来源。
- `new_workbench` 的单/多片段继续按已确认音频时长标准化；`legacy_web` 单片段仍直接
  使用 RunningHub 结果，旧多片段仍按原算法生成快速顺序拼接预览，不补帧、不按目标
  音频时长裁切。
- 定向隔离回归为 `35 passed`，数字人完整回归为 `168 passed`。本轮只修改本地代码和
  文档，尚未备份或更新生产服务器，也未修改生产账号数据库。

## 2026-08-06 后续内容分析契约（模块 1 已完成）

- 这是模块 0～8 原定范围完成后的新增功能，不改变现有音频切分和 RunningHub 并行生成链。
  禁止把上一段视频最后一帧作为下一段输入，避免把多段并行任务退化为串行。
- 服务商无关的 `jyd.content-analysis.v1` 已在 `app/services/content_analysis/` 定义；当前
  provider v3 在一次云端请求中返回音乐、字幕断点和三字段视觉计划。服务端仍包装公开响应
  元数据，并拒绝未知视觉锚点或概念。
- 字幕单元只包含原文字符范围、原文和语义绑定，不包含时间戳。数字人服务端严格验证原文
  完整覆盖；工作台再把字符位置确定性映射到 MiniMax 原始时间轴，并执行 11 号真实字宽
  和 80% 最大宽度硬校验。
- 模型只输出受控音乐意图，不返回文件名、路径或 `bgm_identity`。46 首音乐的硬过滤、
  评分、Top1 选择、手动覆盖和模块 6 冻结继承均由本地工作台处理。
- 模块 1 当时只完成契约、JSON Schema 和 20 项定向测试；后续模块 3、4 已补齐数字人
  服务端 Ark 配置、统一接口和缓存，工作台项目侧消费仍未开始。

## 2026-08-06 内容分析服务端模块 4

- 数字人服务端新增统一内容分析接口，工作台继续使用数字人账号短期令牌，不接触 Ark Key。
- 一次 Ark 请求同时要求 `music_intent` 和 `subtitle_units`，但服务端分别校验和保存；一项
  失败不会丢弃另一项成功结果。完整失败不形成粘性缓存，后续请求仍可安全重试。
- 缓存按用户、脚本哈希、模型、契约和 Prompt 版本隔离；脚本文字和豆包完整原始响应不写
  普通日志。默认服务端统一并发上限为 10，超出请求排队。
- 本模块只提供数字人服务端 API，工作台项目尚未开始保存分析快照、映射 MiniMax 时间戳、
  执行本地音乐 Top1 或修改 4B 预览。

## 2026-08-06 内容分析工作台模块 5

- 工作台现已消费本项目既有 `/api/workbench/content-analysis`，按 `ProjectItem` 一条脚本
  一次请求，项目批量最多并发 10 行。Excel/CSV 导入和脚本编辑不自动请求；点击工作台
  “生成声音预览”后，才分析本批声音目标中首次导入或文本变化的行。
- 工作台逐行保存音乐/字幕双状态和合法结果，脚本修改只失效该行。相同账号、相同脚本仍
  复用本项目按脚本哈希保存的服务端缓存；仅重新生成声音不会重做文本分析，单行显式分析
  重试才使用 `force_refresh`。
- 跨行失败和音乐/字幕分支失败均独立处理。内容分析失败不会重新调用 MiniMax、RunningHub
  或剪映，也不会改变已经保存的音频、MiniMax 原始 cues、基础视频或成片状态。
- 工作台智能内容分析模块 6 已在本地 4B 阶段完成字幕语义到 MiniMax 时间轴映射。数字人
  服务端仍只返回无时间戳的 `subtitle_units`；工作台校验脚本、分析、当前音频和 raw cues
  绑定版本，失败时保留 raw cues 并回退旧排版。
- 工作台智能内容分析模块 7 已接入本地唯一 Top1、项目默认/无音乐降级、手动选择优先级和
  变体冻结继承；音频变化但文本不变时不重调豆包，只按新时长重算本地选择。

## 2026-08-06 智能内容分析模块 8

- 新增跨项目契约验收，把本项目 `analyze_content` 的实际响应直接交给工作台响应复核、
  `subtitle_units + raw_cues` 映射和本地音乐匹配器。
- 验收覆盖完整成功、两种部分成功、错误索引安全重算，以及空格、换行和 `~` 精确字符；
  并复核逐行/分支隔离、并发 10、失败回退、版本失效、手动 BGM 优先和无付费副作用。
- 新增跨项目测试 `3 passed`；最终完整 mock 回归为数字人 `216 passed`、工作台
  `260 passed`。未调用真实豆包、MiniMax、RunningHub 或剪映，未修改生产环境。

## 2026-08-07 日志与可观测性规划（实施顺序 1～4 已完成）

实施顺序 1～4 已完成源码与自动化测试，尚未写入 `D:\JYD-new`、打包或部署生产环境；
实施顺序 5 仍待后续决定和执行。

### 边界与原则

- 继续保持任务数据隔离：旧网页任务使用 `legacy_web`，新版工作台任务使用
  `new_workbench`；旧网页任务记录不得显示新版工作台任务。
- 日志不按新旧入口强行拆成两套物理文件。云端继续按 Web、语音、媒体和视频服务分文件，
  但每条业务事件必须携带来源字段，并允许在管理员运维页按来源筛选。
- 区分“任务轨迹”和“诊断日志”：任务轨迹用于回答项目进行到哪一步，采用可查询的结构化
  事件；诊断日志用于程序异常、堆栈、网络和性能排查，采用自动轮转的文件日志。
- 本地工作台与云端使用同一个关联编号串联一次操作，不能把幂等请求编号兼作日志关联编号。

### 当前基线

- 云端日志位于 `data/logs/`，现有 `web.log`、`audio_worker.log`、`media_worker.log` 和
  `video_worker.log` 按服务记录。批次、语音、媒体和视频事件现已携带 `source_channel`、
  批次编号与 `correlation_id`；管理员运维页现可按“全部/旧网页/新工作台”筛选首次加载、
  历史查询和后续实时日志。最近 7 天原始日志下载不受页面筛选影响。
- 工作台现已补齐 `workbench.log`、`render.log`、`collector.log` 和独立 Agent 的
  `agent.log`。`server.log` 继续只承载启动与致命错误；四类日志统一轮转并在写入前脱敏。
- 新版工作台现可查看当前项目的简洁运行记录，并下载项目级诊断包。诊断包只收录当前项目
  的安全摘要和可关联日志，排除脚本文本、素材文件及路径、操作负载、凭据和其他项目日志；
  独立 Agent 在另一台电脑时，其 `agent.log` 需在该电脑单独取得。
- 数据库中的项目状态、任务状态和操作状态是业务结果，不等同于能够复盘状态变化过程的
  事件日志。

### 统一事件字段

本地和云端的业务事件采用同一字段契约，至少包括：

- `timestamp`、`level`、`component`、`event_code`、`status`、`duration_ms`、
  `error_code`；
- `source_channel`，取值为 `legacy_web` 或 `new_workbench`；
- `correlation_id`，由工作台在一次项目操作开始时生成并传给云端；
- 适用时记录 `user_id`、`project_id`、`project_item_id`、`operation_id`、`batch_id`、
  `batch_item_id`、`task_id` 和第三方平台任务编号。

不得记录密码、访问令牌、Cookie、API Key、完整脚本、媒体内容或第三方完整原始响应。
脚本只允许记录长度和摘要；本机路径优先记录受管目录下的相对路径或文件名。日志写入前统一
执行敏感字段脱敏。

### 目标落盘位置

主处理机打包目录统一使用 `data/logs/`：

| 文件 | 用途 |
| --- | --- |
| `workbench.log` | 工作台页面、项目操作和云端请求 |
| `render.log` | 草稿创建、剪映启动和导出 |
| `collector.log` | 草稿及本地素材采集 |
| `server.log` | 暂时保留兼容，只承载启动和致命错误 |

其他电脑上的独立 Render Agent 使用
`%LOCALAPPDATA%/JianyingRenderAgent/logs/agent.log`。本地文件默认按 10 MB 轮转并保留
14 天；云端继续保留各服务文件，具体保留周期在实施时结合磁盘容量确定。

### 实施顺序

1. 在工作台项目中建立统一日志配置，补齐工作台、渲染、采集器和 Render Agent 的持久
   日志、轮转和脱敏。
2. 在数字人网站的 Web、语音、媒体和视频 Worker 日志上下文中补齐
   `source_channel`、批次编号及关联编号。
3. 工作台生成独立 `correlation_id`，通过接口传递并由云端任务持续继承，实现本地到云端
   的全链路查询。（已完成）
4. 新版工作台增加面向用户的简洁运行记录和面向维护人员的脱敏诊断包；云端管理员运维页
   增加“全部/旧网页/新工作台”来源筛选。旧网页任务记录页面保持不变。（已完成）
5. 如需长期保存和按项目展示状态变化，再增加追加式结构化事件表；短期程序诊断仍使用文件
   日志，避免用数据库保存大段堆栈和高频轮询信息。

其中第 1～4 项均已完成。工作台项目库 schema 为版本 9，`project_operations` 持久化独立
`correlation_id`；云端迁移头为 `0023_batch_correlation_id`，历史批次使用原批次 ID 回填，
新批次始终生成独立关联号。幂等键仍只负责防重复提交，不承担日志关联职责。
完成第 4 项后的完整 mock 回归为数字人网站 `233 passed`、工作台 `274 passed`。

### 验收标准

- 分别执行一条旧网页任务和一条新版工作台任务，旧网页任务记录仍只显示旧任务。
- 云端管理员可按 `source_channel`、账号、批次、任务或 `correlation_id` 查询对应事件。
- 使用一个 `correlation_id` 能串起工作台操作、云端音视频生成、本地渲染和剪映导出。
- 模拟云端请求失败、RunningHub 失败、本地渲染失败和 Agent 离线，均有明确错误码、阶段和
  可操作提示；成功轮询和健康检查不应淹没业务事件。
- 检查日志和诊断包中不存在密码、令牌、Cookie、API Key、完整脚本及不受控绝对路径。

## 语义视觉图片与视频集成（2026-08-10）

工作台先从本地受控目录召回候选。兼容链路仍可通过 `POST /api/workbench/visual-analysis` 请求
云端只做语境判断，请求使用 `jyd.visual-analysis.request.v1`，响应使用
`jyd.visual-analysis.v1`；字符位置属于脚本索引，不是音视频时间。云端不得接收素材绝对
路径，工作台不得接受响应中的未知候选、未知概念、重复/遗漏候选或额外时间字段。

语义判断成功后，工作台优先以当前音频的 FunASR 逐字时间映射并冻结本地 recipe v2，尚未
完成精确校准时才使用 MiniMax `raw_cues` 字符插值回退；图片和视频
共用同一占用表，只在最终 renderer 分支。浏览器预览和 4B 剪映草稿共同消费该配方。视觉分支
可独立分析、重试和失败；它不重新生成声音、不调用 RunningHub，也不改变字幕、BGM、基础
视频或已有成片。

统一链路已允许把同样的本地候选压缩为 `/api/workbench/content-analysis` 的
`visual_context`，由同一次 Ark 调用返回 `visual_plan`。当前阶段不删除独立视觉接口，也不把
本地选材、raw cues 时间绑定、互斥调度或剪映写入迁入云端。工作台项目主流程已切换为统一
调用；MiniMax 时间轴或后续 FunASR 精确时间到达时只在本地重新绑定已有计划，不再次请求模型。
输入锚点会标明 `explicit/enrichment` 和短语上下文；enrichment 只有高度相关的 priority 2
结果才自动采用，弱相关结果保留审核或跳过。

默认受控库现有 38 张图片及 2 条运动视频：明确运动语义优先选择胯下击掌小窗；食物图和动作
小窗默认位于画面下方并与中轴对齐，宽度分别约 78% 和 61.5%。带
`相关素材/b-roll/enrichment` 标签的腹部核心长视频只参与 20～30 秒空窗补充。源视频保留
原文件，配方通过 `source_start_us` 截取区间。剪映与浏览器层级统一为
`下方图片/小窗视频 < 固定人名牌 < 全屏 B-roll < 字幕`，不维护人名牌显隐事件。

## 历史数字人结果补跑 SeedVR2（2026-08-10）

新版工作台修改数字人分辨率后，如果对应云端行已经有成功的数字人源片段但尚无 SeedVR2
记录，不再复用画面启动接口修改已经锁定的数字人参数，也不删除原任务重做 4A。工作台在
用户确认费用后调用 `POST /api/workbench/tasks/{item_id}/enhancement/backfill`，云端逐段把
原数字人结果登记为 SeedVR2 48G 的 `source_result_path`，原数字人 RunningHub Task ID、音频、
图片和时间戳保持不变。清晰化完成后，主分段下载与快速拼接重新只消费放大结果。

补跑接口以整行为事务边界：六段任务必须六个源文件全部存在才统一入队；任何一段不存在或
未成功时返回 409，不产生本次 SeedVR2 任务。重复请求只返回当前增强状态，不创建第二套增强
记录；增强失败重试仍只重试 SeedVR2。历史任务继续遵守“不自动补跑”，只有这次用户显式动作
才会产生清晰化费用。
